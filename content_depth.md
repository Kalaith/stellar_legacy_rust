# Stellar Legacy — Content Depth (long-term, non-deterministic goal)

This is the **standing north star** for deepening the generational-voyage experience after
the v3 redesign (`plan.md`, W1–W7 all shipped). It is deliberately **not a checklist with an
end state** — it defines *directions* of depth, a *rotation discipline*, and *quality bars*,
so that any number of iterative passes (human or `/loop`-driven) can keep adding content
without the game ever feeling "done wrong." There is no finish line; there is only "deeper
than last iteration, still coherent."

## North star

> A 300–600-year charter should play like a **campaign that was written by the voyage
> itself** — every run surfacing beats the player hasn't seen before, every crisis traceable
> to a decision or a neglected subsystem, every returning generation measurably different
> from the founders. Depth means **more distinct situations**, **more consequence coupling**,
> and **more voice** — never just bigger numbers.

## Hard rules (inherited, non-negotiable)

1. **Data-driven everywhere.** New content lands in `assets/*.json` (+ tuning in
   `assets/data/game_config.json`). Rust changes are for *new mechanics/schema*, never for
   embedding content. Missions are never hardcoded in Rust. **Events are split one file per
   family under `assets/events/<family>.json`** (merged at load, duplicate-id-guarded) — add a
   new event to its family's file, not one monolith.
2. **Determinism of the *tick*, not of a *played run* (relaxed by the real-time loop — read
   this).** All randomness still flows through the state-owned `sim.rng` (never `get_time` /
   `miniquad::date::now` / `Math.random` — the one entropy source is the startup `srand`, and
   `fixed_seed` in `game_config` overrides it for testing). So a **hand-stepped `advance_*`
   replays identically** for a seed, and the seeded campaign skeleton at LAUNCH is reproducible.
   But a **live real-time run is no longer a strict seed replay** — the wall-clock auto-advance
   cadence and the 30 s decision-timeout picks depend on player timing. Do **not** re-assert
   "same seed ⇒ same *played* run," and do not add a second entropy source. See "Established
   mechanics" below for the RNG-stream lessons this creates for tests.
3. **Structure before volume.** If a content idea needs a schema field, add the field
   (`#[serde(default)]`, back-compatible) and one exemplar, then grow the catalog in later
   iterations.
4. Repo constraints hold: 800-line file limit, no `mod.rs`, UI stays a pure view layer
   pushing `UiAction`, clippy `-D warnings` clean, soak/playthrough tests green.

## Established mechanics (real-time loop) — build on these, do not reverse

A run of mechanics passes (2026-07-23, *after* iteration 121) reshaped the frame the content
sits in. These are **load-bearing**: the next content round must build on them, not silently
undo them. Each lives in `game_config` and flows through the seeded tick.

- **Time is real-time while under way; frozen while docked.** The month clock auto-advances
  (`real_time.seconds_per_month`, with a Pause / 1× / 2× / 3× selector); there is **no manual
  "advance the year" key or button** anymore. The tick itself is unchanged in *what* it does
  each simulated month — beats, events, economy all fire exactly as before — only *who drives
  it* changed. Do not reference "press Space / one press = one year" in content or help text.
- **Every decision auto-resolves.** A blocking event or dilemma left unanswered for
  `real_time.decision_timeout_secs` (30 s) auto-picks a **random available option** (through
  `sim.rng`). So every event must stay **survivable if left to chance**: never author one whose
  random default is catastrophic-only, and keep the available outcome set sane (autoplay *and*
  the timeout both draw from `available_outcome_indices`).
- **Population tolls are ranged, and can take named characters.** `apply_outcome` rolls an
  outcome's `population_delta.count` within a ±`real_time.impact_variance` band (the modal shows
  "~X–Y souls may be lost"); a loss ≥ `mortality.event_death_loss_threshold` may also claim a
  crew officer or relative (`mortality::event_claim`). **Author population deltas as the *centre*
  of the intended range**, not the worst case — the engine spreads and may escalate it.
- **Characters age and die continuously.** Aging is a yearly "Founding Day" (`mortality::
  annual_aging`); death is a monthly age-scaled roll (`mortality::monthly_tick`); the dynasty
  self-renews toward `mortality.dynasty_target_size` via yearly births. `succession` and crew
  no longer age or kill — that is `mortality`'s job. Content must **not** assume a static roster
  or that the founding leader persists. New voice pools exist: `flavor.crew_death`,
  `flavor.fuel_gain` (periodic fuel-scoop report, `flavor.fuel_report_gap_years`).
- **Tabs/screens split by voyage state.** `Screen::tabs(in_port)` returns different sets
  (DRYDOCK + MARKET docked; CONTRACT under way); the SHIP tab is a shipyard docked and a
  status readout under way. Menu flow is Boot → title/main menu (Continue / New Game / Settings
  / Exit) → new-game picker. A new screen must honor this split.

### Lessons (apply to every future pass)

- **RNG-stream sensitivity.** `mortality::monthly_tick` consumes `sim.rng` *every month*, so any
  test or harness that advances many years must (a) **resolve or clear the pending decision each
  step** (else `advance_months` trips its `has_pending_decision` debug_assert), and (b) **not
  assert on exact seeded event identity/order** across long spans — assert on *invariants*
  instead (a living dynasty is always led; a beat eventually fired; a stat crossed a band; a
  fuel report appeared). The autoplay soaks already follow this — copy their loop.
- **Any attrition mechanic needs a renewal counterweight.** Continuous death without matching
  births makes extinction *inevitable* (learned the hard way — every voyage died out until the
  yearly birth model landed). A new mechanic that kills dynasts (a plague arc, a mortality
  event, a harsher curve) must be weighed against `dynasty_target_size` / `annual_birth_chance`,
  or it silently guarantees a game-over. Tune in the `mortality` block, never in Rust.
- **Docked = timeless is a design lever, not a bug.** Repairs, refit, market, and charter
  choice happen with the clock stopped; only the crossing runs against the hours. Content that
  wants to pressure the player with time belongs *under way*, not in port.

## Baseline

*Origin baseline 2026-07-21: 72 events, 10 charters, 6 factions, 6 subsystems, no event gates beyond phase/year/generation/drift.*

| Axis | Current (updated 2026-07-23, after iteration 121) |
| --- | --- |
| Events | **259** total (every family ≥12), split per family under `assets/events/` — legacy_drift 60, survival 47, engineering 30, diplomacy 30, biology_medical 21, comedy 16, mystery 15, ethics 15, science_anomaly 13, exploration_first_contact 12; **events carry state-gated complications** (a twist + extra consequences when the ship is in a matching state; gateable on drift/subsystem/faction/consequence/shortage/**recurrence**/**lived-state** (`max_population` thinned crew, `min_lean_food_years` chronic hunger — so a crisis reads and bites differently where the ship is least able to bear it: the failing air on a skeleton crew too few to hold every deck, a ration triage on a ship with no fat left to cut) — first-match rides, so an event can play several ways) — and a complication can be **choice-targeting** (`applies_to_outcomes`: the twist always *shows*, but its extra toll lands only on named outcomes, so a twist can punish a *specific* decision — deferring a crack again on a ship that already defers costs extra hull, fixing it escapes it); **8 events are three-way trilemmas**; **outcomes can be state-gated** (`EventOutcome.requires` — an outcome offered only to a ship that earned it: a subsystem-knowledge floor, a past consequence, *or a reputation* (`min`/`max_reputation`), appended after the always-shown base choices so index-0/autoplay is untouched, the modal renders only the available ones — so a kept-expert bay unlocks a clean reactor fix, a banked war chest unlocks a shipyard repair, and **a good name or a feared one each unlocks a choice the other lacks** (the-wary-encounter: a merciful ship trades on its good name, a feared ship lets its name intimidate — leverage a no-name ship can't reach)); **chains fire two ways** — opportunistically (`requires_consequence`) or on a **determined clock** (`schedule_followup` → a `scheduled_only` payoff at a fixed year); **recurring crises escalate** — `event_fire_counts` lets a complication gate on `min_prior_occurrences`, so the third outbreak of the same plague wears the ship through where the first was routine |
| Voice | data-driven flavor: obituary/succession/coming-of-age (per-generation, **deepened for the real-time-loop continuous-mortality cadence** — deaths and leader-successions fire dozens of times a voyage now, not once a generation, so the two most-hit pools were doubled: obituary 6→14, succession 6→14, coming_of_age 5→10, crew_death 4→9, indexed by member/crew id so a long crossing rarely repeats a farewell), **crew-turnover pools both ways** (retirement + appointment + training), extinction prose, ambient dead-air lines, phase-transition lines, homecoming prose keyed by success level, **recurring-crisis pools** (per-year famine + fuel-stall), **faction-mood pools** (a people crossing into restlessness/contentment says so once), **the ship-wide morale voice** (the *collective* twin: when the whole crew's morale crosses into a grim or a buoyant band the decks say so once — `announce_ship_mood` mirrors `announce_faction_moods`, `SimState.morale_band` tracks it, launch baseline recorded not announced — the natural voice for the morale it73 just gave a maintenance lever), **the polity-mood voice** (the ship's *political climate* — the member-weighted aggregate mood of the aboard peoples, it100's `aboard_approval_mean`: when the polity crosses into broad discontent or broad ease it says so once, `announce_polity_mood` + `polity_mood_band`, distinct from the crew's *spirits* (morale) and any one people's mood — a ship can be high-spirited yet politically restive), **the reputation voice** (the ship remarking that its *name* has begun to mean something — a merciful or a feared reputation crossing a *gentle* band (0.66/0.34) says so once, `announce_reputation_name` + `reputation_voice_band`, the quiet marker before the it109 beat's defining reckoning: corridor talk half-proud/half-wary that the dark now greets the ship differently), **the governance voice** (the ship's *institutions* crossing a band — the fourth ship-level stat-voice and the governance twin of the morale and polity ones: when `stability` slips *into* a fraying band (`stability_voice_low` 0.4, gentler than the it102 collapse *beat* at 0.3, so the voice — a government noticed slipping — precedes the reckoning) or firms *into* good order (`stability_voice_high` 0.7), one pooled line surfaces — quorums missed and offices unfilled and decisions drifting, or the councils working and the charter honored in practice — `announce_stability_mood` + `stability_voice_band`, distinct from the crew's *spirits* (morale) and the peoples' *contentment* (polity): a ship can be high-spirited and fairly-governed yet institutionally slipping; the launch band recorded not announced, so a founding ship's sound government reads as baseline, and via the it108 security recovery a well-kept corps can *firm the institutions back up* and the decks hear it), **subsystem-maintenance pools** (repeatable field-repair + knowledge-training verbs); **drift-aware ambient** (past the drift threshold the quiet dead-air lines draw from a *drifted* pool — the lived-in texture gone alien: planets remembered as myth, the founders spoken of like gods, rites no one recalls ordering — so the log itself reflects how far the people have come), **population-aware ambient** (once the crew thins past `ambient_population_threshold` 500 the quiet draws from a *hollow* pool — the same texture gone sparse and echoing: corridors built for thousands walked by hundreds, a mess hall serving forty in one warm corner, sealed cold residential rings — and it *precedes* the drifted pool, emptiness the louder note in a silence; the ambient twin of the it81 thinning beats), **scarcity-aware ambient** (once the ship has been lean for `ambient_lean_years_threshold` 10 years the quiet draws from a *lean* pool — the texture gone thin and rationed: the fast mess line and measured plate, grow-decks run at a joyless pitch, festivals whose feast is a gesture, hunger moved in as a low constant tenant; the ambient twin of the it89 long-hunger mechanic), **prosperity-aware ambient** (the *first positive-condition* ambient — once the larder has stood full for `ambient_fat_years_threshold` 10 years *and* no grimmer note holds, the quiet reads *fat*: second helpings without a thought, grow-decks at a comfortable idle breeding a sweeter fruit, round-cheeked loud children, quarrels that stay small, laughter the loudest thing aboard — so a ship's good years finally *sound* good; the ambient mirror of it96 sustained plenty) — precedence **lean > hollow > drifted > fat > ordinary** (grim notes first, loudest to quietest; a prosperous-but-not-grim ship reads fat; only a ship neither reads plain ordinary) — **the milestone and council-summons lines are pooled too** (voice round 19 — the two loudest remaining form letters: `Milestone reached: X` fires several times a charter across many charters, and `Council decision required: X` preceded *every* blocking decision, dozens a voyage; both now draw from an 8-line pool substituting `{milestone}`/`{title}`, indexed by log length so consecutive fires vary, data-load-guarded to carry their slot) — every frequent log line is pooled, and the ambient now reads the ship's identity |
| Event gates | phase, min_year/gen/drift, **min_morale** & **min_unity** (recovery/cohesion honest gate) & **max_legacy_loyalty** (covenant-lapse honest gate) & **max_stability** (governance-collapse honest gate) & **min_objective_fraction** & **max_population** (crew-thinning honest gate) & **max_dynasty_size** (near-end-of-the-*line* honest gate, the dynasty twin of max_population — content surfaces only once the founding dynasty has dwindled) & **min_lean_food_years**/**min_fat_food_years** (chronic-scarcity / sustained-plenty honest gates), **era ceilings** (max_year/max_generation), **consequence chains** (`requires_consequence`) *and* **consequence bars** (`forbidden_consequence` — the negative gate: a disqualifying history keeps an event out of the pool, so a choice permanently closes a door — trust barred to a known oathbreaker, a founding vindication barred to a ship that buried its record), **graded reputation** (`SimState.reputation` — the ship's cumulative *character*, a continuous 0-1 trait where consequences are booleans: outcomes nudge it a little (`reputation_deltas`) so a *tendency* builds across a hundred choices, and events gate on it (`min_reputation`/`max_reputation`); **now multi-trait** (the `SimState.reputation` map was always keyed by id, so the character is *multi-faceted*, not one dial): the **mercy** trait, lifted by taking castaways aboard / sharing a thin table and lowered by the cold choices, opening a merciful ship's door — the-reputation-precedes-us, a stranger who trusts your name — or a feared ship's — the-feared-name, a station that deals with you the way one deals with something dangerous; **and the orthogonal `resolve` trait** (the ship's name for *steadfastness* — seeing things through, holding its nerve — built by holding a contested line (the-line-in-the-dark) and lowered by yielding it, entirely separate from mercy so a ship can be kind-and-yielding or cruel-and-unbending: a steadfast name is *leverage* — the-unblinking-ship, a rival that backs down from a hull famous for never folding (and may be met with generosity, which *builds mercy* — the two traits interacting) — while a yielding name is a *liability* — the-ship-that-folds, a hard party that names a worse price expecting this hull to blink, endured as a tax or broken by a costly stand that begins reversing the name)), **charter tags**, **dominant faction**, **factions aboard**, **subsystem knowledge-below** & **condition-below**, **provisioning shortage** (food/fuel/parts/energy) *and* **provisioning abundance** (`food_above`/`credits_above` — the first gates keyed to plenty, the mirror of the shortage set: a fat-years choice only surfaces on a genuinely flush ship) *and* **chronic scarcity / sustained plenty** (`min_lean_food_years`/`min_fat_food_years` — persistence gates, not instantaneous state: `SimState.lean_food_years`/`fat_food_years` count consecutive years the store sits below the lean line / at-or-above the fat line, each reset when the condition ends, so content tells a lean *generation* from one bad winter, and a *lifetime* of plenty from one bumper year) — **and `lean_food_years` now carries a *systemic* bite, not just gates** (`chronic_hunger_morale_drain` — the provisioning axis's first tick-level coupling: once the ship has been lean past `chronic_hunger_years` 5, the year tick drains a little morale every year the hunger holds, so a grinding multi-year scarcity doesn't merely gate content and read hungry in the ambient but *mechanically wears the crew's spirits down* — and via the it103 ship-mood voice the decks audibly go heavy as it does; one bad winter stays below the threshold, the acute famine events' domain) — **and now it wears the *body* as well as the spirit** (`chronic_hunger_death_bonus` — the provisioning axis's coupling to the real-time-loop mortality system: while the ship sits lean past `chronic_hunger_years`, a flat monthly death toll is added to every character's age curve, so a hunger that grinds on for years thins the roster, the old and weak first; the infirmary (it131) still eases it and the hard age cap still holds — the *winnowing* the survival event of the same name dramatizes) — **and plenty now has the mirror pole: a long fat spell fills the cradles** (`sustained_plenty_birth_bonus` — where a sustained lean thins the roster with extra deaths, a sustained plenty past `chronic_hunger_years` *multiplies the yearly dynasty renewal* (`× 1 + bonus`), a well-fed generation raising more of its young; a second lever on renewal beside the habitat (it152), so the two provisioning poles now bracket the population — hunger kills, plenty breeds — the *full cradles* survival event dramatizing the fat-generation problem) — **and plenty now lifts the crew's *spirits* too** (`sustained_plenty_morale_lift` — the morale mirror of `chronic_hunger_morale_drain`, on the same sustained threshold: a fat spell held past `chronic_hunger_years` eases morale a little each year, completing the provisioning→morale pole so hunger and plenty bracket the crew's spirits exactly as they bracket its numbers) |
| Charters | **21** in `assets/contracts.json`; **5 topological shapes** (standard, 2 long-station, double-hop, return-dominant, outbound-dominant); **6 objective kinds** (mining, colonization, exploration, rescue, **diplomacy**, **salvage**); **destination-flavored event pools** keyed to 11 tags (incl. **embassy/`inhabited`**); **charter beat-pool bias** (`beat_families`); **scripted timed beats** (`scheduled_beats` force `scheduled_only` events at fixed voyage years); the first **scripted-narrative charter** — the_sunset_relief, a dying-colony evacuation architected around a *two-act* scheduled tragedy; **route hazard** (`hazard` adds to the immediate-crisis category weight for the whole voyage, so a lawless derelict field / a star's reach *breeds more crises* than a quiet survey and pays for it — the risk-profile lever: hollow_fleet 0.28, coronal_tap 0.2); **route annual toll** (`annual_toll` — hazard's *deterministic* companion: where hazard breeds more crises, this is a steady per-year drain applied the whole voyage, so a route whose *nature* wears at a ship *feels* it continuously — coronal_tap's star-reach bleeds morale + hull, hollow_fleet's ship-of-the-dead haunt wears morale + unity; read from the template in the year tick, load-guarded to a survivable headwind); **in-world availability gates** (`requires_faction_aboard` — the writ board reflects *who this ship is now*, not just the dynasty's cross-campaign renown: a mission a people is uniquely trusted with appears only while they're aboard and vanishes if they leave; the board locks & labels it "NEEDS <people>", the select action enforces it; exemplar the_seedbearers_writ, a seedworld greening entrusted only to a hull carrying the Verdant Kin); **charter arcs & deed gates** (`completion_consequence` — a charter seen through to full term records a mark on the sim; `requires_consequence`/`forbidden_consequence` on charters — so completing one mission *unlocks a follow-on* (or a dark deed *bars* delicate work): the first arc is deep_vein_survey → `karst_veins_proven` → the_karst_works, a permanent extraction settlement offered only to a hull that proved the belt *and* kept its word); **completion rewards** (`completion_reward` — a mission seen through leaves the ship a lasting *capability*, chiefly subsystem boons kept across voyages, distinct from its pay: the Karst Works masters extraction (+engineering knowledge), a greening deepens the gardeners' craft (+agriculture), a star-dive enriches the archive (+education); `apply_completion_reward` in the conclude path, so charters differ in the legacy they leave, not just the cash — *and* the mission **shapes the ship's character** (`completion_reward.reputation_deltas`: a voyage carrying refugees deepens mercy, one of cold enforcement hardens it, so the missions a reputation *unlocks* (it106) then *build it further* — a self-reinforcing character spiral through the mission cycle) — **and now the mission cycle also feeds the faction-goodwill well** (`completion_reward.faction_approval_deltas` — seeing a charter through can leave the peoples it served *delighted*, lifting the named aboard factions' approval: the Seedbearers' Writ warms the Verdant Kin whose dream it is, the Karst Works the Steel Covenant makers who raised it, the Sanctuary Run the communal Hearth who blessed carrying refugees home — so a mission a people is uniquely trusted with (it112) can leave that people glad enough to trigger the round-19 `faction_approval_above` gift beats, closing the loop charters↔factions↔events)) — **and a mission can leave the ship a lasting *piece of kit*, not only stats** (`completion_reward.grant_component` — the charter-side mirror of an event's salvage grant: a completed writ can recover a real ship component into the salvage hold, to be fitted in drydock, so a salvage run's prize is tangible capability and not just pay. Exemplars: **the Long Tow** pulls an experimental `warp_coil` from the dead titan it hauls home, **the Hollow Fleet** cuts a `spinal_railgun` from a derelict warship — data-load-guarded to real components); **and abandonment consequences** (`abandonment` — the negative mirror of the completion reward, and the **first charter effect keyed to *failure*** (all prior effects fired on success): a charter concluded at `SuccessLevel::Failure` — defaulted or given up — marks the ship's *name* rather than leaving a legacy, and the completion mark/reward is now correctly gated to a non-Failure conclusion (a charter not seen through earns neither); it composes with the it119 `resolve` trait, since abandoning a writ is the ultimate *folding*: **the sanctuary run** given up hardens the mercy the crew couldn't keep (−mercy) and earns a name for folding (−resolve), while **the hard contract** given up is a *merciful fold* (+mercy — the ship that would not, at the last, strip a home — but still −resolve, a hull the dark marks as one that quits); `apply_abandonment`, a pure fn tested directly, zero soak risk since autoplay completes its charters); **reputation gates** (`min_reputation`/`max_reputation` on charters — the writ board reflects the ship's *cumulative character* (it105), not just its crew or deeds: **the sanctuary run** (a refugee exodus entrusted only to a hull famous for mercy, `min_reputation mercy 0.6`) and **the hard contract** (cold enforcement/asset-recovery work a client hires only to a ship known not to flinch, `max_reputation mercy 0.35`) — so who the ship has *become* opens some jobs and closes others) |
| Campaign beats | seeded skeleton (data-driven pools + **three-era layering**) + **identity-stat threshold beats** (rising drift, rising adaptation, descending unity-crisis, rising morale-flourish, **descending legacy_loyalty-collapse** — the founders' covenant lapsing, content the-lapsed-covenant/the-advisory-founders gated on the honest `max_legacy_loyalty`: the *political* distance from the founders, distinct from the drift beats' *cultural* one — **and descending stability-collapse** — the ship's own *institutions* failing (councils that can't reach quorum, offices unfilled, the charter gone to folklore), content the-ungoverned-ship/the-hollow-offices gated on `max_stability`: governance failure, distinct from unity's *social* fracture and loyalty's *founder-reverence* — every population stat now has a beat) + **the recovery beat** (the crisis beat's hopeful mirror: once the ship has fractured and `unity` climbs back above `recovery_beat_threshold` 0.6 — a hysteresis band clear above the crisis line — a beat marks *the mending*, and resets the crisis counter so a relapse re-arms the collapse beats; cohesion now has a full fall-and-rise arc on one stat; content the-mending/the-forged-unity gated on the honest `min_unity`) + **objective-progress beats** (the mission's `objective_fraction` crossing 0.5/1.0) + **the homecoming beat** (forced once the charter enters its Return leg — the first beat keyed to a voyage *phase*: the climactic identity reckoning as a generation turns for a homeport that still remembers the founders it no longer resembles) + **the depopulation beat** (the crew's *headcount* — the one major state dimension no beat watched: as the population falls past founding-fractions 0.6/0.4/0.25, a survival beat is forced — the sealed ship's defining slow tragedy of a crew that only ever thins, marked at its stages; campaign-scoped counter so a recruited-up ship never re-marks a passed stage; content the-thinning-decks/the-hollow-muster/the-last-quarter gated on the honest `max_population` ceiling) + **the reputation beat** (the first beat on the ship's *cumulative character* (it105) rather than a population stat: when the `mercy` trait crosses *into* a strong band — famously merciful ≥0.78 or feared ≤0.22 — a beat is forced, the ship reckoning with the name it earned by a hundred choices and by who ruled it (it107); `SimState.reputation_beat_band` tracks it, a return to the middle re-arms; content the-name-we-carry/the-name-we-earned gated on `min`/`max_reputation`) + **the subsystem-collapse beat** (the beat lattice's first trigger keyed to a *subsystem's condition* — the physical-crisis dimension no beat watched, where every prior beat keyed to a stat, time, phase, the objective, or a political change: `subsystem_beats` names (module, red-line, family) triples, and the first tick a listed module's condition falls to/below its threshold a beat is forced from its family, *guaranteeing* the reckoning where before only a reactive `condition_below` event might roll; campaign-scoped `SimState.subsystem_beats_fired` tracks it by id so a repaired-then-re-collapsed module doesn't re-mark; the first exemplar is the **engineering keystone** at condition ≤0.25 — the it62 keystone failing means the whole ship's decay now outruns its own repair, a death-spiral, so content the-mending-lost/the-cannibalized-ship gated `condition_below 0.25`: strip the ship to rebuild the core, or accept a hull that will only ever wear down; eat a redundant system to feed the bay, or hold every system whole as all of them fail) + **the power-transition beat** (forced when the ship *changes hands* — the first beat keyed to a *political* event rather than a stat/time/phase: demographic drift grows a launch-minority into a decisive majority, or a schism unseats the largest people, and `clear_majority_faction` — largest, but only past a 1.1× margin so tie-break wobbles don't count — differs from the one last marked; the new majority reckons with inheriting a ship built in another people's image; ties into it72 demographic drift) + **periodic anniversary beats** + **dead-air backstop**; plus the it56/57 **scheduled beats** — pacing now keyed to time, every identity stat, mission accomplishment, voyage phase, who runs the ship, *and* **the death of the captain** (the **succession beat**, round 18 — the first beat keyed to the real-time-loop continuous-mortality system and to the death of a *named person*: the month a sitting leader dies in office, `TickReport.leader_died` forces a `succession_beat_family` beat so the ship reckons with an untried command; a planned retirement does not fire it) **and its hopeful mirror, the long-reign beat** (round 19 — once a sitting leader has held the first chair for `long_reign_years` 35, a beat is forced: the ship reckoning with an era under one enduring hand, a thing grown rare now that continuous mortality takes most leaders young; `Dynasty.leader_reign_years` counts it up each Founding Day, `long_reign_marked` fires it once, a succession re-arms it — so leadership now has a beat at *both* poles, the captain lost too soon and the captain who would not leave) **and the third leadership beat, the dynasty-crisis beat** (round 20 — the first beat keyed to the *dynasty's* headcount, not the population's: when continuous mortality outruns the renewal and the founding line dwindles to `dynasty_crisis_size` 3, a beat marks the ship staring at the end of the family that has led it since dock; `Dynasty.dynasty_crisis_marked` fires it once, re-arming only when the line is restored to its target — the emotional payoff of the extinction the mortality system made possible, with an honest `max_dynasty_size` event gate so the "last of the line" content surfaces only on a dwindled dynasty) |
| Factions | **6** authored (pick 3); **per-faction approval meter** (`FactionState.approval`, 0.5 at launch) — event choices shift it (`faction_approval_deltas`), and a people slighted past a threshold generates its own withdrawal (`faction_approval_below` gate), so *how you treat a people* decides whether it stays (exemplar: the First Flame grievance→withdrawal loop, recoverable by buying goodwill back) — **and now goodwill has a *positive* gate too, not only a grievance one** (`faction_approval_above` — the mirror of `faction_approval_below`: an event surfaces only while the named people is aboard *and delighted* (approval ≥ threshold), so a well-treated people *gives back*. Exemplars: **the Hearth's feast** (a glad Hearth Union throws open the long tables — a ship-wide morale/unity lift for the cost of the stores, or a modest observance) and **the volunteered works** (a proud Steel Covenant rebuilds the engineering bay on its own time for only the materials — a subsystem boon no paid contract could buy, or a spurned gift that costs their goodwill). So the approval meter is no longer only a fault line to avoid but a well to draw on: treat a people well and it offers the ship the best of itself); each has a signature beat + a fracture path (schism / friction→fracture); **both roster-change directions exist** (schism sheds a people; merger folds one in) — **and a departure now costs the ship the departed's *craft*, not just their heads** (`departed_faction_knowledge_loss` — when a schism or withdrawal loses a whole people, the module they tended (`FactionDef.tended_subsystem`) loses a chunk of its living expertise in `remove_faction`, the ones who truly understood it gone; so mistreating a people into leaving strips the knowledge it carried, feeding the knowledge-crisis events and leaning on the education keystone's slow re-teaching — the factions↔subsystems coupling from the *loss* side, the mirror of the recruit boon's gift); 6 friction pairs + 2 mergers; **persistent rivalries** (`FactionDef.rivals`, authored symmetric — the friction pairs made a lasting relationship: favoring one people via an event's approval *gain* sours its aboard rivals by `rival_approval_spillover` 0.3, so the approval meter is a *balancing act* — you cannot please a people without a cost to those it quarrels with; a slight is not a gift to rivals, the mechanic is the cost of *favoritism*); **and standing alliances** (`FactionDef.allies`, authored symmetric and never overlapping a rivalry — the *positive* twin of rivals: favoring a people via an event's approval *gain* also warms its aboard allies by `ally_approval_spillover` 0.2, so the meter is not only a set of fault lines but a landscape of **coalitions** — courting one people is a gift to its kin as much as a slight to its rivals; the natural kinships are the r5 merger pairs, the communal **Hearth ↔ the garden-tending Kin** and the **Accord's courts ↔ the Covenant's forges**, so building a bloc lifts more than the one people you named); **faction-colored event reactions** (complications gated on dominant faction); **recruitable pool personalities** — each people brings a signature one-time `recruit_boon` dowry (makers→engineering, gardeners→agriculture, Hearth→morale/unity, …) so which you take aboard matters; **the dominant faction steers identity drift** (`ideology` scales the yearly voyage-drift — a tech-embracing majority drifts faster, a tradition-bound one slower); **and the dominant faction bends the ship's *reputation*** (`reputation_leanings` — the standing character of who runs the ship drifts its it105 traits over the generations, no dramatic choice required: a kind majority (Hearth +0.5, Verdant Kin +0.3 on `mercy`) grows the ship a merciful name, a cold one (Ascension −0.4, Steel Covenant −0.2) hardens it, scaled by `dominant_reputation_lean_per_year`); **faction-specific dilemma outcomes** (`DilemmaOption.dominant_faction`/`dominant_faction_odds` — the dominant people backs or hinders a *legacy dilemma's* gamble, shown honestly in the modal); **aggregate approval steers the ship's cohesion** (`approval_unity_coupling` — the faction system's first coupling to `unity` itself: each year unity drifts by the member-weighted mean mood of the aboard peoples, so a content polity steadies the ship and a resentful one erodes it *toward the very crisis the beats watch for* — mistreating your factions doesn't only risk their departure, it frays the whole ship's cohesion); **and a divided ship is harder to *govern*** (`ideology_spread_stability_penalty` — the faction system's coupling to **stability/governance**, distinct from the approval→unity one: it reads not how *content* the peoples are but how ideologically *split* they are — the member-weighted MAD of the aboard peoples' `ideology` (`aboard_ideology_spread`), so a coalition spanning the tech-embracing↔tradition-bound spectrum past `ideology_spread_threshold` 0.6 strains the institutions and erodes stability each year, while a single-minded ship governs freely; a ship can be *content yet fractious* (high approval, wide spread) — the two couplings are orthogonal — and a well-kept security corps (it108) can offset the drain, so a diverse ship *demands* good governance to stay stable; self-limiting via it72, as the augmented Ascension (the +0.9 extreme) dwindles the spread narrows); **demographic drift** — the balance of power is no longer fixed at launch: each people's share waxes/wanes per generation by `FactionDef.growth_bias` (the fecund Hearth +0.03 grows toward the majority, the augmented Ascension −0.04 dwindles), so a launch-minority can become the majority and **the dominant faction — the lever behind drift/dilemmas/gates — can flip mid-voyage**; **approval bends that growth** (`approval_growth_factor` — the link between the r8 approval meter and r11 demographic drift: each generation a people's bias gains `factor·(approval−0.5)`, so a **beloved people waxes toward the majority and a resented one wanes even beyond its nature** — how you treat a people decides not just whether it leaves but whether it grows or fades over the centuries; neutral 0.5 approval is inert) |
| Subsystems | **6** (medical, life-support, agriculture, security, education, engineering) with decay + knowledge-gated repair; events `subsystem_deltas` condition/knowledge; **every module has a knowledge-crisis + a condition-breakdown beat** + tier flavor (18 lines) + graduated engineering failure (creep 0.55, seize 0.3); cross-couplings at three levels — **event**: agriculture→medical, engineering→medical cascade chain; **systemic tick (ship)**: the engineering bay is the **keystone** — its condition scales every other module's yearly decay **and, now, the ship's fuel burn** (`engineering_fuel_burn_penalty` — the subsystem axis's coupling to *fuel*/provisioning: a well-tuned bay burns the drive clean, a failing one burns rich, scaling the per-travel-month burn by `1 + penalty·(1 − condition)`, so neglecting the keystone speeds every module's decay *and* drinks the tank faster, a compounding cost paid in the one currency the dark does not refund); **systemic tick (people)**: each module has a **tending faction** whose approval erodes while its module sits neglected (rot → approval → withdrawal) **and, reciprocally, whose approval scales its module's decay** (`tender_approval_decay_scale` — a devoted people keeps its domain sharp, a resentful one lets it rot ~15% faster) — closing the loop into a **spiral**: neglect a module → sour its tenders → they let it rot faster → sours them more; **condition buys output, not just the absence of failure** — the two modules that only ever *cost* the ship now earn their keep by how well they're kept: the medical bay's **condition** softens famine deaths **and, its most fundamental job, thins the real-time-loop monthly age-based death roll** (`medical_mortality_relief_per_condition` — the first subsystem coupling to the mortality system: a bay in good repair lowers every character's monthly death chance, a failing one leaves the aging to their age, clamped below 1 and never cheating the hard age cap — so upkeep on the infirmary literally buys the crew years), the security corps' condition recovers unity **and stability** (`security_stability_recovery_per_condition` — the corps' *governance* domain: keeping the ship's institutions functioning, the first maintenance-driven counterweight the it102 stability stat has, steadying a fracturing government toward a 0.7 ceiling), the **agriculture bay's condition scales its food yield** (`agriculture_condition_food_penalty` — a rotting farm feeds fewer, penalty-below-full so a pristine farm keeps the baseline; the food module's long-missing condition→output coupling, and it feeds the it79 spiral: neglect the farm → less food *and* sour the Verdant Kin → they let it rot → less food still), and the **habitat's condition moves morale** yearly (`swing·(condition−0.5)` — a sound home lifts spirits, a failing one drags them, the only maintenance-driven counterweight morale has to the voyage strain) **and now the habitat also scales the dynasty's *renewal*** (`habitat_renewal_penalty` — the home is where families are raised, so its condition multiplies the real-time-loop yearly birth chance, `1 − penalty·(1 − condition)`, penalty-below-full: a sound home brings the young up on schedule, a failing one raises fewer, closing a neglect spiral with the morale swing — let the home rot and the ship loses both its spirits and its children, and the dwindling line inches toward extinction), so upkeep pays back continuously; and the **life-support plant literally sustains life** — below `life_support_failure_threshold` (0.2) a badly-collapsed plant *thins the crew* each year, scaled to how far it has failed (`life_support_failure_mortality` 0.006 peak at zero condition), the module's most fundamental effect and a systemic driver of the it81 depopulation — **and it needs power as well as repair** (`life_support_energy_critical` 800: below it the grid's power availability caps the plant's effective condition for the mortality check, so a *sound* plant on a near-empty grid kills as surely as a broken one, making **energy a life-or-death resource** at last, not just a brown-out) **— and the green decks are the ship's *lungs*** (`agriculture_life_support_contribution` — a living biosphere scrubs air the mechanical plant would otherwise carry alone, so the agriculture bay's condition, times this, is *added* to the plant's effective condition before the mortality check: a generation ship's closed garden is real redundancy, and keeping the farm green means a failing plant kills far fewer — a fresh module→module systemic coupling, capped below the failure threshold so even a pristine garden only *softens* a dead plant, never wholly replaces it; it also gives the it80 food-yield spiral a second stake, since letting the Verdant Kin's garden rot now costs *breathable air* as well as food); **systemic tick (identity)**: the education/culture archive is the ship's memory of its founders — its **knowledge** scales down the yearly *cultural* drift + loyalty-fade (`archive_drift_resistance`), so a vivid archive slows the people forgetting who they were (but the body still adapts to the ship regardless) — the first subsystem coupling to identity, not production; **systemic tick (mission)**: a charter names the module its work leans on (`objective_subsystem` — mining→engineering, greening→agriculture, science→archive) and that module's **condition scales how fast the objective accrues** on-station (`objective_condition_penalty` — a pristine bay works at the base rate, a rotting one slower, penalty-below-full so a kept ship is unchanged) — the subsystem axis's first coupling to the mission the voyage exists for; **and education is the knowledge *keystone*** (the counterpart to engineering's decay keystone): its **condition** scales the generational **knowledge transmission** for *every* module (`education_transmission_condition_penalty` — a crumbling archive passes less of the founding craft forward each generation, penalty-below-full so a pristine archive keeps the baseline), so every module now has a condition→output effect |
| Campaign | Seeded skeleton beats + reactive fills; phase-gated event weighting |

Update this table occasionally (not every iteration) so drift from baseline stays visible.

## Depth axes — what "deeper" means per axis

Each iteration picks **one axis** and makes it deeper. Rotate; don't camp
(see rotation discipline below).

### 1. Event families (the workhorse axis)
- Grow families toward and past parity (comedy at 6 vs legacy_drift at 14 is fine short-term;
  long-term every family wants 12+ with real internal variety).
- **Complications and twists over new one-offs**: an event that can arrive with 2–3
  complications is worth three flat events. Prefer adding `complication` branches /
  follow-up chains to existing templates where the schema allows; extend the schema when
  it doesn't.
- **Chains**: multi-event arcs where an early choice re-fires a consequence decades later
  (the schema for delayed follow-ups is itself a depth deliverable if missing).
- Replace remaining placeholder flavor (anything still smelling of `event_notes.md`
  brainstorm, e.g. Tribbles-derivatives) with in-universe voice.

### 2. Charters / missions
- New charters should introduce a **new shape**, not a reskin: different phase structures
  (long-station vs double-hop vs deep survey), different quantified objectives, different
  risk profiles, destination flavor that colors which event families weight up.
- Charter-specific event pools / beat overrides (data-driven: charter tags that gate events).

### 3. Factions
- Deepen from structure-v1 toward the layered future the schema reserved: faction-colored
  event reactions, inter-faction friction events, faction-specific dilemma outcomes,
  schism/assimilation beats with more texture, recruitable pool personalities.
- Approval meters / stat modifiers only when an iteration deliberately takes that step —
  it's a mechanics iteration, not a content sprinkle.

### 4. Subsystems & knowledge
- More failure/repair texture per subsystem: distinct breakdown events per module, knowledge
  crisis beats ("the last person who understood the reactor is dying — arrange a teaching
  succession?"), tier-specific flavor on upgrades.
- Cross-couplings (agriculture failure → survival event pressure → medical load) expressed
  in data where possible.

### 5. Campaign skeleton & pacing
- More beat archetypes for the seeded skeleton; era texture (early-voyage vs mid vs
  homecoming beats); drift-threshold beats that fire on `cultural_drift`/`adaptation`
  crossings; better dead-air detection (long stretches with nothing eventful = a bug in
  content coverage, not a mercy).

### 6. Provisioning & opportunity
- More shortage-driven opportunity events (garden-world stop archetype), fuel-crisis
  branches, cargo/salvage texture on Return legs.

### 7. Voice & presentation (cheap, high-value)
- Log-line variety, dated flavor lines, generational obituary/succession flavor, help/hint
  text, richer outcome prose. Pure JSON edits; a good "small iteration" axis.

## Consequence coupling — the depth multiplier

Whenever adding content, prefer variants that **touch two systems**: an event that damages a
subsystem AND shifts a faction, a charter whose objective interacts with knowledge decay, a
dilemma whose cost lands a generation later. Isolated content adds breadth; coupled content
adds depth. One deliberate coupling per iteration is a good bar.

## Iteration discipline (for /loop or manual passes)

1. **Pick the axis** — rotate: the axis least-recently touched wins ties; never the same
   axis twice in a row unless finishing a schema change started last iteration.
2. **Scope one iteration** at "one sitting": e.g. 4–8 events with complications, or 1–2 new
   charters, or one schema extension + exemplars, or one coupling mechanic wired through
   `event_resolver.rs`/tick.
3. **Author in JSON first**; touch Rust only for schema/mechanics the content needs.
4. **Verify**: `cargo test -p stellar_legacy` (incl. the long soak + automated playthrough
   harness), `cargo clippy --all-targets --all-features -- -D warnings`, `cargo fmt -- --check`.
   If the iteration changed pacing-relevant content, eyeball a harness transcript for
   dead air / event spam.
5. **Log it** in the rotation log below (one line), then commit — **`mytherra` house style** (matching the `RustGames/mytherra` history):
   - **Subject**: an evocative *in-world* sentence naming the change's effect in the fiction (not a functional summary), plus a `(GDD <a> <-> <b>)` cross-reference to the `gdd.md` §sections it couples — e.g. `An embassy is graded on the peace it keeps … (GDD 5.2 <-> 5.4)`. §5.1 economy · §5.2 contracts · §5.3 dynasty · §5.4 events · §5.5 factions · §5.6 determinism · §6 data model · §7 world/progression.
   - **Body**: the *before* state (what was flat/blind/missing) → what changed → the deeper loop it closes / why it matters at generational scale → a "data-driven and deterministic" implementation note naming the schema/field + the test added → a final `N tests pass (one new), determinism preserved` line → the `Co-Authored-By` trailer.
   - Keep it about the *systems coupling*, in the game's voice; the rotation-log line above stays the terse functional record.

## Quality bars (reject content that fails these)

- **Choice matters**: every event offers options with genuinely different consequences —
  no "OK" buttons on anything above flavor weight.
- **Phase-appropriate**: gated to the phases where it makes fictional sense.
- **Century-aware**: content that only makes sense at generational scale (drift gates,
  min_year/min_generation) is preferred over timeless filler.
- **No repetition tells**: if a template will plausibly fire 3+ times in one voyage, it needs
  complication variety or tighter gating.
- **In-universe voice**: no placeholder names, no fourth-wall, consistent tone with
  `event_design_notes.md`.

## Non-goals

- No new game modes, no cryosleep, no one-way colonization — the round-trip refit loop is
  the frame.
- No content in Rust; no balance constants in Rust.
- No breaking the automated playthrough harness — it is the primary playtest channel and
  every iteration leaves it green.
- **Do not reverse the real-time-loop mechanics** (see "Established mechanics"): no manual
  time-advance path, no "same seed ⇒ same *played* run" guarantee, no static-roster assumption,
  no un-splitting the state-dependent tabs. Build content on top of them.

## Rotation log

*(append one line per iteration: date · axis · what landed)*

- 2026-07-21 · (bootstrap) · baseline captured, this document created.
- 2026-07-21 · event families + coupling · added `requires_consequence` gate (schema + `passes_gate` wiring) so an early choice can unlock a later event; authored the `sealed_ward → the_ward_reopens` chain plus 5 parity events (biology_medical 6→8, survival/mystery/diplomacy/comedy/science_anomaly 6→7). Events 72→79. Couplings used: consequence chain, `faction_loss` on harsh rationing, fuel/drift trade, gated salvage grant. +1 test (92 total).
- 2026-07-21 · charters + coupling · new charter↔event coupling: added `tags` to charters (all 10 tagged) copied onto the active contract, and a `requires_charter_tag` event gate so a destination colors its own event pool. Authored 4 tag-keyed events (boarding scare on `hostile_space`, settler-drain on `garden`, starless-reach drift on `deep_space`, richer-find early return on `uncharted`). Events 79→83. Data-load check rejects tags no charter carries. +1 test (93 total).
- 2026-07-21 · factions + coupling · faction↔event coupling: `dominant_faction_id`/`is_faction_aboard` helpers + two event gates — `requires_dominant_faction` (signature events when a faction runs the ship) and `requires_factions_aboard` (inter-faction friction). Authored 4 faction-colored events (Ascension cradle-augmentation, First Flame reactor-rite, Verdant Kin garden-vs-salvage, Flame×Ascension partition). Events 83→87. Data-load check rejects unknown faction ids. +1 test (94 total).
- 2026-07-21 · subsystems + coupling · subsystem↔event coupling: outcome `subsystem_deltas` (signed condition/knowledge changes, clamped) wired into `apply_outcome`, and a `knowledge_below` crisis gate so events fire as a module's know-how decays. Authored 4 events (the-last-engineer teaching succession, coolant-loop rupture damaging the eng bay, grow-deck blight denting agriculture + food, forgotten-medicine crisis). Events 87→91. Data-load check rejects unknown subsystem ids. +1 test (95 total). Event gates now: consequence, charter-tag, dominant-faction, faction-aboard, knowledge-below.
- 2026-07-21 · campaign skeleton · made the seeded-beat pools data-driven — lifted the hardcoded Rust phase→family tables into a `campaign_skeleton` config block (honoring the data-driven hard rule) — and added **era layering**: beats in the first 20% of the voyage also draw a founding-era pool (exploration/anomaly/comedy), beats in the last 20% a homecoming-era pool (legacy_drift/ethics). `generate_beats` now takes the config; all 3 call sites updated. Data-load check rejects pool families with no events. +2 tests (96 total). No new events this pass (structural).
- 2026-07-21 · provisioning + coupling · provisioning↔event coupling: `food_below`/`fuel_below`/`spare_parts_below` shortage gates on events, wired into `passes_gate`. Retro-fitted the existing opportunity events so they now fire *because* you're short (garden_world food≤4000, fuel_skim fuel≤0.45, resupply_cache food≤5000) rather than at random. Authored 3 new shortage beats (the-dry-tank fuel crisis strips the ship for mass, the-empty-lockers parts foundry, the-laden-return cargo/escort risk on the Return leg). Events 91→94. +1 test (97 total).
- 2026-07-21 · voice · killed the game's worst repetition tell: the obituary / succession / coming-of-age lines fire every generation (12+×/voyage) and were 3 hardcoded Rust strings. Lifted them into a data-driven `flavor` config block with 6/6/5 authored variants, indexed by generation (deterministic, no RNG perturbation) so a seed still replays exactly. Placeholders `{name}`/`{generation}`/`{births}` substituted. Data-load check + helper unit test (98 total). This completes one full rotation through all 7 axes.
- 2026-07-21 · event families (round 2) · brought **ethics** to parity (6→11, the thinnest family) leaning on round-one mechanics: the-stowaway census dilemma, the-mercy-dose (gated on low medical knowledge, dents the bay), the-archive-lie, a century-spanning consequence chain (the-mutineers-sentence exiles a faction → the-exiles-return generations later via `requires_consequence`), plus science_anomaly parity (the-second-star). Events 94→**100**. Added a data-load provenance check: every `requires_consequence` tag must be produced by some outcome (typo guard). No new test fn (content pass); 98 total. Refreshed baseline table.
- 2026-07-21 · charters (round 2) · added 2 charters with a genuinely new **long-station** shape — most of the voyage parked on-site, not in transit (the-deep-camp, mine a cinder vein for 300 of 480 yr; hearthfall-accord, an 8-generation embassy residency). Both carry a new `long_station` tag; authored 2 tag-keyed events (the-stationkeepers: a generation that knows only the worksite; the-idle-hull: a parked ship seizes up as no moving one does). Charters 10→12 (grid already scales), events 100→102. +1 soak test flying the 480-yr Deep Camp end to end (99 total).
- 2026-07-21 · factions (round 2) · gave signature events to the 3 factions that lacked them — Steel Covenant (workshops over classrooms: +engineering, −education), Meridian Accord (slow arbitration: +stability), Hearth Union (the long table: +morale/unity). Added 2 more inter-faction friction pairs (forge×hearth, accord×ascension). Deliberate new coupling: **faction→subsystem** via `subsystem_deltas` on faction outcomes. Events 102→107; all 6 factions now have signature beats + 3 friction pairs. Added a data-load coverage assertion (every faction has a `requires_dominant_faction` event); 99 total.
- 2026-07-21 · subsystems (round 2) · round one gave knowledge-crisis beats only to engineering + medical; this pass covers the other 4 — life_support (the-breath-keepers, scrubber chemistry lost), security (the-unlearned-watch, peace hollowed the corps), education (the-teachers-gap, schools teaching shape not substance), agriculture (the-lost-gardeners, craft not passed on). Plus the doc's example cross-coupling: the-hungry-wards (agriculture shortfall → malnutrition → medical-bay load, moving both subsystems). Events 107→**112**. Added a data-load coverage assertion: every subsystem has a `knowledge_below` crisis event; 99 total. Refreshed baseline.
- 2026-07-21 · campaign skeleton (round 2) · **drift-threshold beats**: new `drift_beats`/`drift_beat_family` config + a `drift_beats_fired` contract counter + a `fire_drift_beat` hook in the advance loop. The first month cultural_drift reaches each of 0.3/0.5/0.7/0.85, a legacy_drift beat is forced — so the signature Long-Term Expedition beats fire as *consequences of how far the voyage has changed the people*, not random rolls. Fires once per threshold, deterministic. Data-load check (ascending, in-range, family has events) + a firing unit test; the soaks now exercise it. No new events (structural). 100 tests.
- 2026-07-21 · provisioning (round 2) · completed the shortage-gate set with `energy_below` (food/fuel/parts/**energy**), wired into `passes_gate`. Authored 5 beats: the-dimming (energy crisis → brown out habitats or industry, couples energy→subsystem), the-ice-moon (stop to mine volatiles when fuel low), a food-shortage consequence chain (the-seed-corn eats the replant reserve → the-barren-decks a generation later via `requires_consequence`, both denting agriculture), and the-homeward-wreck (Return-leg salvage when parts-scarce, grants a component). Events 112→**117**. +1 test (101 total). Refreshed baseline.
- 2026-07-22 · voice (round 2) · **ambient flavor** for dead-air: a 10-line `ambient` pool + `ambient_gap_years` in the flavor config, emitted from the year-boundary tick once per gap-years of event-less quiet (indexed by year, deterministic, dated by the log, never resets the event ramp). Long centuries between decisions now read as lived-in — murals, drifted songs, a keeper unsure which parts of the founding log are still true. Data-load check (ambient non-empty when enabled) + a quiet-stretch unit test (102 total). Completes the **second full rotation** through all 7 axes.
- 2026-07-22 · event families (round 3) · pushed the thinnest family, exploration_first_contact, from 7 to the doc's 12+ bar with genuine **first-contact** scenarios the family had lacked (it was mostly navigation flavor): the-wayfarers (a fellow generation ship), the-wary-frontier (a burned-before civilization), the-young-world (prime-directive dilemma), the-silent-monuments (ruins of a vanished people), the-last-broadcast (an eon-old beacon awaiting a reply). Plus science_anomaly and comedy parity lifts (the-wandering-comet, the-great-bake-off). Events 117→**124**; every family now ≥9. Data-load category check caught a family-vs-category typo mid-pass. 102 tests.
- 2026-07-22 · charters (round 3) · added a third charter **shape**: the double-hop (twin_survey), a `[travel, operation, travel, operation, return]` topology — two anomalies surveyed in one voyage before turning home, objective accruing cumulatively across both Operation legs. Verified the phase engine already handles arbitrary topologies (phase_at walks segments; operation_months sums them). New `double_hop` tag + 2 keyed events (the-second-departure: turn deeper out or take partial pay and go home via `force_return`; the-far-deep: a stranger second anomaly). Charters 12→13, events 124→**126**. +1 soak test flying the 440-yr Twin Survey across both legs (103 total).
- 2026-07-22 · factions (round 3) · **faction-specific schism beats** with a new mechanic: outcome `faction_loss_id` + `apply_faction_loss_by_id` shed the *named* faction (not just the smallest), so a schism removes the group it's actually about. Authored 3 drift-gated schisms — the-ascension-exodus (the augmented depart to become what they're becoming), the-flame-orthodoxy (the faith hardens: bend the whole ship to its rites or let it secede), the-verdant-secession (the Kin plant a world and stay). Each gated on min_cultural_drift + faction aboard, coupling factions + drift + faction_loss. Events 126→**129**. Data-load check extended to `faction_loss_id`; +1 targeted-loss unit test (104 total).
- 2026-07-22 · subsystems (round 3) · **condition-breakdown gate**: generalized `KnowledgeGate`→`SubsystemGate` and added `condition_below` (the physical-failure parallel to knowledge_below), wired into `passes_gate`. Every subsystem had a knowledge crisis; now the modules can also fire *breakdown* beats as their condition rots — the-failing-air (life-support failing in earnest: all hands or seal the dying decks and lose people), the-open-locks (security decayed to uselessness), the-seized-works (engineering bay so worn it chokes the ship's whole capacity to mend). Events 129→**132**. Data-load subsystem-ref check extended to `condition_below`; +1 gate unit test (105 total).
- 2026-07-22 · campaign skeleton (round 3) · **adaptation-threshold beats** — the physiological parallel to round 2's drift beats, completing the doc's "cultural_drift/adaptation crossings." New `adaptation_beats`/`adaptation_beat_family` config + `adaptation_beats_fired` counter + `fire_adaptation_beat` (shared `force_family_beat` helper extracted). As the people's `adaptation` crosses 0.35/0.6/0.8, a biology_medical beat fires — the descendants growing shipborn. Authored 3 adaptation-themed events (the-shipborn-body, the-recycled-palate, the-quiet-lungs: fitness for the ship becomes fragility to its failures). Events 132→**135**. Data-load beat check generalized to both stat kinds; +1 firing test; 2 timeline tests clear both beat lists (106 total).
- 2026-07-22 · provisioning (round 3) · pure content pass on the mature shortage-gate set: the-long-winter (a **compounding crisis** gated on low food AND low energy at once — hunger and cold together), a rationing-discipline consequence chain (the-lean-pact swears equal shares → the-pact-remembered generations on, when the pact has hardened into identity and merit-reward lands like heresy), and Return-leg texture (the-trade-convoy converts cargo→provisions; the-drifting-tanker, a derelict fuel bounty when the tank is low). Events 135→**140**. +1 test locking the multi-shortage AND semantics (107 total). Refreshed baseline.
- 2026-07-22 · voice (round 3) · **occurrence-aware phase-transition flavor**: the phase lines were hardcoded, so the double-hop charter reprinted the same "Departure burn complete"/"makes station" text on its *second* Travel/Operation — a real repetition tell. Added a data-driven `phase_lines` pool (keyed by phase) + `ActiveContract::phase_occurrence`, indexed so the second departure/arrival reads differently; missing pool falls back to the built-in line so the log never blanks. `phase_transition_line` now takes the flavor config. Data-load key check + a double-hop variety unit test (108 total). No new events (voice pass). Completes the **third full rotation** through all 7 axes.
- 2026-07-22 · charters (round 4) · a fourth charter **shape**: the return-dominant tow (the_long_tow), `[travel 70, operation 60, return 320]` of 450 yr — the only charter whose hard stretch is the *haul home*, not the outbound or the on-station work. Grapple a dead titan-ship at a near waypoint (objective accrues in the short Operation), then nurse an overloaded ship and its parasitic tow across three centuries of deep dark; success weighted onto PopulationSurvival + ResourceEfficiency (enduring the return) over MissionCompletion. Verified the engine needs no change — objective banks in Operation, the return grades on survival/efficiency/cohesion across all months. New `long_return` tag + 3 Return-phase-keyed events giving the haul its signature texture: the-towline (re-rig the failing anchor for parts vs shed part of the prize to lighten ship — engineering condition/spare parts vs fuel/hull), the-overloaded-decks (bear the tow's draw and strain life-support vs let it go cold and lose freezable salvage — life_support coupling), the-endless-haul (a generation born to nothing but the tow: make the labor sacred → unity+drift vs point them at a home they'll never see → morale/stability but restless). Charters 13→**14**, events 146→**149**. +1 soak test flying the return-dominant voyage + asserting its topology (110 total).
- 2026-07-23 · factions (round 18) · the faction system couples to unity (it100 approval→unity), reputation (it107), subsystem decay (it79), and demographics (it72/86) — but never to **stability/governance**, and every coupling to date read either *approval* or the *dominant* faction, never the **composition of the whole roster**. Built a coupling on a genuinely new input: **a divided ship is harder to govern**. New `aboard_ideology_spread` (the member-weighted mean-absolute-deviation of the aboard peoples' `ideology` — how ideologically *split* the polity is, 0 for a single-minded ship) + config `ideology_spread_threshold` (0.6) / `ideology_spread_stability_penalty` (0.01): each year a spread past the threshold erodes `stability` by `penalty·excess`, so a coalition spanning the tech-embracing↔tradition-bound spectrum strains the institutions while an aligned ship governs freely. It's distinct from the it100 approval→unity coupling — that reads how *content* the peoples are, this how *aligned* — so a ship can be content yet fractious (the two are orthogonal), and it's the faction system's first touch on the *governance* stat rather than cohesion. The balance is a real dynamic: a diverse ship *demands* good governance (the it108 security→stability recovery offsets the drain), and it's **self-limiting** via it72 — as the augmented Ascension (the +0.9 ideological extreme) demographically dwindles over the centuries, the spread narrows and the strain eases. Gentle (the sorted founding trio's MAD ≈0.667 erodes only ~0.0007/yr, well within the security recovery's reach); deterministic (reads constant ideology + the living roster, no RNG); erodes-only past the threshold so an aligned/typical ship is near-inert, all 9 soaks green (autoplay's security recovery offsets it; silent soaks don't assert stability). `#[serde(default)]` 0 threshold = off. Events unchanged (242). +1 test proving a coalition split across the spectrum governs worse than an aligned one, which the coupling leaves untouched (217 total).
- 2026-07-23 · charters (round 18) · every charter effect built so far fires on *success* — completion pay (it-W2), completion consequence/arc (it92), completion reward + reputation (it99/it113). **Failure had no consequence beyond reduced pay**: a ship could default on a relief writ, leave a people to die, turn for home with the holds half-empty, and its *name* was untouched. Added **charter abandonment consequences** — the negative mirror of the completion reward, and the first charter effect keyed to *failure*. New `ContractTemplate.abandonment` (an `Abandonment` of reputation_deltas + a log) + `apply_abandonment` (a pure fn, mirroring `apply_completion_reward`), applied in the conclude path when the charter concludes at `SuccessLevel::Failure` — and the positive completion mark/reward is now correctly gated to a *non-Failure* conclusion (a charter not seen through earns neither; a latent it15/it99 gap where the legacy applied on any conclusion, now closed). It composes with the it119 **`resolve`** trait — abandoning a writ is the ultimate *folding* — and reads the *nature* of the mission abandoned: **the sanctuary run** given up hardens the mercy the crew couldn't keep (−mercy) and earns a name for folding (−resolve); **the hard contract** given up is a *merciful fold* (+mercy — a ship that would not, at the last, strip a home that people still called home — but still −resolve, a hull the dark marks as one that quits). So the mission cycle is now symmetric: seeing a charter through builds character and capability, defaulting on one costs the ship's name — and quitting a cruel job can even be a small redemption. Deterministic (nudges reputation on failure, no RNG); zero soak risk (autoplay completes its charters, never concluding at Failure; `apply_abandonment` tested directly). Data-load: abandonment reputation ids extend the it105 provenance set. Charters/events unchanged (21/242 — fields on 2 charters). +1 test proving abandoning the sanctuary run hardens and folds the ship, abandoning the hard contract is a merciful fold, and an ordinary charter's failure marks nothing (216 total).
- 2026-07-23 · event families (round 18) · *opens the eighteenth rotation.* The graded-character system (it105+) was rich but had only ever exercised **one trait: mercy** — so the ship's "character" read as a single moral dial, not a person with facets. Yet the infra was multi-trait all along (`SimState.reputation` is a `HashMap<String, f32>`; `reputation_deltas`/`min_reputation`/`max_reputation` are generic over id; the data-load provenance check collects *all* produced trait ids). So this was a **purely data-driven** move: introduce a **second reputation trait, `resolve`** — the ship's name for *steadfastness* (seeing things through, holding its nerve), orthogonal to mercy (a ship can be kind-and-yielding or cruel-and-unbending). Three new diplomacy events, mirroring how mercy was introduced (a builder + two readers): **the line in the dark** *builds* it (a stronger hull demands a charted lane the ship has every right to — hold the claim and dare them to test it, +resolve at a real cost, or stand aside and keep the peace, −resolve and a name for folding); **the unblinking ship** (`min_reputation resolve 0.62`) reads it as *leverage* (a rival backs down from a hull famous for never folding — press the advantage the name gives, or *offer terms anyway*, generosity from strength that **builds mercy**, the two traits visibly interacting); **the ship that folds** (`max_reputation resolve 0.38`, `min_generation 4`) reads it as *liability* (a hard party names a worse price expecting this hull to blink — pay the tax on a soft name, or make a costly stand *this once* that begins reversing it). No Rust change — the reputation, beat, voice, and faction-lean systems all key on the *configured* mercy trait, so `resolve` is simply built and read by events, proving the character is genuinely multi-faceted. Deterministic (reads reputation, no RNG); resolve launches neutral (0.5) and its events gate past what a fresh/autoplay ship reaches, all 9 soaks green. Data-load provenance already generic (resolve is produced by the-line-in-the-dark, gated by the two readers). Diplomacy 26→**29**, events 239→**242**. +1 test proving resolve builds from holding a line and drops from yielding, opens a door mercy can't, and is *orthogonal to mercy* (building one leaves the other untouched) (215 total).
- 2026-07-23 · voice (round 17) · *completes the seventeenth rotation.* The ship has band-crossing voices for its *spirits* (the it103 morale voice), its *political climate* (the it104 polity voice), and its *name* (the it111 reputation voice) — but its **institutions** stayed mute. it102 gave stability a collapse *beat* (the defining governance-failure reckoning) and it108 a maintenance recovery, yet nothing voiced the quieter thing: a government slowly slipping, or quietly firming back up, between the reckonings. Added the **governance voice** — the fourth ship-level stat-voice, mirroring `announce_ship_mood` exactly. New `announce_stability_mood` + `SimState.stability_voice_band` + `stability_fraying`/`stability_firming` pools (4 each) + thresholds (`stability_voice_high` 0.7 / `stability_voice_low` **0.4**, deliberately gentler than the it102 collapse beat's 0.3 so the voice — a fraying *noticed* — precedes the reckoning). When stability crosses *into* a fraying band (quorums missed, offices unfilled, decisions drifting, the charter worked around) or a firm one (the councils working, offices answering, the charter honored in practice), one pooled line surfaces. It's the *governance* twin of the spirits and political-climate voices, and distinct from both: a ship can be high-spirited (morale) and fairly-treated (polity) yet institutionally *slipping* — the machinery of government is its own dimension. The launch band is recorded not announced (a founding ship's institutions are sound → the firm start reads baseline, mirroring the it103 morale-band record), and it composes with it108: a well-kept security corps that firms the institutions back up now has the decks *hear* the recovery. Pure voice — being an announcement (no pending event), it needs *no soak guard* (like the it104/it111 voices, the silent soaks' stability drift narrates itself harmlessly). Deterministic (reads stability, indexed by year, no RNG); a return to the middle re-arms. `#[serde(default)]` empty pool = inert. Events unchanged (239). +1 test proving a sound founding government is silent, a slipping one says so once, staying frayed doesn't reprint, and a firming one announces afresh (214 total). **Completes the seventeenth full rotation through all 7 axes.**
- 2026-07-23 · provisioning (round 17) · the provisioning axis is exhaustively built — shortage gates (food/fuel/parts/energy), abundance gates, persistence counters (lean/fat years), compounding paradoxes, foresight chains, and couplings to factions, objective, population, subsystems, and reputation — but **every one of those is an event gate or a counter**. The axis had never touched the *systemic tick*: scarcity gated content and drove counters, but never mechanically *did* anything year over year. Built the axis's first **systemic coupling: chronic hunger wears the crew's spirits down**. New `chronic_hunger_years` (5) + `chronic_hunger_morale_drain` (0.004): once the it89 `lean_food_years` counter reaches the threshold, the year tick drains a little morale every year the lean holds — so a grinding multi-year hunger doesn't merely *gate* content (it89) and *read* hungry in the drift-aware ambient (voice r13), it **mechanically grinds the ship down**, the persistent counterpart to the acute famine events. Threshold-gated so one bad winter is inert (that stays the acute events' domain) — only a *sustained* lean bites. It composes forward into the voice: as the drain pulls morale into a grim band, the it103 ship-mood voice announces the decks going heavy, so a long famine now *sounds* as bleak as it is. The natural systemic parallel to it100 (faction approval → unity) and it73 (habitat → morale), from the provisioning side at last — and it deepens morale's environmental pressure map (voyage strain down, habitat up, and now the larder down when it's been thin too long). Deterministic (reads the lean counter, no RNG); the silent and autoplay soaks stock food huge so `lean_food_years` stays 0 → no drain, determinism + all 9 green. `#[serde(default)]` 0 = off. Events unchanged (239). +1 test proving a ship long lean loses morale where a comfortably fed one does not, the gap exactly the year's hunger toll (213 total).
- 2026-07-23 · campaign skeleton (round 17) · the beat lattice keys on time (anniversary, dead-air), every identity/population stat (drift/adaptation/unity/morale/loyalty/stability/population), the mission (objective), a voyage phase (homecoming), a political change (power-transition), and the ship's character (reputation) — but **one whole dimension had no beat: the ship's physical subsystems**. Every module has reactive `condition_below` *events*, yet they need the RNG to roll; nothing *guaranteed* a reckoning the moment a keystone truly failed, the way the stat beats guarantee one when unity or stability collapses. Added the **subsystem-collapse beat** — the skeleton's first trigger keyed to a *subsystem's condition*. New `subsystem_beats` (a list of {subsystem, threshold, family} triples) + `fire_subsystem_beat` (in the chain after the stability beat): the first tick a listed module's condition falls to/below its red line, a beat is forced from its family. Campaign-scoped via `SimState.subsystem_beats_fired` (tracked by id, once per module a voyage, so a repaired-then-re-collapsed keystone doesn't re-mark). The first exemplar is the **engineering keystone** at condition ≤0.25: because engineering is the it62 decay keystone (its condition scales *every* module's rot), a bay this far gone means the whole ship now decays faster than it can be repaired — a death-spiral, not a bad year, and exactly the defining crisis that deserves a guaranteed beat rather than a lucky roll. Two new engineering events gated `condition_below 0.25`: **the mending lost** (rebuild the core by stripping whole decks, or accept a ship that will only ever wear down) and **the cannibalized ship** (`min_generation 4` — eat a redundant system to feed the bay, or hold every system whole as all of them fail together). Gated on a contract (a beat of the voyage), like the other beats, so the no-contract soaks are inert; the four long *silent* soaks that fly a full unrepaired voyage (which rots engineering past 0.25) disarm it alongside the other forced beats; autoplay resolves any collapse beat in stride — all 9 green, determinism holds (reads condition, no RNG). Data-load: each beat names a real module, a red line in (0,1], and a family with events. `#[serde(default)]` empty = off. Engineering 26→**28**, events 237→**239**. +1 test proving the beat fires once the keystone rots past its red line and stays silent on a sound bay (212 total).
- 2026-07-23 · subsystems (round 17) · it91 made a failing life-support plant literally *thin the crew* (condition → mortality), and it97 made it need power too — but the effective life-support condition read *only* the mechanical plant (worse of repair and grid power). Yet on a real generation ship the mechanical scrubbers are only half the air system: the **green decks are the ship's lungs**, a living biosphere that scrubs CO₂ and makes O₂ as surely as any machine. That closed loop was never modeled — a flourishing garden fed the crew (it80) but did nothing for the air they breathed. Built the coupling **agriculture biosphere → life-support**: new `subsystems.agriculture_life_support_contribution` (0.15), added to the plant's effective condition before the it91 mortality check as `contribution · agriculture_condition`, so a well-kept farm buys real slack against a failing plant (a fully collapsed plant kills ~75% fewer with a pristine garden than a dead one) while — **capped below the 0.2 failure threshold** — even a perfect garden only *softens* a dead plant, never wholly replaces it (the plant still holds pressure, heat, water, waste). A fresh module→module *systemic* coupling at the mortality layer (distinct from the it62 engineering keystone, which scales all *decay*): the ship's two air systems, mechanical and biological, at last back each other up. It also gives the it80 food-yield spiral a second, graver stake — letting the Verdant Kin's garden rot now costs *breathable air* as well as bread, so the tender-neglect spiral (it79) can end in suffocation, not just hunger. Deterministic (reads condition, no RNG); `#[serde(default)]` 0 = off. The two it91/it97 mortality tests left agriculture at its founding 1.0, which the new bio term would have masked — isolated each by zeroing the garden (they now measure the mechanical plant/grid alone, unchanged results); autoplay keeps agriculture repaired so the coupling only ever *helps* there, and the neglected-voyage soaks (which rot agriculture alongside life-support → near-zero bio) still limp to their asserted ends with no extinction — all 9 green. Events unchanged (237). +1 test proving a thriving garden helps a collapsed plant sustain more of the crew, yet a garden alone cannot wholly replace it (211 total).
- 2026-07-23 · factions (round 17) · it92 gave the approval meter its first *relationship* — **rivals**: favoring a people sours those it quarrels with, so the meter became a balancing act you can't max for everyone. But that built only the political landscape's **fault lines**; it had no **coalitions** — no way for two peoples' fortunes to rise together. Built the positive twin: **standing alliances**. New `FactionDef.allies` (authored symmetric, never overlapping a rivalry) + config `ally_approval_spillover` (0.2) + `apply_ally_approval_spillover` (the exact mirror of it92's rival spillover, called right after it in `apply_outcome`): when an event *lifts* a people's approval, each of its aboard allies shares a fraction of the goodwill — so courting one people is a **gift to its kin** as much as a slight to its rivals, and the approval meter is now a full political landscape with both fault lines and blocs. The natural kinships are the **r5 merger pairs** — the peoples the ship has folded into one before: the communal **Hearth Union ↔ the garden-tending Verdant Kin** (the-green-hearth, the table and the garden that fills it) and the **Meridian Accord's courts ↔ the Steel Covenant's forges** (the-technocrats-compact). It reshapes the whole meter: with rivals *and* allies, a player builds a coalition (lift a bloc and its allies rise with it) against the peoples it quarrels with, rather than tending six isolated dials — and it composes with everything downstream (an allied bloc's shared approval keeps their tended modules sharp via it79, waxes their demographics via it86, steadies unity via it100). Deterministic (reads the favored deltas, no RNG — only a positive gain spills, a slight never wounds allies); launch approval 0.5 and the silent soaks (no events → approval unmoved) untouched, all 9 green. Data-load: allies name real, other peoples; alliances symmetric; and a pair is never both kin and rival (the two spillovers would fight over one relation). `#[serde(default)]` empty = a people that stands alone. Events unchanged (237). +1 test proving favoring the Hearth warms its ally the Kin but not the unrelated Covenant, and that slighting the Hearth is no wound to the Kin (210 total).
- 2026-07-23 · charters (round 17) · it106 made a ship's *reputation unlock missions*; it107 made *who rules* drift it. But the missions themselves — a voyage carrying refugees to safety, or one stripping a defaulted colony — surely shape the ship's character too, yet completing a charter left the reputation untouched. Added **charter completion → reputation**: extended the it99 `CompletionReward` with `reputation_deltas` (applied in `apply_completion_reward`, folded into `is_none`), so seeing a charter through nudges the ship's character. Marked the two reputation-gated charters (it106) to build the very trait they require: **the sanctuary run** (unlocked by mercy ≥0.6) grants **+0.06 mercy** on completion — four centuries of carrying the frail and frightened deepens the ship's kindness; **the hard contract** (unlocked by mercy ≤0.35) grants **−0.06 mercy** — four centuries of the cold thing done without flinching hardens it. This closes a **self-reinforcing loop**: a merciful name unlocks the sanctuary run (it106), completing it *builds* mercy (it113), which draws the ship toward more sanctuary runs; a feared name unlocks the hard contract, completing it hardens further, drawing more hard work. The events (it105), the rulers (it107), and now the missions (it113) all pull the ship's character one way, compounding — a ship becomes, through the very work its character opens to it, more the thing it already was. Deterministic (nudges reputation on completion, no RNG); zero soak risk (autoplay completes via its own path, not the conclude hook; `apply_completion_reward` is a pure function tested directly). Data-load: completion reputation ids name a real trait (extending the it105 provenance to charter completions). Charters/events unchanged (21/237 — fields on 2 charters). +1 test proving the sanctuary run deepens mercy on completion and the hard contract hardens it (209 total).
- 2026-07-23 · event families (round 17) · *opens the seventeenth rotation.* it105 made reputation gate *whole events*; it106 made it gate *charters*. But within a single event, the ship's character didn't shape its **choices** — a famously merciful ship and a feared one, facing the same tense standoff, were offered the identical options, when in truth each could *leverage its name* for a resolution a no-name ship can't reach. Added **reputation-gated outcomes**: extended it77's `OutcomeRequirement` with `min_reputation`/`max_reputation` (folded into `outcome_available` + `is_unconditional`), so a good name and a feared one each unlock choices the other lacks — reputation as **leverage**, not a moral restriction. Exemplar **the wary encounter** (diplomacy): a hull blocks a passage the ship needs, and what happens next depends on what the stranger has *already heard* of this ship. Base choices always shown (withdraw and reroute; stand firm and deal hard). But a merciful ship (`min_reputation mercy 0.6`) can **trade on its good name** — the stranger, having heard the ship deals fairly, eases into an easy passage and an exchange of gifts (a good name opens doors a gun only holds shut); and a feared ship (`max_reputation mercy 0.35`) can **let its name intimidate** — the stranger clears the passage and throws in tribute rather than test a reputation it believes, the easiest passage the ship ever bought, paid for by the fear it earned (and deepening it: −mercy, the next such encounter easier still). It's the outcome-level parallel to it106's charter reputation gates — reputation now unlocks not just *missions* but *choices within events*. Gated outcomes appended after the base ones (it77's rule: outcome 0 unconditional), so autoplay/index-0 is untouched. Data-load: outcome reputation gates name a trait some outcome nudges (extending the it105/it16 provenance check). Deterministic (reads reputation, no RNG); a neutral ship sees only the base choices, all 9 soaks green. Diplomacy 25→**26**, events 236→**237**. +1 test proving a no-name ship gets neither leverage, a merciful ship the good-name option only, and a feared ship the intimidation only (208 total).
- 2026-07-23 · voice (round 16) · *completes the sixteenth rotation.* it109 gave the reputation a *beat* (a reckoning at the strong bands 0.78/0.22), but a reputation is felt long before it becomes defining — the ship notices, in its quiet way, that strangers greet it differently, that its name is *starting* to mean something. Added the **reputation voice**: `announce_reputation_name` + `SimState.reputation_voice_band` + `reputation_merciful`/`reputation_feared` pools + gentler thresholds (`reputation_voice_high` 0.66 / `_low` 0.34). When the watched trait (mercy) crosses *into* a merciful or feared band, one pooled line surfaces — corridor talk, half-proud or half-wary, that word has gone out and the dark now hails the ship with a new warmth or a new caution — before that name grows defining enough to force the it109 beat's reckoning. It's the reputation-side companion to the r11 morale voice and it104 polity voice: three ship-level band-crossing voices now (spirits, political climate, name). Reads the freshly-drifted reputation each year (after the it107 dominant lean), so a ship whose *rulers* are steering it toward a name hears the drift narrated even absent a dramatic choice. A return to the middle re-arms. Pure voice — and, being an announcement not a beat, it surfaces **no pending event**, so unlike the it109 beat it needs *no soak guard* (the silent soaks' reputation drift simply narrates itself harmlessly). `#[serde(default)]` empty pool = inert; determinism + all 9 soaks green. Events unchanged (236). +1 test proving a growing merciful name says so once, staying merciful doesn't reprint, and a feared name announces afresh (207 total).
- 2026-07-23 · provisioning (round 16) · it105/109 built the ship a *character* (its mercy reputation) — but it had never mattered where it matters most: **survival**. Reputation isn't just how strangers greet you; in a famine, it decides whether anyone *helps* you — a merciful ship's good name an insurance policy it never knew it was buying, a feared ship's a debt that comes due at the worst hour. Made **reputation a survival factor**: two famine beats whose options turn on the ship's earned name (`food_below` compounded with `min`/`max_reputation`, the reputation-side of the it82 paradox pattern). **The kindness returned** (`food_below 3500` + `min_reputation mercy 0.62` — a stranger's hull matches course and shares its stores unasked, *because word of this ship travels*: accept gladly and carry the bond forward (+mercy, the web of mercy holding the ship up for once), or accept but insist on paying fair, a giver even in the hour it must receive) and **the famine faced alone** (`food_below 3500` + `max_reputation mercy 0.35`, `min_generation 4` — the ship hails for aid and the dark, this time, does not answer, for a hull known to take and not give faces its lean years the way it made others face theirs: endure it alone on grim discipline, or take what you need by force, −mercy, a ship no one will help becoming one no one dares refuse). So the reputation the events and rulers built is now a larder the dark cannot empty — or a door it will not open — closing the it105 loop on the survival side: mercy is a resource, ruthlessness a liability, both paid out at the moment the ship is weakest. Deterministic (reads reputation, no RNG); a neutral ship faces ordinary famines (neither gates), and the strong-reputation states rarely arise in the food-flush soaks, all 9 green. Survival 43→**45**, events 234→**236**. +1 test proving a merciful name is helped and a feared one shunned in a famine, a neutral one neither, and reputation bites only where the larder is already thin (206 total).
- 2026-07-23 · campaign skeleton (round 16) · every population stat now has a beat — so a fresh *non-stat* trigger. it105 gave the ship a graded character (its `mercy` reputation), built by choices (it105) and by who rules (it107), but nothing marked the *moment* that character becomes **defining**: when the ship has grown so famously merciful, or so feared, that the name itself demands a reckoning. Added the **reputation beat** — the skeleton's first trigger on the cumulative character, not a population stat. New `reputation_beat_trait`/`reputation_beat_high` (0.78)/`reputation_beat_low` (0.22)/`reputation_beat_family` (legacy_drift) + `fire_reputation_beat` (after the stability beat) + a `SimState.reputation_beat_band` tracker: when mercy crosses *into* a strong band a beat is forced (the ship confronting its name), a return to the middle silently re-arms, so a ship that builds and loses and rebuilds a reputation reckons with each crossing. Two legacy_drift beats gated on the trait: **the name we carry** (`min_reputation mercy 0.72` — a generation inherits a famous mercy, a promise made by the dead it must now keep: bind the ship to its best name as a creed, or refuse to be imprisoned by a reputation and keep the freedom to be, sometimes, other than kind) and **the name we earned** (`max_reputation mercy 0.28`, `min_generation 4` — a generation inherits the sum of a hundred hard choices it did not make: own the feared name as a tool, or begin the long thankless work of a different one). It closes the it105/it107 loop — the choices and the rulers build the character, and now the character, when it becomes defining, forces its own reckoning. Requires a contract (a beat of the voyage); the silent full-contract soaks (whose dominant-faction lean drifts reputation to a strong band over the centuries) disarm it alongside the other threshold beats. Data-load: family has events, trait is nudged by an outcome, bands order. Legacy_drift 52→**54**, events 232→**234**. +1 test proving the beat fires on crossing into a merciful name, re-arms at the middle, and reckons anew on crossing into a feared one (205 total).
- 2026-07-23 · subsystems (round 16) · it102 added the stability stat and its governance-collapse beat — but **stability had no maintenance-driven recovery**: it only ever fell (via events), with no positive driver, exactly the gap it73 filled for morale and it59 for unity. And the natural counterweight is obvious: the **security/justice corps** upholds institutional order. it59 already gave a sound corps → *unity* recovery (keeping the peace between the people); its *other* domain — keeping the ship's *institutions* functioning — maps straight to stability. Added **security condition → stability recovery**: new `security_stability_recovery_per_condition` (0.006) + `security_stability_recovery_ceiling` (0.7) → `security_stability_recovery` (mirrors it59's unity recovery: condition-scaled, only steadies a ship below the ceiling), wired into the year tick beside it. It gives the it102 stability stat its first maintenance lever — a fracturing government can be *shored up* by keeping the corps sound, not only wrecked by events — completing the pattern where every governance/wellbeing stat (morale/unity/stability) now has both an event-driven fall and a subsystem-driven counterweight. Deliberately ceilinged at the founding baseline (0.7): a good corps steadies a failing government but does not manufacture perfect order from nothing. Deterministic (reads condition, no RNG); stability launches at 0.7 = ceiling so nothing recovers until events lower it, and the it102 beat still fires (one year's ~0.006 recovery doesn't lift a collapsed stability past the threshold) — determinism + all 9 soaks green. `#[serde(default)]` 0 = off. Events unchanged (232). +1 test proving a sound corps recovers stability more than a wrecked one and a well-ordered ship gets no boost (204 total).
- 2026-07-23 · factions (round 16) · it105 gave the ship a graded character (its `mercy` reputation), built by event *choices*. But reputation should also drift from a subtler source: **who runs the ship**. A vessel led for generations by a communal, kind people becomes known for mercy even without any single dramatic choice; one run by a coldly pragmatic people hardens over the centuries. Built the coupling **the dominant faction shapes the ship's reputation** — composing it105 (reputation), it72 (dynamic dominance), and the peoples' characters. New `FactionDef.reputation_leanings` (a per-trait signed lean) + config `dominant_reputation_lean_per_year` (0.008): each year the dominant people's leanings drift the ship's reputation by `lean · per_year` (`SimState::apply_dominant_reputation_lean`, in the year tick after the polity voice). Assigned by nature on the `mercy` axis: **Hearth Union +0.5** (the long communal table), **Verdant Kin +0.3** (life-tending), **Ascension Circle −0.4** (flesh as first draft, coldly pragmatic), **Steel Covenant −0.2** (tools over doctrine); Meridian/First Flame neutral. So reputation is built not only from event choices but from the standing character of the majority — and (via it72) as demographic drift flips who rules, the ship's *name* slowly follows the new rulers' character. It compounds with it105/it106: a merciful majority drifts the ship toward the sanctuary-run charters that deepen mercy, a cold one toward the hard work that hardens it — the peoples, the reputation, and the missions offered all pulling in one direction. Deterministic (reads the dominant faction, no RNG); a fresh ship is neutral and reputation feeds only event/charter gates (which autoplay flies past), so determinism + all 9 soaks green. Data-load: leanings name a real trait (produced by an outcome) and stay in [−1, 1]. `#[serde(default)]` empty = a people whose rule leaves the name to its choices. Events unchanged (232). +1 test proving a Hearth-run ship grows merciful, an Ascension-run one hardens, and an unleaning people touches nothing (203 total).
- 2026-07-23 · charters (round 16) · the writ board reflects the ship's *crew* (it78, faction-aboard) and its *deeds* (it92, consequence gates) — but it105 just gave the ship a **graded character** (its reputation for mercy), and nothing read it at the charter level, though a reputation is exactly what opens and closes *jobs*. Added **charter reputation gates**: `ContractTemplate.min_reputation`/`max_reputation` (reusing it105's `ReputationGate`), folded into the shared `meets_in_world_gate`, so the board and the select action both honor them. The board now reflects *who the ship has become*, not just who it carries or what it did. Two charters: **the sanctuary run** (21st charter — a four-century refugee exodus a consortium entrusts only to a hull famous for treating the vulnerable as kin, `min_reputation mercy 0.6`; influence-heavy pay, a medical completion boon) and **the hard contract** (cold asset-recovery from a defaulted colony that will not give it up, work no soft hull would take and no client would trust a soft hull to finish, `max_reputation mercy 0.35`; credits-heavy, hazardous). So the mercy trait the events build (it105) now decides which *missions* the ship is offered — a merciful name opens the sanctuary and shuts the enforcement writ, a feared one the reverse. Data-load: charter reputation gates name a trait some outcome nudges (extending the it105 provenance check to charters). Zero soak risk (autoplay flies named charters, never the gated board; and a fresh ship is neutral 0.5 so neither gates until choices build a name). Charters 19→**21**, events unchanged (232). +1 test proving a neutral name opens neither, a merciful name opens the sanctuary and shuts the hard writ, and a feared name the reverse (202 total).
- 2026-07-23 · event families (round 16) · *opens the sixteenth rotation.* The event system had *boolean* memory (consequences: a deed done or not) and *per-event counters* (recurrence) — but no notion of the ship's **cumulative character**, a *tendency* built across many separate choices. A ship that has, mission after mission, chosen the merciful path is a different ship from one that repeatedly chose the cold one, yet nothing tracked that drift. Added **graded reputation**: new `SimState.reputation` (a `HashMap` of named 0-1 traits, 0.5 neutral, via `reputation()`/`adjust_reputation()`) + outcome `reputation_deltas` (nudge a trait a little) + `min_reputation`/`max_reputation` event gates (`ReputationGate`). Where a consequence is one deed, a reputation is the *sum* of a hundred small ones — the first continuous state the event system carries, distinct from every boolean flag and discrete counter before it. Exemplar the **mercy** trait: 8 outcomes across 4 events nudge it (take castaways aboard / share the thin table / enroll the stowaway / permit the mercy dose lift it; the cold twins lower it), building the ship's character over a campaign. Two payoffs read it: **the reputation precedes us** (`min_reputation mercy 0.7` — a stranger in distress who *knows your name* comes in a trust it extends to no one else: live up to the name, or corrode it by charging for the help) and **the feared name** (`max_reputation mercy 0.3` — a station that deals with a ruthless ship the way one deals with something dangerous, exits kept clear: lean into the fear, or begin the slow hundred-choice work of earning a different name). Data-load: a reputation gate names a trait some outcome nudges (typo guard, like the consequence provenance). Deterministic (reads/writes the trait, no RNG); a fresh ship is neutral so nothing gates until choices accumulate, all 9 soaks green. `#[serde(default)]` empty map = neutral everywhere. Diplomacy 23→**25**, events 230→**232**. +1 test proving mercy builds across merciful choices and opens the merciful door, and the cold choices open the feared one and close the merciful (201 total).
- 2026-07-23 · voice (round 15) · *completes the fifteenth rotation.* The ambient system is well-saturated (5 mood pools), so a fresh voice *mechanism* rather than a 6th pool. r8 gives each *faction* a mood voice; r11 gives the *crew's spirits* (morale) a voice — but the ship's **aggregate political climate** (it100's `aboard_approval_mean`, the very thing now steering unity) had none, though a ship can be high-spirited yet broadly *politically* restive, or content-in-mood yet with its peoples seething. Added the **polity-mood voice** (mirroring the r11 ship-morale voice exactly): new `announce_polity_mood` + `SimState.polity_mood_band` + `polity_souring`/`polity_warming` pools (5 each). When the member-weighted mood of the aboard peoples crosses *into* broad discontent (≤0.3) or broad ease (≥0.7) — not a single faction aggrieved but *most of them at once*, a general low-burning sense that the ship no longer looks after its own, or the rare civic weather of a polity that trusts it is being run for all of it — the ship says so once, wired into the year tick after `announce_faction_moods`. It's the *ship-level political* companion to the per-faction moods (r8) and the collective *spirits* (r11): three distinct voices for three distinct things — one people's grievance, the crew's happiness, and the whole polity's sense of fairness. Ties to it100 (the aggregate approval now steering unity gets a voice) + it93 (the rivalry-balancing act). Deterministic (indexed by year, no RNG); launch politics are neutral (0.5) so the baseline is silent, and the silent soaks (no events → approval stays 0.5) never fire it. `#[serde(default)]` empty pool = inert. Events unchanged (230). +1 test proving the polity curdling says so once, staying sour doesn't reprint, and a return to broad ease announces afresh (**200 total**).
- 2026-07-23 · provisioning (round 15) · food is a fully life-or-death resource — famine, chronic hunger, sustained plenty — but **energy only ever *browned out* the ship** (the-dimming): a power shortage was atmospheric, never fatal. Yet it101 just made the life-support plant kill people when it fails, and a plant, however sound, cannot run without power. Made **energy a life-or-death resource**: folded power availability into the it101 mortality — `life_support_mortality_loss` now uses the *worse* of the plant's physical condition and the grid's power (`effective = plant.min(energy/life_support_energy_critical)`, critical 800), so a well-repaired plant on a near-empty grid thins the crew as surely as a broken one on a full grid (a scrubber array with no current is a dead plant). Energy joins food at the top of the resource stakes: run the reactor dry and the air stops, no matter how well you kept the plant. Plus a content beat **the cold equations** (`energy_below 1500` — the grid can no longer power everything and life-support's draw is non-negotiable: black out the ship to a warm breathing core and endure the cold dark, vs keep it livable and run the plant thin — a gamble the thin air collects from the old and weak first). Deterministic (reads energy + condition, no RNG); the it101 test (full energy) is unaffected, and the soaks keep their grids powered so no soak newly starves — all 9 green. `#[serde(default)]` 0 critical = off. Engineering 25→**26**, events 229→**230**. +1 test proving a sound plant on a full grid loses no one and the same plant on a dead grid still kills (199 total).
- 2026-07-23 · campaign skeleton (round 15) · the stat-beats cover drift↑, adaptation↑, unity (crisis+recovery), morale↑, population↓, legacy_loyalty↓ — but one population stat still had no beat: **stability**, the functioning of the ship's institutions. Unity marks the *people* fracturing into camps; legacy_loyalty (it95) marks the *founders'* authority lapsing — but neither marked the ship's own *government* quietly ceasing to work. Added the **stability-collapse beat** (the descending governance mirror, structured like crisis/loyalty): `stability_beats` (0.3/0.18) + `stability_beat_family` + `fire_stability_beat` (after loyalty) + a `stability_beats_fired` contract counter. The three-way distinction is the point: crisis = the people at war with each other; loyalty-collapse = the founders no longer obeyed; stability-collapse = the institutions no longer *functioning*, even on a ship whose people get along and still revere the founders — councils that can't reach quorum, offices unfilled, the charter honored the way old customs are. Plus the honest gate `max_stability` (the governance twin of `min_morale`). Two legacy_drift beats gated `max_stability 0.3`: **the ungoverned ship** (a polity running on habit and goodwill — rebuild the institutions in earnest vs make the informal self-rule honest, fragile against a real crisis) and **the hollow offices** (`min_generation 5` — a crisis needs a binding decision and no one is empowered to make it — put the power back in the offices vs improvise it, this time and every time). Completes the population-stat beat coverage: every stat now has a beat. No silent-soak guard needed — stability doesn't fall in the eventless soaks (voyage drift doesn't touch it), so it never fires there; all 9 green. Legacy_drift 50→**52**, events 227→**229**. +2 tests (the beat fires as the institutions fail past the threshold and not on a well-governed ship; the max_stability gate opens only on a failing government) (198 total).
- 2026-07-23 · subsystems (round 15) · the `life_support_habitat` module — literally the system that *sustains life* — touched only morale (it73) and its own decay reduction. Its most obvious real-world function was missing: a life-support plant that fails badly should *kill people*. Built the coupling: **life-support condition → mortality**. New `subsystems.life_support_failure_threshold` (0.2) + `life_support_failure_mortality` (0.006): above the threshold the plant sustains everyone (no loss); below it, `life_support_mortality_loss` thins the crew each year by `mortality · (threshold − condition)/threshold · population`, scaled linearly from 0 at the threshold to the peak at zero condition — floored, so a barely-failing plant on a small crew may cost none. The module's third and most fundamental effect: neglect the plant that keeps the air breathable and it does not merely dent morale (it73) or risk a one-off breakdown event (the-failing-air) — it *continuously thins the ship*, a systemic driver of the it81 depopulation. Deterministic (reads condition, no RNG); gentle by design (a slow attrition, the pressure to keep the plant alive, not a massacre). Verified against the hardest cases: the three neglected-voyage soaks decay life-support to near-collapse over centuries, and the mortality is gentle enough that they still limp to their asserted end (no extinction); autoplay repairs the plant so it never triggers there — all 9 + neglected soaks green. `#[serde(default)]` 0 threshold = off. Events unchanged (227). +1 test proving a sustaining plant loses no one, a collapsing plant costs lives, and a fully collapsed plant thins the crew faster than a half-failed one (196 total).
- 2026-07-23 · factions (round 15) · *iteration 100.* The faction system feeds subsystem decay (it79), demographic growth (it86), and rival approval (it93) — but it had never touched the **ship's own cohesion**. A ship whose peoples are broadly content should hold together and one of resentful peoples should fracture, yet aggregate faction discontent had no bearing on `unity`, the very stat the crisis/recovery beats (it88) turn on. Built the coupling: new `SimState::aboard_approval_mean` (the **member-weighted** mean mood of the aboard peoples, so a large content majority outweighs a small soured minority) + config `approval_unity_coupling` (0.01). Each year `unity += coupling·(mean_approval − 0.5)` — a content polity steadies the ship, a resentful one wears at it. The point is where it leads: a ship of soured peoples doesn't only risk their *departure* (the r8 withdrawal) — its unity now erodes *toward the crisis the beats watch for*, so broad political discontent can, on its own, fray a ship into a cohesion crisis (and, mended, a recovery). The faction system, drift, and the crisis/recovery beats close into one loop: mistreat your peoples → they sour → the ship's unity falls → a fracture beat fires. Member-weighted so a dwindling soured minority (it72) matters less than the thriving content majority it86 helped build. Deterministic (reads approval, no RNG); neutral mood (0.5) is inert, so launch and the silent soaks (no events → approval stays 0.5) are untouched, all 9 green. `#[serde(default)]` 0 = off. Events unchanged (227). +1 test proving a ship carrying a content people ends more united than one carrying a resentful people, all else equal (195 total).
- 2026-07-23 · charters (round 15) · charters differ in shape, destination, beats, objective kind & module, hazard, toll, availability, and arcs — but every charter's *completion* gave the same thing: pro-rated pay. Nothing lasted. Yet a mission seen through should leave a **lasting mark**: building a great extraction works should make the ship's engineers permanently better. Added the **completion reward**: new `ContractTemplate.completion_reward` (a `CompletionReward` of subsystem boons + optional resource/population + a narration line) + `apply_completion_reward` (a testable helper) hooked into the conclude path beside the it92 completion-consequence — a capability the ship *keeps across voyages*, chiefly subsystem knowledge/condition, distinct from the cash it was flown for. Marked 5 charters by their nature: **the_karst_works** → +0.12 engineering knowledge (the great works master large-scale extraction), **deep_vein_survey** → +0.05 engineering (the survey hones the crews), **the_seedbearers_writ**/**founding_colony** → agriculture knowledge (greening a world deepens the gardeners' craft), **the_sunward_dive** → +0.1 education knowledge (the star-data enriches the archive). So charters now differ in the *legacy* they leave, not just the pay — and it composes the it94 objective_subsystem loop (a mission run on a module also *improves* that module). Zero soak risk (autoplay completes via its own path, not the conclude hook; and the helper is a pure function tested directly). Data-load: completion_reward subsystem ids name real modules. `#[serde(default)]` empty = an ordinary charter with no legacy. Charters/events unchanged (19/227 — a field on 5 charters). +1 test proving completing the Karst Works masters extraction and an ordinary charter leaves nothing (194 total).
- 2026-07-23 · event families (round 15) · *opens the fifteenth rotation.* This session added a rich set of new *state* dimensions — a thinned crew (`max_population`, it81), a chronic hunger (`lean_food_years`, it89) — but **complications couldn't read any of them**. A complication gated on drift, subsystems, consequences, a food shortage, factions, or recurrence — so the same crisis read identically on a full crew and a skeleton one, on a well-stocked ship and one worn thin by decades of want. Made complications **read the ship's new lived state**: added `Complication.max_population` + `min_lean_food_years` gates (folded into `active_complication`), so a crisis lands harder where the ship is least able to bear it. Two exemplars on complication-free crises (no new events — the r7 philosophy): **the failing air** gains a `too_few_hands` twist (`max_population 400` — a skeleton crew cannot work the failing plant *and* hold the dying decks at once, so a fuller ship would have held them all but this one must let a compartment go cold to keep another breathing: −8 crew, −morale) and **the thin table** gains a `nothing_left_to_cut` twist (`min_lean_food_years 12` — a ration triage on a ship lean for a generation already, no fat to shave, no reserve, so the cut a well-fed ship shrugs off goes straight to a bone already showing: −10 crew, −morale). The point: a crisis's weight is no longer only its own — it is the crisis *plus how little slack the ship has left*, which is exactly how real hardship compounds. Deterministic (reads the state counters, no RNG); all 9 soaks green. Events unchanged (227 — richer crises, not more of them). +1 test proving the thinned-crew twist rides only on a skeleton crew and the chronic-hunger twist only on a ship worn by years of want (193 total).
- 2026-07-23 · voice (round 14) · *completes the fourteenth rotation.* The ambient quiet reflects the ship's identity (drifted, r10), headcount (hollow, r12), and hunger (lean, r13) — but every one of those is a note of *decline*. A thriving, long-prosperous ship read the same neutral "ordinary" quiet as one merely scraping by; the good years had no *sound*. it96 just gave abundance a persistence counter, so this adds the **first positive-condition ambient**: new `ambient_fat` (7 lines) + `ambient_fat_years_threshold` (10), keyed to `SimState.fat_food_years`. Once the larder has stood full for a decade *and no grimmer note holds*, the quiet draws from the fat pool — second helpings offered without a second thought, the good bread every day, grow-decks at a comfortable idle breeding a sweeter fruit rather than a hardier one, round-cheeked loud children who take the full larder as the plain shape of the world, quarrels that stay small, a hundred small kindnesses hunger would have priced out, laughter the loudest thing aboard. Slotted at the *bottom* of the precedence chain (**lean > hollow > drifted > fat > ordinary**): the grim conditions speak first, so the good years only sound good on a ship not otherwise in decline, and a ship neither grim nor notably flush still reads the plain ordinary. Pure voice (no gameplay change), the ambient mirror of the it96 sustained-plenty mechanic and the ambient *set's* first note that isn't decline. `#[serde(default)]` empty pool = inert; determinism + all 9 soaks unaffected. Events unchanged (227). +1 test proving a long-prosperous ship's quiet reads fat, a merely getting-by ship's reads ordinary, and a prosperous-but-hollowed ship reads hollow (a grim note wins) (192 total).
- 2026-07-23 · provisioning (round 14) · it89 gave *scarcity* a persistence counter (`lean_food_years`) — but abundance stayed purely instantaneous, so there was no telling a windfall year from a *lifetime* of plenty, though on a generational ship they raise very different people. Built the symmetric mirror: new `SimState.fat_food_years` (consecutive years the store sits at or above a new `fat_food_threshold` 15000, reset when plenty ends — the exact inverse of `lean_food_years`) + a `min_fat_food_years` event gate. Two beats (both `food_above 15000`): **the soft generation** (`min_fat_food_years 15` — a cohort raised never seeing a measured plate, plenty the natural order: drill the discipline they lack (manufactured fasts, keep the lean ways alive) vs let them have their birthright — the latter seeds **`grown_soft`, reusing it75's the-long-winter complication** so the plenty-raised generation breaks worse in a real famine) and **the distrustful plenty** (`requires_consequence bred_to_thrift` — *closes the it89 chain*: a ship that made scarcity into identity, when the stores fill again, cannot trust them — keep the lean discipline and hoard against a famine that's over, vs let the young unlearn a fear whose cause is a generation dead). So the abundance framework now has both its instantaneous gates (it75) and its persistence counter, symmetric with scarcity, and the it89 thrift arc pays off. Deterministic (a counter read, no RNG); silent soaks stock food huge so `fat_food_years` climbs but event_chance 0 fires nothing there, all 9 green. `#[serde(default)]` 0 threshold = off. Survival 41→**43**, events 225→**227**. +1 test proving the soft generation needs a flush larder *and* years of it — one bumper harvest is not a lifetime of plenty, and a store run down ends it (191 total).
- 2026-07-23 · campaign skeleton (round 14) · the identity-stat beats cover drift↑, adaptation↑, unity (crisis↓ + recovery↑), morale↑, population↓ — but **one major identity stat had no beat: legacy_loyalty**, the ship's attachment to the founders' *authority*. The drift beats mark the crew becoming culturally *different*; nothing marked the distinct moment the founders' *covenant lapses* — a generation that stops treating the founding charter as binding. Added the **loyalty-collapse beat** (the descending mirror on legacy_loyalty, structured like the crisis beat): new `loyalty_beats` (0.35/0.2) + `loyalty_beat_family` (legacy_drift) + `fire_loyalty_beat` (after crisis) + a `loyalty_beats_fired` contract counter — as loyalty falls past each threshold a beat is forced. The distinction is the point: this is the *political* distance from the founders (their authority ceasing to bind), not the drift beats' *cultural* one (the people's texture changing) — a ship can drift far while staying loyal, or repudiate the founders while staying culturally close. Plus the honest gate: new `max_legacy_loyalty` event gate (the descending twin of `min_morale`). Two legacy_drift beats gated `max_legacy_loyalty 0.35`: **the lapsed covenant** (the charter obeyed in name only — say plainly it's ours to amend, an honest self-rule, vs renew the covenant deliberately before it dies) and **the advisory founders** (`min_generation 5` — a founding decree read aloud and simply *worked around* — rule by the living's judgment, vs fear a ship that answers to no one and restore the decrees' weight as prudent ballast). Completes the identity-stat beat coverage. Silent soaks clear `loyalty_beats` alongside the other threshold beats (voyage drift lowers loyalty over the centuries); all 9 soaks green. Legacy_drift 48→**50**, events 223→**225**. +2 tests (the beat fires as the covenant lapses past the threshold and not on a devoted ship; the max_legacy_loyalty gate opens only on a lapsed one) (190 total).
- 2026-07-23 · subsystems (round 14) · every subsystem effect touches decay, production, identity, or the crew — but **none had ever touched the mission objective**, the one quantity the voyage exists *for*, though the connection is obvious: a well-kept extraction bay mines faster, a rotting one slower. Built the **subsystem→objective coupling**. New `ContractTemplate.objective_subsystem` (the module a charter's work leans on, copied onto the `ActiveContract` at launch) + `subsystems.objective_condition_penalty` (0.4): the on-station accrual in `advance_contract` is scaled by `1 − penalty·(1 − condition)` of that module — a pristine bay works at the base rate, a rotting one down to 60%. Penalty-below-full (like it80/it87) so a well-kept ship's objective is untouched, and read *before* the mutable contract borrow. Marked 8 charters by their nature: mining/salvage (deep_vein_survey, the_karst_works, coronal_tap, the_long_tow, hollow_fleet) → **engineering_bay**, greening/colonization (founding_colony, the_seedbearers_writ) → **agriculture**, the science dive (the_sunward_dive) → **education_culture**. The subsystem axis at last reaches the mission — neglect the module your voyage runs on and you don't just risk a breakdown, you arrive *less done*. Safe by construction: completion is time-based (it-r9), so a slowed objective lowers score/pay but never blocks completion; the aborting-soak flies its charter via direct `advance_contract` with no decay (module stays 1.0 → factor 1.0), and autoplay repairs its modules — all 9 soaks green. Data-load: objective_subsystem names a real module. `#[serde(default)]` empty = off. Charters/events unchanged (19/223 — a field on existing charters). +1 test proving a rotting engineering bay mines the survey slower than a pristine one (188 total).
- 2026-07-23 · factions (round 14) · the approval meter treated every people in isolation — favor one and only that one moved — yet the factions have long carried **friction pairs** (r4): peoples genuinely at odds. But those rivalries were pure event-scenery; nothing made *favoring* one people a political cost with its rivals. Made the friction a **persistent relationship**: new `FactionDef.rivals` (authored symmetric across the 6 founders — Covenant↔Kin over deck-space, Accord↔Flame law-vs-creed, Ascension↔Flame augmentation-vs-tradition, Hearth↔Ascension the cold table) + config `rival_approval_spillover` (0.3). When an event's `faction_approval_deltas` *lifts* a people's approval, each of its aboard rivals loses a fraction of that gain to resentment (`SimState::apply_rival_approval_spillover`, wired into `apply_outcome` after the approval deltas land). So the meter becomes a **balancing act** — you cannot max approval for everyone, because pleasing one people is a slight to those it quarrels with, and (via the it8 withdrawal + it86 growth couplings) courting one faction too hard can drive its rival toward leaving *or* fading. Deliberately the cost of *favoritism*, not schadenfreude: only a positive gain spills; a slight to a people does not gladden its rivals. Data-load: rivals name real, other peoples, and rivalries are symmetric (a one-sided grudge is an authoring slip); `#[serde(default)]` empty = off. Deterministic (reads the favored deltas, no RNG); autoplay's approval shifts now ripple to rivals but stay gentle, all 9 soaks green. Events unchanged (223). +1 test proving favoring the Covenant sours its rival the Kin but not the unrelated Hearth, and that slighting the Covenant is no gift to the Kin (187 total).
- 2026-07-23 · charters (round 14) · the recent charter rounds gave availability (it78, faction-aboard) and a route toll (it85), but the charter never had a **mission arc** — completing one charter unlocking a follow-on, a campaign narrative spanning contracts. Built it by composing this session's it78 in-world gate + it84's forbidden_consequence onto charters, plus a completion seed. New `ContractTemplate.completion_consequence` (a mark recorded on the sim when a charter is seen through to full term — hooked into the conclude path in `advance`, after payout, before the contract clears) + `requires_consequence`/`forbidden_consequence` charter gates (folded into the shared `meets_in_world_gate`, so the writ board and the select action both honor them). The board now reflects the ship's **deeds**, not just its crew (it78) — completing a mission opens the next, a dark deed bars delicate work. The first arc: **deep_vein_survey**, seen through, records `karst_veins_proven`, which unlocks **the_karst_works** (19th charter — a permanent extraction settlement on the belt the survey charted, `requires_consequence karst_veins_proven` *and* `forbidden_consequence broke_a_bargain`: offered only to a hull that proved the veins *and* kept its word, since no consortium hands its lucrative works to an oathbreaker). Data-load: charter deed gates name a consequence *something* produces — an event outcome *or* another charter's completion (the arc's own seed counts), a typo guard spanning both systems. Zero soak risk (autoplay flies named charters directly, never the gated board; and it completes via its own path, not the conclude hook, so no soak records the mark). Charters 18→**19**, events unchanged (223). +1 test proving the works are barred until the survey proves the veins, offered once it does, and barred again to a ship that broke a bargain (186 total).
- 2026-07-23 · event families (round 14) · *opens the fourteenth rotation.* The complication system is rich, but a riding complication's extra toll landed on **whichever outcome the player picked** — it was a *state* twist, never a *choice* twist. Yet often the twist should punish a specific decision: an unstable reactor makes *pushing through* worse but leaves *scramming* alone. Added **outcome-conditional complications**: new `Complication.applies_to_outcomes` (outcome ids) — when non-empty, the complication's extra deltas + log land *only* when the chosen outcome is named, while the `description_add` still always shows (the twist is visible before the choice). Gated in `apply_outcome` on `outcome.id`, data-load typo-guarded (the named outcomes must be real outcomes of that event). It makes the twist interact with the *decision*, not just the state — the complication system's expressiveness completed. Exemplar on **hull_fracture** (no new event — the r7 philosophy): a new `a_ship_that_defers` complication (`requires_consequence deferred_maintenance`, `applies_to_outcomes ["monitor_it"]`) — on a ship that already carries a backlog of put-off repairs, the twist shows ("the ledger of 'later' has grown long"), but its extra hull/morale toll lands *only* if you defer this crack too, adding it to a list the ship has shown it does not come back to; *fixing* it (reinforce, or the it77 war-chest repair) escapes the toll and breaks the habit. So the twist rewards breaking a pattern and punishes continuing it — a moral the flat complication couldn't express. Deterministic (reads the chosen outcome, no RNG); all 9 soaks green. Events unchanged (223 — a complication on an existing event, richer not more). +1 test proving the toll lands on the defer choice and not on the fix, and rides only on a ship that already defers (185 total).
- 2026-07-23 · voice (round 13) · *completes the thirteenth rotation.* The ambient dead-air quiet reflects the ship's *identity* (drift-aware, r10) and its *headcount* (hollow, r12) — but it89 just gave the ship a new lived condition it couldn't hear: **a sustained hunger**. A ship a decade into a long lean — the fast mess line, the measured plate, the grow-decks pushed hard, the low daily preoccupation with the next harvest — should *sound* hungry in its quiet stretches. New **scarcity-aware ambient** (the fourth ambient pool, exactly paralleling r10/r12): `ambient_lean` (7 lines) + `ambient_lean_years_threshold` (10), keyed to it89's `SimState.lean_food_years`. Once the store has sat below the lean line for a decade, the quiet draws from the lean pool — the lived-in texture gone thin and rationed: children taught to clean the plate to the glaze and name it virtue, the harvest figures traded like weather, a festival whose feast is a slightly fuller plate and the ache of pretending, hunger become a low constant tenant behind the day's work. Wired into the ambient block with a clear **precedence: lean > hollow > drifted > ordinary** — a sustained hunger is the most immediate lived condition, an empty ship the next, an alien culture the quietest — so a ship both hungry and hollowed reads hungry first. Pure voice (no gameplay change), the ambient companion to the it89 long-hunger *beat*: the mechanic gates the chronic-scarcity reckoning; this makes the ordinary years of the long lean *sound* lean. `#[serde(default)]` empty pool = inert; determinism + all 9 soaks unaffected (soaks stock food huge so `lean_food_years` stays 0). Events unchanged (223). +1 test proving a long-hungry ship's quiet reads lean, a fed one's reads ordinary, and lean precedes hollow when both hold (184 total).
- 2026-07-23 · provisioning (round 13) · every provisioning gate to date is **instantaneous** — food is low *right now*. Nothing distinguished a bad *year* from a bad *generation*: a brief dip and a decades-long grinding scarcity gated identically, though on a generational ship the difference is the whole story — a cohort raised never knowing a full plate is a different thing from one hungry winter. Added the first gate keyed to **persistence**, not state: new `SimState.lean_food_years` (a rolling count of consecutive years the store sits below a new `lean_food_threshold` 4000 — the "not comfortably stocked" line, distinct from the near-famine `low_food_threshold` 500; incremented in the year tick after food settles, reset the moment the larder recovers) + a `min_lean_food_years` event gate. This is to a *continuous state* what the r11 recurrence-escalation was to *discrete event fires* — content that reads the grinding weight of a long scarcity, not just its acute crisis. Exemplar **the long hunger** (`food_below 4000` + `min_lean_food_years 15`): not a famine year but a famine *generation*, hunger settled from crisis into climate, the children of the lean years knowing nothing else — break it by main force (retool decks to grow-beds, spend the reserves on one great resupply push, teach the young that scarcity is an enemy to beat) vs make the ship a people of the lean (ration woven into rite, thrift bred to the bone, scarcity made into character — seeds `bred_to_thrift`, a people that will look on any future plenty with the deep suspicion of those who learned not to trust a full plate). Deterministic (a counter read, no RNG); silent soaks stock food huge so `lean_food_years` stays 0 and the gate never opens there; autoplay's index-0 (break the hunger, +food) self-limits by ending the streak; all 9 green. `#[serde(default)]` 0 threshold = off. Survival 40→**41**, events 222→**223**. +1 test proving the long hunger needs a currently lean larder *and* years of it — one bad season is not yet a lean generation, and a recovered store ends it (183 total).
- 2026-07-23 · campaign skeleton (round 13) · every threshold beat fires on a stat moving *one* direction — drift↑, adaptation↑, unity↓ (crisis), morale↑ (flourish), population↓ (depopulation) — so nothing marked a stat *recovering*. A ship that fractured into a unity crisis (the crisis beat fired) and then climbed back out — that hard-won turnaround, the mending — got no beat, though it is one of the better stories a voyage can tell. Added the **recovery beat**, the crisis beat's hopeful mirror: new `recovery_beat_threshold` (0.6) + `recovery_beat_family` (legacy_drift) + `fire_recovery_beat` (after the crisis firer). When the ship has fractured (`crisis_beats_fired > 0`) and `unity` climbs back to/above the threshold, a beat is forced and **the crisis counter is reset** — so the collapse beats re-arm and a relapse fires afresh, modeling a real oscillation. The threshold sits well clear above the crisis band (0.2/0.35) so there's a hysteresis dead-band and no flip-flop (data-load enforces `recovery > worst_crisis`). Cohesion now has a **full fall-and-rise arc on one stat** — crisis on the way down, recovery on the way up — where flourish/crisis split across morale/unity. Plus the honest gate the content needs: new `min_unity` event gate (the cohesion twin of `min_morale`), so recovery content never surfaces on a fracturing ship. Two legacy_drift beats gated `min_unity 0.6`: **the mending** (a ship that came apart and healed decides whether to enshrine the reconciliation as an inoculation against the next fracture, or let the peace be ordinary) and **the forged unity** (`min_generation 3` — a crew never so united as just after nearly tearing in half: spend the hard confidence on something bold, or bank it as the sober wisdom of the once-fractured). Silent soaks clear `crisis_beats` so `crisis_beats_fired` stays 0 and recovery never fires there — no new guard; the crisis test (unity 0.33 < 0.6) is untouched; all 9 soaks green. Legacy_drift 46→**48**, events 220→**222**. +2 tests (recovery fires only after a crisis when unity recovers and resets the counter to re-arm; the min_unity gate opens only on a reunited ship) (182 total).
- 2026-07-23 · subsystems (round 13) · after it80 (agriculture), every module had a condition→output effect *except one*: **education_culture**. Its *knowledge* drives drift-resistance (r10), but its physical *condition* did nothing — the last module where "still standing" and "actually working" were the same. Gave it the elegant role waiting for it: **education is the knowledge keystone** — the counterpart to engineering's decay keystone. Where engineering's condition scales every module's physical *decay* (r7), education's condition now scales every module's *knowledge transmission* to the next generation. New `subsystems.education_transmission_condition_penalty` (0.5): in `transmit_knowledge` the tier-based transmission is multiplied by `1 − penalty·(1 − education_condition)`, so a vivid archive (condition 1.0) carries the founding craft forward in full — the untouched baseline — while a crumbling one loses more of it each generation. Penalty-below-full (like it80) so nothing rebalances. The pairing is the point: engineering's condition keeps the ship's *body* sound, education's keeps its *mind* sharp, and both are ship-wide keystones a player must maintain — neglect the archive and the whole crew forgets its crafts faster, on every module at once. Completes condition→output coverage across all 6 modules. Deterministic (reads condition, no RNG); founding condition 1.0 = baseline so the existing transmission test + determinism hold; autoplay repairs the archive (full transmission), all 9 soaks green. Config `#[serde(default)]` 0 = off. Events unchanged (220). +1 test proving a vivid archive carries knowledge forward better than a crumbling one at the same schooling tier (180 total).
- 2026-07-23 · factions (round 13) · the faction axis carries two mature but *disconnected* systems: the **approval meter** (r8 — how you treat a people; below a line they withdraw, and it79 feeds their tended module's decay) and **demographic drift** (r11/it72 — `growth_bias`, a *fixed* per-faction rate that waxes or wanes a people's share each generation). They never interacted — a people's growth was pure nature, indifferent to how the ship treated it. Built the missing link: **approval bends demographic growth**. New `factions.approval_growth_factor` (0.04): in `apply_faction_demographic_drift` the effective bias is now `base_growth_bias + factor·(approval − 0.5)`, so a beloved people (approval → 1) waxes toward the majority and a resented one (→ 0) wanes even beyond its base bias — how you treat a people decides not just whether it *leaves* (the r8 withdrawal) but whether it *grows or fades* over the centuries, and (via it72) whether it ever becomes the dominant faction that steers drift/dilemmas/gates. It closes a loop across the two biggest faction systems: mistreat a people and it both sours *and* dwindles; cherish one and it thrives into the majority. Neutral approval (0.5) is inert, so the it72 demographic test (both peoples at 0.5) and determinism are untouched, all 9 soaks green. `#[serde(default)]` 0 = off. Events unchanged (220). +1 test proving two peoples of identical nature diverge by their standing — the cherished waxing, the resented waning, around a neutral baseline (179 total).
- 2026-07-23 · charters (round 13) · every *named* lever is built; r11 gave charters `hazard` (a **stochastic** toll — a dangerous writ breeds more crises), r12 gave availability gates. But no charter had a **deterministic ongoing toll** — a route whose *nature* is felt every year, not only in the crises it throws. Added the **annual toll**: new `ContractTemplate.annual_toll` (an `AnnualToll` of resource/ship/population deltas), applied once per voyage-year in `year_boundary_tick` — read straight from the template via the contract's `template_id` (no copied field to store/serialize, unlike hazard). Hazard's deterministic companion: where hazard *raises the crisis rate*, this is a **steady drain the whole voyage**, so a route that wears at a ship *feels* that way continuously. Marked the two most-punishing (soak-unflown, so zero regression) routes by their fiction: **coronal_tap** (plasma-mining in a dying star's reach — the radiation-and-heat toll: morale −0.004/yr, hull −0.002/yr) and **hollow_fleet** (recovering a fleet of the dead in hostile space — the grim, haunted work: morale −0.005/yr, unity −0.002/yr). Both already pay well + carry hazard, so the ongoing toll deepens the risk/reward coherence — these routes are dangerous, draining, *and* lucrative. Deltas kept gentle, survivable headwinds (data-load bounds the per-year crew drain to ≤3, so a runaway toll is caught); added `PartialEq` to the three delta structs for the zero-toll check. Deterministic (no RNG); ordinary charters (`annual_toll` all-zero) are untouched, `#[serde(default)]` keeps old data inert; all 9 soaks green (autoplay flies its own charters, none newly-marked). Charters unchanged (18 — two existing marked), events unchanged (220). +1 test proving the star-reach route wears morale and hull every year faster than a quiet survey, which exacts nothing (178 total).
- 2026-07-23 · event families (round 13) · *opens the thirteenth rotation.* The consequence-gate system had a positive gate — `requires_consequence` (an event *needs* a past choice on record) — but **no negative**: no way to say a generally-available event is *barred* for a ship with a disqualifying history. So a choice could open future content but never permanently *close a door*. Added **`forbidden_consequence`** (the negative twin, `EventTemplate`): the event stays out of the pool if *any* listed tag is on record — checked in `passes_gate` right after the positive gate, data-load typo-guarded like it. It completes the consequence system and enables true branching where a deed forecloses content. Two exemplars in different flavors: **the stranger's trust** (diplomacy, `forbidden_consequence [broke_a_bargain]` — a passing hauler's generous terms on nothing but the ship's word, the old spacer's trust *never extended to a hull known to have broken a bargain*; ties to the r7 debt arc, a karma/reputation bar) and **the founders vindicated** (legacy_drift, `min_generation 5`, `forbidden_consequence [buried_second_log, the_lost_archive]` — a late-voyage council with the whole record open finds the founders provably wise and reveres them anew, *impossible for a ship that buried its founding truth or let the archive die*: you cannot vindicate what you hid or forgot — a thematic-precondition bar, and a **multi-tag** one closing on either). Deterministic (reads recorded consequences, no RNG); most ships carry neither tag so the events fire freely, and only a specific dark history bars them; all 9 soaks green. Diplomacy 22→**23**, legacy_drift 44→**46**, events 218→**220**. +1 test proving the trust is offered to a clean name and barred once a bargain is broken, and the multi-tag bar closes on either tag (177 total).
- 2026-07-23 · voice (round 12) · *completes the twelfth rotation.* r10 made the ambient dead-air quiet **drift-aware** (a far-drifted ship's quiet reads alien, drawn from a `ambient_drifted` pool); r11 gave the ship's collective morale a voice. But the ambient quiet ignored the ship's most *physical* change — **how many people are left**. it81 just gave the crew-thinning its beats; this gives it its ambient. New **population-aware ambient** (the third ambient pool, exactly paralleling r10): `ambient_hollow` (7 lines) + `ambient_population_threshold` (500) — once the crew has thinned to or below the ceiling, the quiet stretches draw from the hollow pool: the same lived-in texture gone sparse and echoing — corridor lights coming up deck by deck for a single walker and going dark behind them, a mess hall built for six hundred serving forty in one warm corner, a muster read in full in under a minute, sealed cold residential rings labelled with families no longer numerous enough to fill them. Wired into the ambient block in the year tick with a clear **precedence: hollow > drifted > ordinary** — emptiness is the louder note in a silence, so a ship both thinned *and* far-drifted reads hollow first. Pure voice (no gameplay change), the natural companion to the it81 depopulation beats: the beats mark the *stages* of thinning, the ambient makes the *ordinary quiet between them* sound as empty as the ship has become. Absolute headcount threshold (founding ~1000), `#[serde(default)]` empty pool = inert. Determinism + all 9 soaks unaffected (soaks stock food huge so crews rarely thin to 500). Events unchanged (218). +1 test proving a thinned crew's quiet reads hollow, a full crew's reads ordinary, and hollow precedes drifted when both hold (176 total).
- 2026-07-23 · provisioning (round 12) · it75 added the **abundance gates** (`food_above`/`credits_above`) but left them isolated — two lone beats, and the abundance framework never *interacted* with the mature shortage set. Built the first interaction: the **abundance-scarcity paradox** — the bitter binds only a rich-yet-stranded generation ship faces, each compounding an abundance gate with a shortage gate on a single event (the abundance twin of r3's the-long-winter, which compounded two *shortages*). No new mechanic — `passes_gate` already ANDs all gates; this is the it75 gates finally *combined* with the shortage set. Two beats: **the gilded hunger** (`credits_above 20000` + `food_below 3000` — the treasury never fatter and the larder never thinner, a fortune lightyears from any port to spend it at, the oldest lesson of the dark relearned: *you cannot eat money* — endure on the founders' discipline and keep the useless treasury whole vs burn deep into the fuel reserve chasing a resupply the money might finally buy) and **the well-stocked doldrums** (`food_above 18000` + `fuel_below 0.25` — the mirror irony: holds brimming and the tank dry, a crew with everything to eat and no push left, becalmed fat and going nowhere — wait it out on the ample stores vs spend the last fuel in one hard burn to claw back into a live current). The point is that abundance is not safety on a generation ship: wealth in the wrong resource, or in the wrong place, is its own kind of peril, and the it75 gates now say so in combination. Autoplay-safe (index-0 bounded: endure / wait); the paradox states never arise in the food-flush silent soaks, all 9 green. Survival 38→**40**, events 216→**218**. +1 test proving the gilded hunger needs wealth *and* famine at once — neither alone brings it (175 total).
- 2026-07-23 · campaign skeleton (round 12) · the beat lattice keys on time, every identity stat (drift↑/adaptation↑/unity↓/morale↑), mission progress, voyage phase, and political transition — but **one major state dimension had no beat: the crew's headcount**. A generational sealed-crew ship (no organic growth) only ever *loses* people, and that slow demographic peril — the founders' thousands dwindling to hundreds over the centuries — is the voyage's defining tragedy, yet nothing marked the decks thinning. Added the **depopulation beat** (the descending mirror of the flourish beat): `depopulation_beats` (founding-fractions 0.6/0.4/0.25) + `depopulation_beat_family` (survival) + `fire_depopulation_beat` in the beat chain after flourish — the first month the population falls to/below each fraction of its *founding* size, a survival beat is forced. **Campaign-scoped** (`SimState.depopulation_beats_fired`, not a contract counter) so a ship recruited-up in drydock between voyages never re-marks a stage it already passed — the founding-referenced decline tracked across the whole campaign. Plus the honest gate the content needs: new `EventTemplate.max_population` (absolute headcount ceiling, the descending mirror of `min_morale`; kept absolute not fractional so `passes_gate`'s ~55 callers need no `data` threading), wired into `passes_gate`. Three escalating survival beats gated to a thinned ship: **the thinning decks** (`max_population 600` — consolidate to the living decks vs keep the whole ship lit), **the hollow muster** (`400` — mothball what can't be tended vs drive the crew harder), **the last quarter** (`250`, `min_generation 3` — pool into a viable core vs hold the founders' way of life even now, the manuals' red line). Data-load: depopulation_beats descending + in-range + family has events. Silent soaks stock food huge so population never thins → the beat never fires there (no guard needed); autoplay resolves any thinning beat in stride; all 9 green. Survival 35→**38**, events 213→**216**. +2 tests (the beat fires as the crew crosses the first fraction and not on a full ship, campaign-scoped; the max_population gate opens only on a thinned crew) (174 total).
- 2026-07-23 · subsystems (round 12) · it59 closed the "condition→output" asymmetry for medical and security (a rotting bay should *earn less*, not just risk a breakdown) — but it left the **food module** behind. Food production was `production.food · crew_mult · (1 + agri_bonus)` where `agri_bonus` is **tier-only**: a rotting hydroponics bay fed *exactly* as many as a pristine one, condition irrelevant. Built the missing coupling: **agriculture condition scales food yield**. New `subsystems.agriculture_condition_food_penalty` (0.3) → yield factor `1 − penalty·(1 − condition)` (`agriculture_condition_food_factor`, folded into the food-production line). Deliberately **penalty-below-full** (not the it73 swing-around-half): a pristine farm (condition 1.0) yields the *untouched baseline* so nothing rebalances and no soak shifts, and only degradation bites (−18% at the 0.4 neglect threshold, −30% at total collapse, clamped ≥ 0). It closes the same asymmetry it59 fixed, now for the module that matters most in a famine — and it feeds the it79 tender-spiral perfectly: neglect the farm → less food (new) → famine → sour the Verdant Kin (it79) → they let it rot faster (it79) → less food still. Keeping the hydroponics in repair now pays back every year, not only at the breakdown cliff. Deterministic (reads condition, no RNG); founding condition 1.0 = baseline so identity+determinism hold; autoplay repairs the farm (yield stays full) and the famine soaks set food huge, all 9 green. Config `#[serde(default)]` 0 = off. Events unchanged (213). +1 test proving a pristine farm yields the baseline, a rotting one feeds fewer, and the factor never turns food negative (172 total).
- 2026-07-23 · factions (round 12) · the faction system is exhaustively built, but the **approval meter** is only ever a *threshold + event* lever: events shift it, neglect erodes it, and below a line a people withdraws — it never touches the *continuous tick*. And the subsystem rounds built only *half* a loop — **subsystem neglect sours its tending faction** (`apply_subsystem_neglect_sentiment`), but a devoted people tended its domain no better than a resentful one. Built the missing reverse: **a module's tending-faction approval scales its yearly decay**. New `subsystems.tender_approval_decay_scale` (0.3): each module's decay is multiplied by `1 + scale·(0.5 − approval)`, so a devoted tender (approval→1) slows its module's rot ~15%, a resentful one (→0) speeds it ~15%, neutral leaves it be — read via new `SimState::tender_approval(data, id)` (the first aboard people whose `tended_subsystem` matches), folded into `decay_subsystems` alongside the r7 keystone multiplier. The point is the **spiral it closes**: neglect a module → its tenders sour (existing) → soured tenders let it rot faster (new) → the worse rot sours them further — a self-reinforcing decline (and, run forward, a virtuous circle: keep a people content and its domain stays sharp for less upkeep). The first time faction approval feeds the systemic tick, not just gates a beat. Deterministic (reads approval, no RNG); launch approval 0.5 is neutral so nothing changes until a played choice or neglect moves it — determinism + all 9 soaks green. Config knob `#[serde(default)]` 0 = off. Events unchanged (213). +1 test proving a resentful people lets its farm rot faster than a devoted one, all else equal (171 total).
- 2026-07-23 · charters (round 12) · every *named* charter lever is built (5 shapes, destination pools, beat-bias, objective kinds, scheduled beats, hazard), and charter availability already gates on **renown** — but that's *cross-campaign* dynasty fame. The charter never had an **in-world** availability gate: a writ offered because of *who this ship is this voyage*. Added `ContractTemplate.requires_faction_aboard` — a mission offered only while the named founding peoples are aboard *right now* (the charter-level parallel to it77's outcome gates, and the in-world twin of `min_renown`). New shared `meets_in_world_gate(sim, template)` (reads the living roster) checked by both the writ board (locks the card and labels it "NEEDS <people>" rather than "RENOWN n") and the SelectCharter action (a locked writ can't be put under consideration). So the board reflects the ship's *character*, not just its fame — and a charter appears/vanishes as a people joins or leaves (schism/withdrawal). Exemplar the_seedbearers_writ (18th charter): a 420-yr seedworld greening a consortium entrusts to one hull only — the ship carrying the **Verdant Kin**, whose gardeners alone are trusted to coax a biosphere from dead rock (colonization, 40 000 hectares, garden+long_station tags, biology_medical beat-bias, min_renown 100). Data-load: the gate names real founding peoples + at least one charter exercises it. Zero soak risk — autoplay flies *named* charters directly, never via the gated board. +1 test proving the writ is offered only while the Verdant Kin are aboard and an ungated charter always is (170 total). Charters 17→**18**, events unchanged (213).
- 2026-07-23 · event families (round 12) · *opens the twelfth rotation.* The set has parity, chains (opportunistic + scheduled), complications, trilemmas, and recurrence escalation — but every crisis has always offered the **same fixed exits** regardless of how prepared the ship is. Complications add a *twist*, never a *choice*; nothing lets a past investment *unlock a better path*. New mechanic **state-gated outcomes**: `EventOutcome.requires` (an `OutcomeRequirement` — `requires_consequence` and/or `min_knowledge` subsystem floors, `is_unconditional()` fast-path). An outcome carrying one is offered only to a ship that earned it; `outcome_available` + `available_outcome_indices` filter, and the modal renders only the available outcomes while pushing the *real* index (so `apply_outcome`/`ResolveEvent`/autoplay indices are untouched). **Gated outcomes are authored after the unconditional ones**, enforced by a data-load guard (outcome 0 must be unconditional) so a ship is never left choiceless and autoplay's index-0 always lands on an available outcome. The outcome-level parallel to the whole-event gate system — the "your preparation pays off with an option others don't have" fantasy the set never had. Two exemplars on the workhorse engineering crises (no new events — same philosophy as r7 complications): **coolant_breach** gains a master-controlled-cooldown outcome (`min_knowledge engineering_bay ≥ 0.7`) — a ship that kept its engineers sharp closes the breach clean, no annex vented, no one lost, where an unprepared ship must vent or crudely patch; **hull_fracture** gains a shipyard-grade frame replacement (`requires_consequence war_chest`) — *ties back to it75's the-full-coffers*: the prudent choice to bank the treasury pays out generations later as a hull made whole rather than braced. Data-load: outcome knowledge-floors name real subsystems, outcome consequence-gates are produced by some outcome (typo guards, extending the existing checks). Deterministic (reads state, no RNG); silent soaks and autoplay unaffected (base outcomes always available). Events unchanged (213) — same count, crises that now reward a prepared ship. +1 test proving the master fix hides on a lost-expertise bay and shows on a sharp one, the war-chest repair hides without the reserve and shows with it, and both resolve by real index (169 total).
- 2026-07-23 · voice (round 11) · *completes the eleventh rotation.* Voice has given a line to generational life, crew turnover, extinction, ambient quiet, phase transitions, homecoming, recurring crises, per-faction moods, subsystem maintenance, and drift-aware ambient — but one collective thing stayed mute: **the ship's own overall mood**. Individual factions announce when they cross into restlessness or contentment (`announce_faction_moods`), yet the whole ship's morale crossing a threshold — the decks going heavy or light — said nothing, even though it73 just gave morale a maintenance lever (a sound habitat lifts it), making band-crossings something the player now *causes*. New `announce_ship_mood` (the collective twin of the faction-mood voice): when the crew's morale crosses *into* a grim (≤0.3) or a buoyant (≥0.7) band — not every year it sits there — one pooled ambient line surfaces (`ship_mood_darkening`/`ship_mood_lifting`, 5 each), deterministic (indexed by year, no RNG), settling back to steady silent-but-remembered. `SimState.morale_band` tracks the last-announced band (serde-default); the **launch baseline is recorded, not announced** (`morale_band` initialized from the 0.7 launch morale after construction, so the hopeful start isn't misread as a lift). Wired into the year tick right after `apply_voyage_drift`, so it reads morale *after* the habitat lift and voyage strain have both settled for the year. Ties the it73 morale lever to a felt payoff — keep the habitat sound, morale climbs, and the decks audibly lift; let it fail and they go grim. Pure new voice (no gameplay change). +2 tests (the pools are authored and vary; a crossing says so once, stays quiet while it holds, and a recovery announces afresh) (168 total). Events unchanged (213). Determinism preserved.
- 2026-07-23 · provisioning (round 11) · the shortage set is exhaustively built — food/fuel/parts/energy gates, compounding crises, insurance/debt/rationing chains, and couplings to factions, objective, population, subsystems, and dated foresight — but **every provisioning gate is `_below`**. The whole framework is reactive to *want*; nothing keys on *plenty*, though a generational ship hits fat years as surely as lean ones. New **abundance gates** `food_above`/`credits_above` (the exact mirror of the shortage gates in `passes_gate`: the event stays out of the pool until the ship is genuinely flush — food half again past its 12000 founding stock, treasury well past its 10000) — the first gates keyed to prosperity rather than scarcity, so a fat-years choice reads as a *consequence of plenty* rather than a random roll. Two exemplar beats: **the fat years** (`food_above 18000`, survival — a generation that never knew a thin plate: bank the surplus and keep the founders' thrift → `kept_the_thrift`, vs ease the rations and let the ship live well → `grown_soft`) and **the full coffers** (`credits_above 25000`, diplomacy — a treasury swollen to a fortune: bank a war chest against lean contracts → `war_chest`, vs pour the prosperity into the decks and people, refitting the habitat). And the round *closes a loop back into scarcity*: a **complication on the-long-winter gated `requires_consequence grown_soft`** — a ship that feasted through its fat years meets the harshest famine with a generation that never learned to go hungry, and it bites harder (extra losses, extra heartbreak). *"A ship pays for its plenty in the winter that follows it."* So the abundance choice isn't flavor — it reaches generations forward into how well the ship weathers its next famine, the positive mirror of prudence made to matter. Survival 34→35, diplomacy 21→22, events 211→**213** (+2). +1 test proving the fat-years gate waits for real surplus and that feasting softens the ship into a worse long winter (166 total). Determinism preserved.
- 2026-07-23 · campaign skeleton (round 11) · the beat lattice keys on time, every identity stat, mission progress, and voyage phase — but it72 made faction **dominance dynamic** (demographic drift can grow a launch-minority into the majority), which opens a beat trigger nothing had: not a stat crossing but a **political transition**, the moment *power changes hands*. New `power_transition_beat_family` (`legacy_drift`) + `fire_power_transition_beat` in the beat chain (before anniversary): when `clear_majority_faction` — the largest aboard people, but only past a **1.1× margin** so a near-even split's tie-break wobble counts as *no* clear majority — differs from the one the skeleton last marked, a beat is forced and the new majority recorded. The first clear majority a voyage is recorded silently (no beat at launch); a genuine *change* fires. Requires an active contract (a beat of the voyage, not drydock). New `SimState.last_dominant_faction` tracks it (serde-default, empty until first mark). Two content beats (legacy_drift, `min_generation 4`): **the shifting helm** (a people that boarded a minority now sets the course — govern in continuity vs remake the ship in its own image) and **the inherited ship** (a generation realizes the ship was built by/for a people it's no longer chiefly made of — keep faith with the build vs reshape it for the living). The margin was the fix for a real trap: the founding factions launch ~equal, so a bare-largest "dominant" flips on tie-break noise the moment demographic drift nudges it — which fired the beat spuriously and broke three long silent soaks; the decisive-majority threshold means short soaks never develop a clear majority (safe) and only the 340-year contract soak does (already guarded). Events 211→213 (+2). +1 test proving the beat fires on a decisive hand-change and not on a steady majority (165 total). Determinism preserved.
- 2026-07-23 · subsystems (round 11) · a fresh module→stat coupling the mature axis lacked: **the habitat drives morale**. The ship has no organic population growth (a sealed crew that only shrinks/shifts), so a habitat→population coupling doesn't fit — but the `life_support_habitat` is where the people *live*, and its condition never touched how they *feel*. Morale is a stat with almost no positive driver — voyage-drift only strains it *down* (−0.0012/yr). New `habitat_morale_swing` (0.01): each year `swing·(condition − 0.5)` moves morale, so a home kept above the midpoint lifts spirits (+0.005/yr at full condition), a failing one (cramped, cold, patched) depresses them, a neutral one does neither — the first maintenance-driven *counterweight* morale has to the strain of the centuries, and it feeds the r8 flourish beat (a well-kept happy ship reaches its golden ages). `habitat_morale_effect` wired into the year tick after the r9 unity recovery. Clamped 0-1, deterministic. (The lift exposed that the silent full-contract soak, which climbs morale now instead of only declining, could trip the flourish beat — cleared `flourish_beats` in that soak's guard alongside the other forced beats; all playthrough soaks green.) Events unchanged (211). +1 test proving a sound habitat lifts morale, a failing one drags it, a middling one neither (164 total).
- 2026-07-23 · factions (round 11) · the faction system is deeply built, but one thing was always *static*: **the balance of power itself**. Which people is dominant — the lever r9 (ideology→drift), r10 (dilemma odds), and r1/r6 (event gates) all lean on — was fixed at launch and changed only when a schism shed one. Made it **dynamic**: new `FactionDef.growth_bias`, a gentle per-generation share shift applied each generation (`SimState::apply_faction_demographic_drift`, before the existing assimilation, then `rebalance_factions` renormalizes to the head count), so a people's numbers wax and wane over the centuries. Assigned by nature: the fecund **Hearth Union** +0.03 and life-tending **Verdant Kin** +0.02 grow, the traditional **First Flame** +0.01 holds, the augmented **Ascension Circle** −0.04 dwindles (a people that does not reproduce naturally). The consequence is the whole point: over 12 generations a launch-minority can become the majority and **the dominant faction flips mid-voyage** — so who steers the ship's drift, backs its dilemmas, and colors its events is now a story that evolves, not a launch condition. Feeds cleanly into the existing assimilation (a people that dwindles far enough gets folded into a larger one — demographic decline, not just cultural drift, can now end a people). Never drifts a people below one soul (that is the schism's/assimilation's job). Data-load guards `growth_bias ∈ [−0.2, 0.2]`; `#[serde(default)]` 0 keeps old data stable; deterministic (no RNG); all playthrough soaks green over their full centuries. Events unchanged (211). +1 test proving a level-pegging launch (augmented ahead) flips to a Hearth majority over 12 generations (163 total).
- 2026-07-23 · charters (round 11) · built the doc's one still-unbuilt charter lever — **"different risk profiles."** Charters differed in shape, objective, destination, beat-bias, and scripted structure, but every voyage was equally *dangerous*: a quiet mining survey and a lawless-derelict recovery drew crises at the same rate. New `ContractTemplate.hazard` (copied onto the active contract at launch, `#[serde(default)]`) added to the **immediate-crisis category weight** for the whole voyage in `category_weights` — so a dangerous writ genuinely *feels* dangerous, breeding more crises the whole way, not just when the ship is already in distress. It's the charter's own risk, distinct from the ship's present state. Marked the two most-hazardous routes (both unflown by any soak, so zero regression risk): hollow_fleet (0.28 — a derelict-fleet recovery in hostile space) and coronal_tap (0.2 — plasma-mining inside a dying star's reach), each already paying well (3800cr+320inf; 5500cr+2000energy) so the risk/reward reads coherently. Data-load guards hazard ∈ [0, 1]. No new content — a dimension every charter now differs along. +1 test proving a hazardous charter's crisis weight exceeds a survey's by *exactly* its hazard (162 total). Charters unchanged (17), events unchanged (211).
- 2026-07-22 · event families (round 11) · a fresh mechanic serving a quality bar the rich set couldn't: **no repetition tells** for *recurring* crises. A crisis that fires four times across a voyage read identically each time — the fourth reactor failure exactly like the first. Gave events a memory of their own recurrence: new `SimState.event_fire_counts` (per-template, `#[serde(default)]`, incremented as each event resolves *after* its complication has read the prior count) + a new `Complication.min_prior_occurrences` gate, so a complication can ride *only* on the Nth occurrence — a recurring crisis that **escalates** instead of merely repeating. Exemplar: contagion (a recurring outbreak) gains the-third-outbreak complication (`min_prior_occurrences 2`) — the first plague is routine, but the third wears worse than the first two together, "not for the sickness, which the medics knew cold by now, but for the grinding sense of a ship that could not seem to stay well" (−morale/−unity + the ragged wards take −0.05 medical condition). Composes with the existing first-match complication ordering (a low-count occurrence falls through to any other complication; the recurrence one preempts once its threshold is met). Deterministic (counts increment on apply, no RNG); silent soaks fire no events so counts stay empty; autoplay resolves any escalated event in stride, all green. Events unchanged (211) — same count, a crisis that now plays differently the third time. +1 test (the weariness rides only after two prior outbreaks, and shows in the description before the choice) (161 total).
- 2026-07-22 · voice (round 10) · every frequent log line is pooled, so this pass made the log *reflect the ship's identity* — the freshest voice move, tying into the drift arc the last rounds deepened. The ambient dead-air lines (r2) were a flat pool indexed by year, the same "life aboard" texture whether the ship was fresh from home or a century estranged from it. Split it: past `ambient_drift_threshold` (0.5 cultural_drift) the quiet stretches draw from a new **`ambient_drifted`** pool (7 lines) — the identical lived-in cadence gone *alien*: a child asking what a planet felt like and no one able to answer from memory; the founders spoken of "the way old worlds speak of gods, real once and no longer quite believed in"; a festival run solemnly whose first meaning no one living knows; the founding log read aloud in a tongue the founders would need translated. So the log's own texture now marks how far the people have drifted — an early ship's quiet reads familiar, a far-drifted ship's reads strange. Drift-conditioned pool selection in the ambient emission (`#[serde(default)]` empty pool → always the baseline, back-compat); deterministic (drift-gated, year-indexed, no RNG). +1 test proving a far-drifted ship's quiet draws the alien pool and an early ship's the familiar one, never the alien (160 total). Events unchanged (211). **Completes the tenth full rotation through all 7 axes.**
- 2026-07-22 · provisioning (round 10) · the shortage set is all *reactive* (answer a random shortfall) and recently coupled to factions/objective; this pass added a fresh kind — **foresight on a determined clock**, composing round-9's scheduling with complications for the first time in provisioning. the-charted-dearth (`phases:[travel,operation]`, `max_generation 7`): the navigators bring not a crisis but a *date* — a long lean reach of the dark, nothing to skim, computed to sixty years out — and the only question is what a ship does with sixty years of warning. Both outcomes `schedule_followup` the-dearth-arrives at +60 (the reach is charted; it comes either way), but lay-in-against-the-day spends the good decades (credits/food now) to seed `laid_in_for_dearth`, where trust-to-slack banks nothing. the-dearth-arrives (`scheduled_only`) fires on its charted year regardless: ration the crossing hard (−pop/+unity, hunger borne fairly) or spend down every reserve (−credits/parts, alive and hollow) — but a **complication gated on `laid_in_for_dearth`** rides only for the provisioned ship, the vaults a wiser generation filled sixty years early answering now (+food/+morale), *"the cheapest larder a ship ever fills is the one it fills sixty years early."* Distinct from r5's opportunistic vaults (which help *whenever* a famine happens); this is a *dated, guaranteed* dearth you prepare for or don't. No new schema — round-9 scheduling + round-6 complications + consequence chain, composed. Survival 32→**34**, events 209→**211**. +1 test (charting queues the dearth on its date, laying-in seeds the relief the complication reads, trusting-slack banks nothing, and the payoff never rolls on its own) (159 total).
- 2026-07-22 · campaign skeleton (round 10) · the beat lattice keyed on time, every identity stat, and (r9) mission progress — but the doc's era-texture bullet explicitly names *"homecoming beats,"* and r4 only gave the late era a *biased* pool; there was no **forced** homecoming beat. Added it, and it's the **first beat keyed to a voyage phase** rather than a stat/time/objective: `homecoming_beat_family` config + a `homecoming_beat_fired` flag on the contract + `fire_homecoming_beat`, forced once the charter turns for home (enters its Return leg). The voyage's climactic identity moment — which had era-bias and completion-prose but never a guaranteed beat — now lands as one. Authored 2 return-reckoning legacy_drift beats (gated `phases:["return"]`): the-homeward-face (`min_cultural_drift 0.4` — a drifted crew facing a homeport that expects the founders' faces: wear the founders' face → perform the old ways, +loyalty/stability but a lie/−morale, vs arrive as who the voyage made us → honest/+morale/adaptation/drift, "a reunion built on a mask is a reunion with no one at all") and the-stranger-homeport (home has changed too across the centuries: hold to the remembered home → loyalty but a coming disappointment, vs meet home as it is → two strangers with a shared story, +adaptation/drift). Data-load: none needed (family reuse). The silent full-contract soak clears `homecoming_beat_family` alongside the other forced beats; autoplay resolves it on every real return; all soaks green. Legacy_drift 42→**44**, events 207→**209**. +1 test (fires the year the ship turns for home, not before, exactly once) (158 total).
- 2026-07-22 · subsystems (round 10) · every subsystem effect to date touched *production or decay*; none touched the ship's **identity** — so this pass made the one module that thematically should: the **education/culture archive is the ship's memory of its founders**, and a well-kept one resists the people forgetting who they were. New `voyage_drift.archive_drift_resistance` (0.3): the education_culture module's **knowledge** (how much of the founding is still remembered) scales down the *cultural* drift terms — `cultural_drift` and the `legacy_loyalty` fade run at `1 − archive_drift_resistance · education_knowledge` of their ideology/legacy rate, so a ship that keeps its archive vivid drifts culturally slower, a ship that lets it rot forgets faster. Deliberately precise: the archive resists cultural forgetting but **not physiological adaptation** — the body adapts to the ship whether or not the memory holds, so `adaptation` keeps the full rate while the two culture terms slow. Complements it58 (dominant-faction ideology → drift) from a wholly different lever — *who leads* vs *how well you keep the memory* — both bending how far the voyage becomes someone new. Wired into `apply_voyage_drift`, read before the mutable apply; `#[serde(default)]` 0 keeps old configs inert; the existing drift/legacy/ideology soaks hold (equal archive knowledge isolates their terms), all green. Events unchanged (207). +1 test proving a vivid archive drifts culturally slower than a lost one, yet both adapt identically (157 total).
- 2026-07-22 · factions (round 10) · built the doc's one still-named-undone faction direction — **faction-specific dilemma outcomes** — reaching a fresh system the faction rounds had never touched: the **legacy dilemmas** (§5.5), the game's *uncertain*-choice mechanic (24 dilemmas, each with success/failure branches rolled against a `success_chance`). Extended `DilemmaOption` with `dominant_faction`/`dominant_faction_odds`: while the named people is the largest aboard, its craft (or its resistance) shifts that gamble's odds — composed cleanly into the existing `dilemma_odds` (which already carried the Wanderer combat bonus) and shown *honestly* in the modal (Pillar 3: a backed gamble reads +%, a hindered one −%). Authored 5 exemplars across all three legacies and both signs (structure-before-volume): the augmented **Ascension Circle** back the gene-edit (`approve_edit` +0.15) and the neural lace (`weave_the_lace` +0.15); the maker **Steel Covenant** back breaching a sealed deck (`cut_it_open` +0.15) and keeping an ancient engine running (`keep_the_relic` +0.12); and the arbiter **Meridian Accord** *hinder* stripping a dissenter's standing (`strip_their_standing` −0.15 — the courts will not rubber-stamp summary justice). So *who runs the ship* now bends not just what events surface (r1/r6) or how fast it drifts (r9), but the odds its defining gambles pay off. Data-load check (every dilemma option's faction id is real). `#[serde(default)]` fields keep old data loading. +1 test proving a backed gamble lifts only under its faction (156 total). Events unchanged (207).
- 2026-07-22 · charters (round 10) · the topology space is full and every charter lever is built, but round 9 introduced scheduled beats with a *single* beat per charter — so this pass realized the mechanic fully: the game's first **scripted-narrative charter**, a mission architected around a *sequence* of timed beats — an authored arc, not an emergent one. the_sunset_relief (17th charter): a four-century relief writ to a colony under a sun the astronomers have already buried, its dimming computed and its end dated — the writ is not to save the world but to carry off what one hull can and stand with a people through the hours of their ending. Two `scheduled_beats` form a two-act tragedy on the clock the dead read from the stars: the-rising-tide (yr 180 — the first triage, the coast going under and the holds a fraction of the need: lift the most bodies → influence/unity but a coldness/+drift, vs lift the seed of a people who can rebuild → stability/legacy but the shore of the many left behind) → the-last-evacuation (yr 260 — the final window, the world in its last hours: fill the holds and burn clear → save the certain, live with the shore you couldn't return for, vs stay past the safe hour for one more run → hull/lives/fuel but "no one reachable left behind"). Both beats `scheduled_only` (never roll on their own); the charter also biases its skeleton toward ethics (`beat_families`). No new schema — the round-9 machinery, composed into a hand-authored mission story. Ethics 13→**15**, charters 16→**17**, events 205→**207**. +1 test flying the relief and proving both acts fire, in order, on their years (155 total).
- 2026-07-22 · event families (round 10) · a **chain pass completing two dangling threads** (a scan found 23 consequence tags produced by an outcome but gated on by nothing — loose narrative promises the game never kept). Closed the two best. (1) The **ghost-signal predestination loop**, using round-9 scheduling exactly as the fiction demands: ghost_signal (the ship's own call sign, timestamped for a year not yet arrived) now, on *answer*, `schedule_followup`s the-appointed-signal 30 yr out — so the payoff fires on the very year the signal named. the-appointed-signal (`scheduled_only`): the impossible arithmetic — for the ship to have received that signal, some ship must broadcast it now, on this frequency, and there is only one ship on this frequency — broadcast it and close the loop (the voyage folds into itself → unity/loyalty/+drift, a message that says only *keep going*) vs keep the silence and leave the loop open (a hole in causation the living decline to fill → stability but unease/drift). (2) **deferred_maintenance** — produced by the "defer the fix" outcome of *three* engineering crises (system_failure/hull_fracture/aging_infrastructure) yet never collected — now comes due in the-bill-comes-due (`requires_consequence deferred_maintenance`, `min_generation 4`): the whole deferred ledger falls due in one bad decade — fund the reckoning in full → credits/parts but hull + engineering/life-support restored (the debt ends) vs patch forever → a permanent hull/subsystem penalty and the debt re-seeded (it only ever comes due larger, in someone else's watch). Mystery 14→**15**, engineering 24→**25**, events 203→**205**. +2 tests (answering the ghost queues its appointed year; the bill gates on the deferred ledger) (154 total).
- 2026-07-22 · voice (round 9) · every frequent *tick* log line was already pooled, but the recent subsystem work left two **maintenance-verb** lines flat: the **field-repair** line (`"The {name} is patched back toward working order."`) and the **knowledge-training** line (`"A new cohort trains up on the {name}."`) — both player verbs that fire repeatedly across a voyage (the field-repair sink especially), the last flat subsystem-maintenance tells after r5 pooled the upgrade line. Lifted both into data-driven pools (`flavor.subsystem_repair` 5 variants, `flavor.subsystem_training` 4, `{name}` = the module), wired through `repair_subsystem`/`train_subsystem_knowledge` indexed by the month clock so consecutive repairs read fresh, with the built-in line kept as fallback so the log never blanks. With this, **every frequent log line — tick and player-verb alike — is pooled**. Data-load coverage test (both pools vary + weave `{name}`) + a firing test proving a repair draws from the pool, not the flat fallback. Events unchanged (203). +2 tests (152 total). **Completes the ninth full rotation through all 7 axes.**
- 2026-07-22 · provisioning (round 9) · the shortage set has coupled to factions (approval, ration triage), subsystems (bio-contamination, water), and population (castaways) — but never to the **mission objective** that campaign-skeleton r9 just made a live quantity. New coupling: during a shortage the founders' *task* and the living's *survival* compete. New outcome field `objective_progress_delta` (a signed *fraction of the objective target*, so it scales across a 24-beacon survey and a 40,000-tonne camp alike; applied clamped-≥0 only with a contract under way, wired into `apply_outcome`) — the first time an event can *spend* mission progress, not just accrue it (the it60 objective becomes a resource, not only a pacing clock). Exemplar the-fallow-season (Operation + `food_below 3000`): a starving on-station generation must choose — turn the work crews to survival → +food, but the tally slips −6% (the mission comes home a shade less complete, but crewed), vs hold to the founders' work → the objective untouched but the famine bites (−population, +legacy_loyalty, "the founders' due"). Safe by construction: completion is time-based (objective feeds only the MissionCompletion metric of four), so a slipped tally lowers score but never blocks completion; autoplay takes the food-gaining divert (index 0), which self-limits (food up → no longer `food_below`), all soaks green. Survival 31→**32**, events 202→**203**. +1 test proving divert feeds the ship and slips the objective while hold-to-the-work leaves it exactly untouched (150 total).
- 2026-07-22 · campaign skeleton (round 9) · the beat lattice watched *time* (anniversary, dead-air), every *identity stat* (drift/adaptation up, unity down, morale up), and the it56/57 *scheduled* clocks — but never the one quantity the voyage exists *for*: **its mission's progress**. Added the **objective-progress beat** (`objective_beats` ascending fractions 0.5/1.0 + `objective_beat_family` legacy_drift + `objective_beats_fired` counter + `fire_objective_beat`), forced as the active charter's `objective_fraction` crosses each mark — the first pacing keyed to *the work*, not to time or how the people are changing. Also added the honest gate the content needs: a new `min_objective_fraction` event gate (mirror of `min_morale`, requires an active contract), so milestone content never surfaces before its milestone. Authored 2 legacy_drift beats keyed to the *generational* strangeness of a handed-down purpose: the-work-half-done (`min_objective_fraction 0.5` — a generation that inherited a goal it did not choose reaches the midpoint: rededicate to the founders' charter → loyalty/stability, vs ask whether the purpose is still theirs → morale/adaptation/+drift, "the founders would have called it drift; the living called it growing up"), the-charter-fulfilled (`min_objective_fraction 1.0` — the mission *done* with the long return still ahead: mark it the founders' triumph → loyalty/unity, vs face a ship with no more purpose → adaptation/+drift/−stability, freedom a harder question than orders). Data-load check (fractions ascending, in range, family has events); the two silent full-contract soaks clear `objective_beats` alongside the other forced beats; all playthrough soaks green. Legacy_drift 40→**42**, events 200→**202**. +1 firing test (fires as the objective crosses its first fraction, silent before) (149 total).
- 2026-07-22 · subsystems (round 9) · closed a quiet asymmetry the mature system had carried the whole time: **four modules earn a per-tick bonus** (agriculture→food, education→knowledge transmission, life-support→decay relief, engineering→keystone) but **medical and security never did** — their only upside came from *crew* (the medic, the security chief), not the module — and *no* module's output scaled by **condition**, so keeping one in repair only ever bought the absence of a breakdown, never a positive return. Gave the two freeloading modules condition-scaled effects: the **medical bay** now prevents a fraction of famine losses by its condition (stacking with the serving medic, combined reduction capped at 0.9 so a famine is never painless), and the **security corps** recovers unity by its condition (stacking with the security chief, only below the crew ceiling). The fresh idea beyond mere coverage: these scale by *condition*, not tier — so a rotting tier-3 hospital saves fewer than a pristine one, and upkeep finally pays back continuously rather than only at the breakdown cliff. New `medical_famine_relief_per_condition` (0.3) + `security_unity_recovery_per_condition` (0.006) config; both wired into the year tick's famine and unity-recovery steps. Deterministic; softer famines only help the soaks (more survival), all green. Events unchanged (200). +1 test proving a sound bay/corps outperforms a wrecked one by condition, and security gives nothing above the recovery ceiling (148 total).
- 2026-07-22 · factions (round 9) · to vary from five straight approval-meter iterations, reached for the lever the schema *reserved from the start* — `FactionDef.ideology`, commented "unused mechanically in v1; reserved for the modifiers a later pass will layer on." A **deliberate mechanics step** (the doc's caveat for stat modifiers): the dominant faction's ideology now scales the yearly voyage-drift's identity terms (adaptation/cultural_drift/legacy_loyalty) by `1 + dominant_ideology_scale · ideology` (0.35), so *who holds the majority steers how fast the people drift from the founders* — a tech-embracing Ascension majority (ideology +0.9) leans the ship into becoming someone new ~30% faster, a tradition-bound Keepers/Kin majority (−0.9) holds the founders' line ~30% slower. `apply_voyage_drift` now reads the dominant faction's ideology (via `dominant_faction_id` → `data.factions`), computed before the mutable apply; the scale is kept gentle (`.max(0.0)`) so identity still moves the *same direction* whoever leads — only faster or slower. New `voyage_drift.dominant_ideology_scale` config knob (`#[serde(default)]` 0 = off, so old saves/configs are inert). Deterministic (no RNG); the legacy-scaling and bounded-drift soaks still hold (same dominant faction across both sides isolates the legacy term); all playthrough soaks green. Events unchanged (200). +1 behavioral test proving a change-embracing majority drifts the ship faster than a tradition-bound one, yet both still drift (147 total).
- 2026-07-22 · charters (round 9) · with topology full and destination-pools / beat-bias / objective-kinds all done, gave the charter a capability it never had — a **scripted beat on a known clock**, built on last iteration's timing machinery. New `ContractTemplate.scheduled_beats` (`{template_id, at_year}`, ascending), copied onto the `ActiveContract` at launch (like `beat_families`) with a `scheduled_beats_fired` counter; a new `fire_charter_scheduled_beat` (in the beat chain after the it56 follow-up firer) forces the next beat by id once the voyage has run to its `at_year` — contract-relative, so "year X of *this* voyage" works whether the charter is the first or a post-refit fifth. Reuses it56's `scheduled_only` flag (the beat never rolls on its own) via a shared `force_event_beat` helper. Exemplar: the_sunward_dive (a dive into a dying star) now schedules the-stars-appointed-hour at year 180 — the founders' astronomers computed the star's great convulsion to the season three centuries out, and the whole charter exists to place the ship in its light when it comes: ride it out in close → a once-in-an-age harvest (energy/influence/medical knowledge) at a heavy hull-and-lives cost, vs pull to survivable range → a lesser reading, a crew that lives to carry it home (the living refusing a debt the dead booked). Data-load: every `scheduled_beats` names a real `scheduled_only` event and ascends by year. Deterministic (contract-clocked); `#[serde(default)]` keeps saves loading; the standard soaks don't fly the dive, all green. Science_anomaly 12→**13**, events 199→**200**. +1 test proving the dive fires its scripted beat on its appointed year, not before (146 total).
- 2026-07-22 · event families (round 9) · built the **doc-named structural gap** in the chain system: *"the schema for delayed follow-ups is itself a depth deliverable if missing."* Every chain to date fires *opportunistically* — an outcome records a `requires_consequence` tag and the payoff surfaces whenever the RNG next rolls it past its gates, so a promised reckoning can land at an unpredictable time or, if over-gated, never. Added the **deterministic-timing** counterpart: outcome `schedule_followup { template_id, delay_years }` queues a `ScheduledEvent { template_id, fire_year }` on the sim; a new `fire_scheduled_beat` (first in the beat chain) forces that named event by id — past its gates — once the voyage reaches `fire_year`, so an authored arc pays off *on a clock*. Payoffs carry a new `scheduled_only` flag that short-circuits `passes_gate` (they never leak into a random/beat roll — they fire solely when promised, and only then). Exemplar the-sealed-capsule (`max_generation 2`, a founding-era beat): seal a message to the descendants → schedules the-capsule-opens 50 yr out (`scheduled_only`), the founders' voice reaching hands not yet born who must decide whether to be bound by it (honor → loyalty/unity/−drift, vs thank-and-decide → morale/adaptation/+drift). Data-load: every `schedule_followup` names a real, `scheduled_only` event (typo + leak guard). Deterministic (year-clocked, earliest-due-first, tie-broken by id); `#[serde(default)]` queue keeps saves loading; silent soaks seal nothing (event_chance 0) so the queue stays empty; all soaks green. Events 197→**199**. +1 test proving the capsule opens on its determined year, not before, and never rolls on its own (145 total).
- 2026-07-22 · voice (round 8) · gave a **voice to the silent mechanic** the last four iterations built. The faction approval meter (grievances r-factions8, module neglect r-subsystems8, ration triage r-provisioning8 all feed it) had no player-facing signal at all — a people could sour for generations and the first you'd hear of it was its withdrawal beat. Added data-driven **faction-mood pools** (`flavor.faction_souring` / `flavor.faction_warming`, 5 lines each, `{name}` = the people's log name) surfaced by a new yearly `SimState::announce_faction_moods`: when an aboard people crosses *into* the restless band (approval ≤ 0.3, the withdrawal-danger line) or the devoted band (≥ 0.7), one pooled line fires — and a new `FactionState.mood_band` (−1/0/+1, `#[serde(default)]`) remembers it so a people staying restless is never re-announced, while a recovery-then-relapse announces afresh. Indexed by year (deterministic, no RNG). Data-load coverage test (both pools vary + weave `{name}`). The lines are the *warning* the withdrawal loop needed: you now feel the Keepers go cold generations before they gather to leave, and feel a people warm as you win it back. All frequent log lines are now pooled. Events unchanged (197). +2 tests (pool coverage; a souring people says so once not yearly, a recovery says so once opposite) (144 total). **Completes the eighth full rotation through all 7 axes.**
- 2026-07-22 · provisioning (round 8) · the shortage-gate set is saturated (food/fuel/parts/energy, compounding crises, chains, the owed-favor debt arc, gifts), so drove **depth by coupling** — provisioning into the r8 faction approval loop, from the side the faction rounds never built. New outcome mechanic `faction_approval_smallest` (a signed shift to the *smallest* aboard faction's approval, resolved dynamically at apply-time so a general rationing beat need not hardcode a people — the approval parallel to `apply_faction_loss`'s smallest-target; new `SimState::adjust_smallest_faction_approval`, wired into `apply_outcome`). Exemplar the-thin-table (`food_below 3000`): a famine forces a ration triage — share the shortfall equally → unity/loyalty but the whole ship goes hungry (sours no one), vs spare the ship by cutting the smallest, least-essential deck first → stability but −0.2 approval on that people (and −legacy_loyalty, a ship that spends its own). The coupling completes a chain from a fresh driver: a provisioning shortfall → the smallest people soured → (via the r8 `faction_approval_below` withdrawal) they leave, so *how you ration scarcity*, not only how you answer grievances, decides who stays. Structure-before-volume (mechanic + one exemplar). Autoplay picks the equal share (index 0), so soaks stay content and green; silent soaks never fire it (food-gated). Survival 30→**31**, events 196→**197**. +1 test proving the harsh cut sours precisely the smallest deck and the equal share sours no one (142 total).
- 2026-07-22 · campaign skeleton (round 8) · the beat lattice had every pole but one — rising-stat crossings (drift, adaptation), a falling-stat crisis (unity collapse), periodic (anniversary), a dead-air backstop — yet **every archetype answered only to trouble or drift; a voyage going *well* generated nothing of its own**. Added the crisis beat's positive twin, the **flourish (golden-age) beat**: `flourish_beats` (ascending thresholds on `morale`, 0.8/0.92) + `flourish_beat_family` (comedy) + `flourish_beats_fired` counter + `fire_flourish_beat` slotted into the beat chain right after the crisis firer. The first month a well-stewarded ship's morale climbs past each threshold, a celebratory beat is forced — good stewardship surfaces its own moments, not just decline. Also added the honest gate the golden-age content needs: a new `min_morale` event gate (mirror of `min_cultural_drift`), so a celebration never surfaces on a miserable ship whether forced or rolled. Authored 2 comedy beats gated `min_morale 0.8`: the-golden-watch (`min_generation 3` — a generation that has known only good years names its golden age: enshrine it as a holiday → unity/loyalty/a steadying memory, vs spend the good fortune on something ambitious → adaptation/influence, the plenty made into something), the-flowering (the ship's arts bloom in the fat years: feed the flowering → morale/unity/+drift, a culture of its own to carry home, vs keep hands to the work → stability, a small closed door). Data-load check (flourish thresholds strictly ascending, in range, family has events). Starting morale is 0.7 and voyage-drift only wears it down, so the beat fires only when the *player* lifts a happy ship — and silent soaks (morale only falls) never trip it; all playthrough soaks green. Comedy 13→**15**, events 194→**196**. +1 firing test (fires past the threshold, silent below it) (141 total).
- 2026-07-22 · subsystems (round 8) · married the mature subsystem axis to last iteration's approval meter — the freshest coupling left, and its first ever to reach **factions**. Prior subsystem couplings ran module→module (agriculture→medical, the engineering keystone). This one is module→**people**: each module now has a **tending faction** (`FactionDef.tended_subsystem`, one per people — the makers to the engineering bay, gardeners to agriculture, Ascension to the medical bay, Accord to security, Hearth to the habitat, the Keepers to the culture archive; all 6 modules covered), and a yearly tick (`SimState::apply_subsystem_neglect_sentiment`, after `decay_subsystems`) erodes a tending people's approval while its module sits below `neglect_condition_threshold` (0.4) by `neglect_approval_penalty` (0.02/yr). Deterministic (no RNG), gentle by design — sustained neglect, not one bad year. The point is the three-system chain: neglect a module → its people sour → (via the r8 `faction_approval_below` withdrawal) they leave. New `tended_subsystem` field + two `FactionConfig` knobs; data-load check (every `tended_subsystem` is a real module). Autoplay repairs its modules, so tending factions stay content and every soak stays green; silent soaks never fire the withdrawal (event-gated). Events unchanged (194). +1 test proving a rotted bay sours only the makers, a sound one none (140 total).
- 2026-07-22 · factions (round 8) · the one lever the baseline reserved as a *deliberate mechanics iteration* — **an ongoing faction approval meter**. New `FactionState.approval` (0..1, 0.5 at launch, `#[serde(default)]` back-compat); event outcomes shift it via a new `faction_approval_deltas` field (aboard factions only, clamped, wired into `apply_outcome`), and a new `faction_approval_below` event gate keys grievance/withdrawal beats to a soured people (wired into `passes_gate`). The coupling is the point: approval feeds the schism system built back in r3/r4 — a people slighted past a threshold generates its *own* departure, so *how you treat a faction*, not only how far the voyage has drifted, decides whether it stays. Exemplar loop authored for the First Flame (structure-before-volume — one people now, grow the catalog later): the-flame-petition (the Keepers demand the reactor-shrine's standing — grant it → +approval but the grow-decks run dim, vs hold the line → −approval, a slight remembered) and the-flame-withdrawal (gated `faction_approval_below 0.3` + `min_generation 3`: the Keepers, slighted one time too many, gather to leave — release them → sheds first_flame, vs pay any price → +approval buys them back above the line, goodwill dearer to rebuy than to keep). Data-load faction-ref check extended to both new fields; default 0.5 keeps soaks/determinism untouched (approval only moves on a played choice). Events 192→**194**. +1 test walking the whole loop — slight twice to cross the threshold, then either buy them back or let them go (139 total).
- 2026-07-22 · charters (round 8) · with the 5 topological shapes full and destination-pools (r6) + beat-overrides (r7) done, pulled the charter axis's one untouched lever: **the mission's objective *kind***. `ContractObjective` was **label-only** (nothing branches on it — the real target is the scalar `objective_target`/`objective_unit`), yet the taxonomy had genuine mismatches visible on the charter card: an eight-generation **embassy** (hearthfall_accord) graded as *Rescue*, a dead-titan **salvage tow** (the_long_tow) graded as *Mining*. Added two objective kinds — **Diplomacy** (a residency graded in standing/accords) and **Salvage** (mass stripped from a wreck, no seam worked) — and reclassified the two mislabeled charters so the card finally names the mission's true yield. Backed the embassy kind with content the r6 way: the `inhabited` tag carried embassy charters but had **zero keyed events**, so authored a 3-beat **embassy pool** (diplomacy family, Operation-gated on `inhabited`) reading as consequences of *living among a host people for generations* — the-mixed-generation (a cohort half-raised on the host world petitions to stay: grant leave → +influence but −pop/+drift, vs keep the ship whole → unity/loyalty but the hosts cool), the-hosts-petition (the hosts need what only the ship can give — a reactor, a cure, its archive: give freely → big influence but spends the ship's own education knowledge, vs trade on terms → credits, a cooler accord), the-long-residency (`min_generation 5`: the ship's children now speak the hosts' tongue best — become a bridge people → influence/adaptation/+drift, vs hold apart, a guest to the end → loyalty/unity but homesick). Diplomacy 18→**21**, events 189→**192**. +1 test locking the reclassification + that the embassy pool fires only on an `inhabited` charter's Operation (not its Travel, not a mining camp) (138 total).
- 2026-07-22 · event families (round 8) · **trilemmas** — a fresh event move (prior rounds grew count / added complications / chains, but the set stayed overwhelmingly *binary*: 175 of 189 events had exactly two outcomes, only 3 were three-way, and the engine + UI already handle 3-way). Gave five iconic dilemmas a genuine **third path**, each a *different strategic axis* rather than a milquetoast middle, and each touching a different system: tithe_demand → **offer service, not tribute** (pay in competence — survey/repair for passage: big influence, small fuel/parts, no treasury or hull cost, where pay bleeds credits and run bleeds hull); micrometeoroid_storm → **run the gauntlet on the deflectors** (a subsystem gamble: −0.08 engineering condition, near-clean hull, no fuel/morale cost — and it feeds the keystone-decay spiral); cultural_schism → **recognize both calendars** (a governance third way — federate the decks: +stability/+drift, far less unity loss than letting it run, forecloses reunification); skills_drought → **bring in outside hands** (buy expertise vs grow it vs freeze it in records: fills the gap but −legacy_loyalty/+drift, the founding crew diluted); the_wary_frontier → **detour around their reach** (disengage entirely: −fuel, mixed morale, vs engage-open / engage-guarded). Three-way events 3→**8**. Events unchanged (189) — same count, five dilemmas that now branch three ways. +1 test locking all five resolve as three legal outcomes and that the tithe's service path is materially distinct (no credits spent, influence gained, no hull lost) (137 total). Opens the **eighth rotation**.
- 2026-07-22 · voice (round 7) · closed the **crew-turnover voice symmetry** and killed a raw-id leak in one pass. Voice round 5 pooled the *retirement* stand-down line but left its exact positive twin — the **appointment** line, fired whenever a vacant post is re-crewed (repeatedly across a voyage as officers age out) — as a single hardcoded string that also printed the raw `archetype_id` ("security_chief") where `CrewArchetype.name` carries a human label. Lifted it into a data-driven `flavor.appointment` pool (6 variants, `{name}` + `{post}`, indexed by the officer's crew id so a drydock's re-crewing reads fresh line to line) and, right beside it, the equally-flat **training** line (`"…(skill N)"`) into `flavor.training` (4 variants, `{name}`/`{post}`/`{skill}`). New `line_with_name_post` helper does the two-placeholder substitution; both wired through `crew.rs` `recruit`/`train`, each with an improved built-in fallback that uses the human post name so the log never blanks *and* never leaks an id. Data-load coverage test (both pools have variety, weave name + human post, training surfaces `{skill}`). +1 test (136 total). Events unchanged (189). **Completes the seventh full rotation through all 7 axes.**
- 2026-07-22 · provisioning (round 7) · **made a dangling debt real** — the doc's flagged "an early choice re-fires a consequence decades later," built as a *branching* arc rather than a flat payoff. Round 6's the-provisioners-bargain let a fuel-starved generation take fuel on a stranger's terms, seeding `owed_a_favor` — and nothing ever collected. Now the-debt-called-in (`requires_consequence owed_a_favor`, `min_generation 4`) brings the collectors out of the dark, generations after everyone who signed is dust: honor the founders' word → pay in ore/labor/berths, +unity/legacy_loyalty (a hull that keeps its word across generations) and seeds `kept_a_bargain`, vs disown the debt → keep everything but −unity/legacy_loyalty/+drift and seed `broke_a_bargain`. That renege *branches* into the-marked-hull (`requires_consequence broke_a_bargain`, `min_generation 6`): word travels the trade-dark and a waystation the voyage badly needs shuts its doors — buy the name back by paying the old debt double (credits/ore → reputation restored) vs run the gauntlet of closed ports on the ship's own reserves (fuel/hull/parts, +self-reliance). Plus a fresh **inversion texture** the transactional set lacked: the-open-hand (a world offers full holds as a *gift*, no price named) — a ship that trades for everything has no ready answer for charity: accept → +food/fuel but −legacy_loyalty/+drift (learning it can be given to), vs insist on a fair trade → less, but the founders' proud ledger kept clean. Diplomacy 15→**18**, events 186→**189**. +1 test locking the branching chain (renege marks the hull, honor never does; the mark re-fires only for the oathbreaker) (135 total).
- 2026-07-22 · campaign skeleton (round 7) · a new beat archetype — the **periodic anniversary beat**, where the three existing forced-beat archetypes all key on *stat crossings* (rising drift/adaptation, falling unity). Every `anniversary_years` (100) of the voyage a beat is forced from the anniversary family (legacy_drift), giving the crossing a **commemorative heartbeat** as the founding recedes into ritual. New `anniversary_years`/`anniversary_beat_family` config + `anniversaries_fired` counter + `fire_anniversary_beat` slotted into the beat chain before the dead-air backstop. Authored the commemoration content the cadence is *about*, drifting with the centuries: the-founders-day (`max_generation 8`: keep the solemn rite → unity/loyalty vs let it lighten into a festival → morale/drift) and its late counterpart the-half-remembered-founding (`min_generation 9`: teach the founding again → loyalty + education knowledge vs let the holiday stand on its own, meaning whatever the living decide → unity/morale/drift). Data-load check (anniversary family has events when on); the 340-yr silent-isolation soak clears it alongside the other forced beats; all playthrough soaks green. Events 184→**186**. +1 firing test proving the anniversary fires on its cadence (134 total).
- 2026-07-22 · subsystems (round 7) · a **systemic (tick-level) cross-coupling**, where every prior one lived at the event level: the engineering bay is now the **keystone** it thematically should be — the place the ship mends itself, so its condition scales how fast *every other* module decays each year. New `engineering_decay_swing` config (0.4) → per-year decay multiplier `1 + swing·(0.5 − eng_condition)`, so a bay in top repair (cond 1.0) slows the whole ship's rot ~20%, a failing one (cond 0.0) speeds it ~20%, neutral at 0.5; engineering decays at its own rate (it's the source, not subject). Wired into `decay_subsystems`; the doc's "cross-couplings expressed in data" finally expressed in the *tick math*, not just events — neglect the bay and the whole ship rots faster, a real spiral the player must arrest. +1 test proving a failing bay wears the medical bay faster than a sound one; all playthrough soaks green (the autoplay repairs engineering, so it self-corrects) (133 total). Events unchanged (184).
- 2026-07-22 · factions (round 7) · the doc's last untouched faction direction — **recruitable pool personalities**. Recruiting a people in drydock was mechanically identical for all six (same head count, same generic line); now each brings a signature one-time **`recruit_boon` dowry** (new `RecruitBoon` on `FactionDef`: subsystem lift + population lift + its own arrival line), so *which* people you take aboard matters beyond the number. Authored all 6 by their nature: Steel Covenant → engineering knowledge/condition (the makers' craft), Verdant Kin → agriculture (green thumbs), Meridian Accord → security + stability (courts before quarters), Ascension Circle → medical knowledge (their body-tech), Hearth Union → morale/unity (a common table on every deck), First Flame → legacy_loyalty/unity (a fixed point, a fire that won't go out). `recruit_faction_group` applies the dowry + logs the flavor (generic fallback). This is a deliberate one-time stat modifier, not an ongoing approval meter — module header updated to match. Data-load check (every faction has dowry flavor; its subsystem ids real). +1 test proving recruiting the Covenant lifts the engineering bay and logs their arrival (132 total). Events unchanged (184).
- 2026-07-22 · charters (round 7) · the charter axis's *other* named lever, finally built: **beat overrides** ("charter-specific … beat overrides, data-driven"). New `beat_families` on the charter (schema on `ContractTemplate` + `ActiveContract`, copied at launch): its families ride in *every* seeded beat's draw for the whole voyage, so the charter **shapes the campaign it generates** — the seeded skeleton leans toward the mission's flavor, not just a generic phase mix. Wired into `generate_beats` after the phase/era pools. Biased 4 charters by their nature: hollow_fleet (a derelict-recovery) → mystery+ethics (the search for the lost, the moral weight), tarssen_relief & hearthfall_accord (embassies to a living people) → diplomacy, the_sunward_dive (a science dive) → science_anomaly. Data-load check: every `beat_families` names a real family with events, and at least one charter carries a bias. +1 behavioral test proving a heavy bias visibly dominates the schedule vs the same charter unbiased (131 total). Charters unchanged (16) — the same missions now generate differently-shaped campaigns.
- 2026-07-22 · event families (round 7) · the doc's #1 event move done in earnest — **complications over new one-offs** ("an event that can arrive with 2–3 complications is worth three flat events"). Instead of new templates, enriched the workhorse events with 6 new complications (no schema change, no events added — same 184 templates, many more *distinct situations*): pushed the two iconic engineering crises to the "2–3 complications" ideal — system_failure now reads three ways (a failing bay → grim/costlier, else the **Steel Covenant** taking it in hand → competent/+eng-knowledge, else flat), coolant_breach likewise (**Ascension** cold competence, else a worn bay → dire/+deaths, else flat); and gave two more events their first twist — youth_unrest gains a **self-referential** complication (`requires_consequence banked_resentment`: a *second* restless generation on a ship that suppressed the first hits harder), ghost_signal reads as an omen not a signal on a far-drifted crew (`min_cultural_drift 0.6`). +1 test proving first-match ordering when an event carries two complications (a worn bay wins over the Covenant reaction; the Covenant's shows on a sound ship) (130 total). Events unchanged (184).
- 2026-07-22 · voice (round 6) · killed the last frequent repetition tells — the **recurring-crisis lines**. The famine line (`"Rations ran out. The population diminished by {losses}."`) and the fuel-stall line each fired once *per year the crisis lasted*, reprinting the same string down a whole starving or becalmed stretch. Lifted both into data-driven pools (`flavor.famine` 5 variants with `{losses}` woven in; `flavor.fuel_stall` 4 variants), indexed by year so a lengthening famine reads as a lengthening ordeal; built-in line kept as fallback so the log never blanks. Data-load coverage test (both pools have variety, famine carries `{losses}`) + a firing test that a 6-year famine surfaces ≥2 distinct lines (normalizing the `{losses}` digit-run to match templates). With this, every log line that can fire 3+ times a voyage is now pooled. +1 test (129 total). Events unchanged (184). **Completes the sixth full rotation through all 7 axes.**
- 2026-07-22 · provisioning (round 6) · three fresh *scenario textures* the saturated shortage-set lacked (not more complications): the-tempting-world (a **garden-stop with a bio-contamination risk** — the archetype the doc names, given teeth: land and gorge a living world → +food but a stowaway spore dents *both* agriculture and the medical bay, vs skim it sterile from orbit → less food, clean; `food_below`-gated), the-provisioners-bargain (a **fuel-trader's dilemma** — pay a station's steep price in hard credit vs take the fuel and **owe them a favor**, seeding an `owed_a_favor` debt for a future round; `fuel_below`-gated), the-rationed-water (a **water/reclamation** ration the set never had — strip and deep-clean the fouling reclaimers → power/parts + a dry month vs nurse the ration and bury the few the thin water kills; gated on life-support `condition_below`, coupling provisioning to the habitat plant). Events 181→**184**. +1 test proving the tempting world gates on a real famine and its harvest trades food for a two-subsystem contamination hit (128 total).
- 2026-07-22 · campaign skeleton (round 6) · a new **beat archetype**: the **descending crisis beat** — the mirror of the rising drift/adaptation threshold beats. Those fire as identity stats *climb*; this fires as the ship's cohesion *collapses*: the first month `unity` falls to or below each authored threshold (0.35, 0.2 — **descending**), a beat is forced from the crisis family, so a fracturing ship generates its own reckoning instead of waiting on a random roll. New `crisis_beats`/`crisis_beat_family` config + `crisis_beats_fired` counter + `fire_crisis_beat` slotted into the beat chain before the dead-air backstop; data-load check mirrors the threshold-beat one but asserts *descending* thresholds. Authored 2 cohesion-collapse events for the pool: the-two-ships (the crew divided into two peoples in one hull — force reintegration → unity but resentment, vs recognize two communities under one charter → stability/drift), the-broken-council (the government fractured — consolidate under one hand → stability but loyalty lost, vs let the decks self-rule → morale/drift but unity/education loss). Guarded the silent timeline-isolation soaks (voyage drift lowers unity over centuries, so they now clear `crisis_beats` alongside the other threshold beats). Events 179→**181**. +1 firing test; all playthrough soaks green with the crisis beat live (127 total).
- 2026-07-22 · subsystems (round 6) · a **cross-subsystem cascade chain** — the doc's "cross-couplings expressed in data," done as a *chain across generations* rather than a single two-subsystem delta (and a fresh coupling: engineering→medical, where round 4's was agriculture→medical). the-reactor-creep (engineering `condition_below 0.55` — a graduated failure that fires *earlier* than the-seized-works at 0.3): reline the shielding now → parts/downtime but the bay recovers, vs run the core hot and defer → energy/morale now, seeds `reactor_run_hot`. Its payoff the-radiation-bloom (`requires_consequence reactor_run_hot` + `min_generation 4`) re-fires a generation on as a cancer/stillbirth bloom on the annex decks — throw the wards at it (credits/parts + population loss, reline at a premium) vs seal and abandon the hot decks (bigger population loss + lost engineering space). Engineering neglect literally sickening the descendants, the bill landing in someone else's watch. Events 177→**179**. +1 test proving the bloom needs both the neglect on record *and* a later generation, and that a ship that relined never sees it (126 total).
- 2026-07-22 · factions (round 6) · **faction-colored event reactions** — the doc's first-listed untouched faction direction, built cleanly on round 6's complication mechanic (structure-before-volume paying off). Extended `Complication`'s gates with `requires_dominant_faction` + `requires_factions_aboard`, so a state-gated twist can key on *who runs the ship* — the same crisis now reads and plays differently by faction. Authored 3 exemplars on general crises: micrometeoroid_storm under **First Flame** (the Keepers name the storm a trial of faith → unity/loyalty, grim morale), coolant_breach under **Ascension Circle** (augmented reflexes, no dread → fewer dead but drift, a competence the ship half-distrusts), skills_drought under **Hearth Union** (craft passes hand-to-hand at the common table → unity + education knowledge, less lost than feared). Data-load faction-ref check extended to complication faction gates. +1 test proving a faction-colored complication rides only under its dominant faction and joins the shown text (125 total). Events unchanged (177) — same count, three crises that now play three ways.
- 2026-07-22 · charters (round 6) · with five topological shapes now covering the phase-structure space (inventing a sixth would be the reskin the doc warns against), pivoted to the charter axis's *other* named lever: **destination flavor that colors which event families weight up + charter-specific event pools**. The `stellar_hazard` destination had a charter (coronal_tap) but zero keyed events; gave it a **signature event pool** of 3 Operation-phase beats that read as *consequences of working inside a star's reach*: the-coronal-flare (a flare inbound — dump the grid to shields and lose the work's rhythm vs armor up and take doses/hull), the-radiation-years (the steady bath — rotate crews through the shielded core and crawl vs push the work → medical toll but the nurseries adapt, radiation→adaptation coupling), the-star's-pulse (the dying star's magnetic song floods the founders' arrays — filter it out and run clean vs let the ship keep the star's time → unity+drift, a people marked by a sun). Added a new charter to give the pool a second, distinct home — the_sunward_dive, a *science dive* into a dying star's atmosphere (exploration objective, not coronal_tap's plasma mining) — so the destination has variety, not a reskin. Charters 15→**16**, events 174→**177**. +1 test proving the pool fires only on a stellar_hazard charter's Operation (not its Travel, not a starless survey) (124 total).
- 2026-07-22 · event families (round 6) · landed the doc's long-deferred #1 event move — **complications** ("an event that can arrive with 2–3 complications is worth three flat events"). New `Complication` schema on `EventTemplate`: a state-gated twist (drift / subsystem `condition_below` / `requires_consequence` / `food_below`) that, when its gates hold, appends a `description_add` sentence to the event *and* — whichever outcome the player takes — lands extra deltas + a log line on top. Chose the contained form that needs **no outcome-list or PendingEvent change**: the sim is paused while an event blocks, so `active_complication` returns the same answer at present-time (`shown_description`, wired into the modal) and apply-time (snapshotted in `apply_outcome` from the pre-outcome state, so the outcome's own deltas can't move the gate). Authored 3 exemplars on high-frequency events: system_failure gains a compounding twist when the engineering bay is itself failing (extra hull + bay damage), population_growth strains harder on a thin larder (`food_below 3000`), cultural_schism reads differently on a far-drifted ship (`min_cultural_drift 0.6`). Data-load subsystem-ref + consequence-provenance checks extended to complication fields. +1 test proving a complication rides only when its state gate holds, shows in the description, and lands an extra toll the flat event doesn't (123 total). Events unchanged (174) — same count, more distinct situations.
- 2026-07-22 · voice (round 5) · killed the last frequent generational-turnover repetition tell and enriched the death-ending. The **crew stand-down** line (`"{name} stood down from their post."`) fired once *per retiring holder* — several a generation, dozens a voyage, all identical (exactly the tell voice round 1 killed for obituaries). Lifted it into a data-driven `flavor.retirement` pool (6 variants, `{name}`, indexed by holder so consecutive stand-downs in one generation differ). Also enriched the campaign's tragic ending: the **extinction** line (`"The dynasty has no heirs. The line ends here."`) — a flat single string on the death-ending, the counterpart to round 4's homecoming prose — became a data-driven `flavor.extinction` pool (3 variants, indexed by generation). Both keep the built-in line as a fallback so the log never blanks. Data-load coverage test (retirement has variety + weaves the name, extinction non-empty). +1 test (122 total). Events unchanged (174). **Completes the fifth full rotation through all 7 axes.**
- 2026-07-22 · provisioning (round 5) · a **foresight/insurance chain** — the positive mirror of the existing shortcut chains (seed_corn→barren_decks *punishes* a shortcut; this *rewards* prudence). the-deep-stores (early-voyage only, gated `max_generation 3` + travel — the round-4 era-ceiling put to fresh use): spend the ship's early slack on sealed emergency vaults → seeds `deep_stores_built`, vs travel light → morale/unity. Its payoff the-vaults-answer gates on **both** `requires_consequence deep_stores_built` **and** a live famine (`food_below 2500`) + `min_generation 4` — foresight that sits idle for generations until the year it is everything: open the vaults → big food + morale, vs ration the reserve → less food but stability, keeping some sealed against a worse year. Plus a fuel-conservation branch the set lacked, the-slingshot-gambit (`fuel_below 0.25`, travel): thread a gravity well for free velocity → +fuel but hull damage + lives lost, vs burn it safe → spend the thin reserve, everyone lives. Events 171→**174** (survival now the largest family at 27). +1 test proving the payoff needs *both* the investment and the famine, and that opening the vaults feeds the ship (121 total).
- 2026-07-22 · campaign skeleton (round 5) · **dead-air backstop** — the doc's last unaddressed axis-5 item ("long stretches with nothing eventful = a bug in content coverage, not a mercy"). Voice round 2 gave the quiet *ambient flavor* but never made anything happen; now the skeleton **guarantees a beat** once more than `dead_air_years` (30) pass with no event. New `fire_dead_air_beat` slots into the beat chain after the due/drift/adaptation firers and before the reactive roll: it draws a family from a data-driven `dead_air_pool` (survival/engineering/mystery/legacy_drift) via the state RNG (deterministic) and forces a beat via the shared `force_family_beat`, resetting the event clock even if an over-gated moment finds no candidate (so it waits a full interval, never hammers monthly). Config `dead_air_years`/`dead_air_pool` + data-load check (pool non-empty + families have events when enabled). Layers cleanly under ambient flavor (quiet at 8 yr, forced beat at 30). +1 firing test (fires past the gap, silent before it); two timeline-isolation tests switch the backstop off alongside the threshold beats. All playthrough soaks stay green with it live. Events unchanged (171). 120 tests.
- 2026-07-22 · subsystems (round 5) · **tier-specific upgrade flavor** — the doc's one untouched subsystem direction. Every drydock upgrade (6 modules × 3 tiers, up to 18 a run) logged the *same* hardcoded line, `"The {name} is rebuilt stronger."` — a flat repetition tell on a moment that should feel like progress. Added an optional `flavor` string per `SubsystemTier` (schema, `#[serde(default)]`) and made `upgrade_subsystem` log it (generic line preserved as fallback so the log never blanks). Authored all **18 lines**, each in the module's own voice and *escalating* across tiers (agriculture: brighter lamps → a second ring of decks → closed-loop abundance; engineering: a machine shop → a fabrication hall → foundry+mill+clean-room; etc.). Data-load asserts every tier carries flavor; the JSON round-trip also cleaned up pre-existing em-dash mojibake in the module descriptions. +1 test proving an upgrade logs its tier line (not the generic one) and tiers differ from each other (119 total). Events unchanged (171).
- 2026-07-22 · factions (round 5) · **event-driven assimilation/merger** — the doc's named "assimilation beats," and the *union* counterpart to round 4's schism. New outcome `faction_merge_id` + `apply_faction_merge` folds a named faction into the largest other aboard: unlike `faction_loss_id` (a schism that *sheds* a people and drops the head count), a merger keeps every soul aboard — only the separate identity dissolves. Authored 2 merger beats gated on both factions aboard + drift + generation: the-green-hearth (Hearth Union × Verdant Kin, the table and the garden grown into one — bless the union → unity/morale/drift + Kin fold in, vs keep the two founding names → stability/loyalty), the-technocrats-compact (Meridian Accord × Steel Covenant fuse courts+forges into one administration — form the compact → stability/drift + engineering knowledge, a concentration of power the founders split on purpose, vs keep the powers apart → unity/checks). Data-load faction-ref check extended to `faction_merge_id`. Events 169→**171**. +1 test proving a merger dissolves the named faction yet keeps the head count (where a schism drops it) (118 total).
- 2026-07-22 · charters (round 5) · a fifth charter **shape**: the outbound-dominant crossing (the_far_crossing), `[travel 300, operation 70, return 110]` of 480 yr — the mirror of round 4's return-dominant tow, where the *getting there* is the whole trial: a three-century one-way burn to a target at the edge of the reachable dark, a brief survey, then a charted return. No one who casts off lives to see arrival. New `outbound_push` tag + 3 Travel-phase-keyed events giving the long push its signature texture: the-far-horizon (the crossing generation, all cost and no arrival — honor the crossing → loyalty/stability vs let the crossing itself become the point → morale/unity but drift), the-turnback-petition (a movement to abandon the too-distant target and go home with nothing — press on → loyalty but morale/unity cost, vs **turn back via `force_return`** on the *outbound* leg, forfeiting the charter for the descendants' sake), the-aging-outbound (three centuries with no port to refit at — cannibalize redundancy to keep the core sound → spare-parts cost + engineering/life-support condition, vs hoard reserves and arrive threadbare → engineering/security condition rot). Charters 14→**15**, events 166→**169**. +1 soak test flying the outbound-dominant voyage + asserting its topology (117 total).
- 2026-07-22 · event families (round 5) · **hit the "every family ≥12" bar** and did it via the doc's flagged depth move — *chains*, not one-offs. Two of the three new events are **payoffs that complete formerly-dangling consequences** (events that recorded a `long_term_consequences` tag nothing ever paid off): the-answer (science_anomaly, gated `requires_consequence the_dark_body` — generations after the founders pinged an ancient dark mass, the ping returns as an answer: reply and learn what asks back → influence/drift + morale unease, vs strike the frequency and never hear it → unity/stability), the-rule-comes-due (ethics, gated `cold_triage_rule` — a deck fire forces the cold skilled-first triage rule the ship wrote generations ago to actually be applied: honor the law → stability/adaptation but morale/unity, vs break your own law → morale/unity but stability/loyalty and a strained ward). Plus one standalone science twist, the-mirror (the array returns a perfect echo of the ship dead ahead: hail the gravitational-lens ghost → energy/influence + unease, vs log it and pass → stability). science_anomaly 10→**12**, ethics 11→**12**. Events 163→**166**. +1 test locking the triage chain (gates on the setup consequence, choices diverge) (116 total).
- 2026-07-22 · voice (round 4) · **homecoming prose keyed by success level** — the campaign's emotional climax (a 300–600-yr voyage ending) got a single flat line, `"Contract concluded: … — {outcome} (score …)"`, the same for a triumphant full return and a limp-home Pyrrhic. Added a data-driven `flavor.homecoming` pool keyed by the four `SuccessLevel`s (complete/partial/pyrrhic/failure), 2 variants each, `{years}`/`{generation}` woven in, indexed by generation (deterministic — a seed replays the line). The completion path now leads with the level-specific prose then the compact record line; a missing pool just omits the prose (log never blanks), mirroring the phase-lines fallback. Data-load coverage test (all four levels authored + substitution) + helper unit test. Events unchanged (163). Voice pass, 115 tests. **Completes the fourth full rotation through all 7 axes.**
- 2026-07-22 · provisioning (round 4) · content pass adding provisioning *textures the set lacked* (all four resource shortage-gates were mature, but the choices were monotone): the-castaways (a **population-gain** opportunity — every prior provisioning beat *shed* people; this one can take castaways aboard, trading berths + ongoing food for +80 crew and mercy, vs take only their stores and bank food — the new provisioning→population coupling), the-windfall-harvest (a **surplus** opportunity, the counterpart to all the shortages — a garden-charter glut too big to store: put it up for the lean century → +food and agriculture *knowledge* relearned, vs sell the surplus forward → food + credits now; gated `requires_charter_tag garden` + operation), the-recyclers-gambit (a parts shortage that trades **irreplaceable ship-culture for raw stock** — melt the chapel/museum/memorial arboretum → +55 spare parts but morale/drift, vs stay short and whole), the-ration-market (rationing breeds a black market: break it → unity at morale's expense, vs license and tax it → stability + credits but a ship that learns to price its hunger). Events 159→**163**. +1 test locking the castaways population-gain/food-cost divergence (114 total).
- 2026-07-22 · campaign skeleton (round 4) · **deep-middle era + era ceilings**. Completed the three-era texture the doc named (founding / mid / homecoming): era layering had `early_pool`/`late_pool` tinting the first/last 20% but the long middle drew only phase pools. Added a `mid_pool` (legacy_drift/mystery/comedy) layered for beats in `[early_fraction, late_fraction)` — the era no living soul remembers beginning and none expects to end. Paired it with a genuinely new pacing tool the schema lacked: **era ceilings** `max_year`/`max_generation` on events (the mirror of the `min_*` gates, 0 = ungated, wired into `passes_gate`), so era content can *bow out* before homecoming instead of leaking into it. Authored 3 deep-middle events gated `min_generation 6`..`max_generation 13`: the-only-world (a census finds no one aboard knew launch nor expects arrival — teach the voyage as a passage → stability/loyalty vs teach the ship as home → morale/unity but drift), the-equidistant-dark (mid-crossing, the sky stops seeming to move — burn beacons into the void → energy/morale vs run dark and silent → thrift/unity), the-false-noon (a holiday for the mathematical midpoint — a blowout → morale/drift vs a working toast → stability). Data-load era test now requires a non-empty mid_pool; skeleton valid-set + generate_beats updated. Events 156→**159**. +1 test locking the era-ceiling min/in/max window (113 total).
- 2026-07-22 · subsystems (round 4) · **completed condition-breakdown coverage**: round 3 gave physical-failure beats only to life-support, security, and engineering; this pass covers the other three so every module now has *both* a knowledge crisis and a condition breakdown — the-failing-ward (medical bay rotted past patching: strip the ship to rebuild the theatre vs fall back to triage medicine and bury the difference), the-broken-beds (hydroponics seizing: rebuild the beds vs fall back to soil farming), the-crumbling-archive (the record substrate itself dying, distinct from people forgetting: mount a great hand-copying vs let the deep archive fade — dents education knowledge + drift, seeds a `the_lost_archive` consequence). The-broken-beds' soil fall-back is the doc's canonical **cross-coupling expressed in data** — the lean years dent BOTH agriculture and the medical bay (malnutrition load) via a two-subsystem `subsystem_deltas`. Added a data-load coverage assertion mirroring the knowledge one: every subsystem must have a `condition_below` breakdown event. Events 153→**156**. +1 test locking the agriculture→medical cross-coupling (112 total).
- 2026-07-22 · factions (round 4) · **friction→fracture** — a new combined coupling: an inter-faction quarrel that gates on BOTH factions aboard (`requires_factions_aboard`) *and* whose "let it break" outcome sheds the named one via `faction_loss_id` in the same event (no prior event did both — schisms gated on one faction + solo drift; friction events only shifted stats). Gave the 3 factions that had a signature but no fracture path (Steel Covenant, Meridian Accord, Hearth Union) one each, so every faction can now leave: the-forge-and-the-garden (Covenant×Kin over a deck → the machinists found a foundry-world, taking engineering knowledge with them), the-courts-and-the-creed (Accord×Flame → the arbiters depart a ship ruled by creed, security arbitration lost), the-cold-table (Hearth×Ascension → the Hearth withdraws to a world where all still eat at the table, morale/warmth lost). Plus a reconciliation-texture friction, the-fire-and-the-field (Flame×Kin over reactor power: dim the eternal shrine-burn for the grow-decks vs keep it lit and starve the gardens — friction→agriculture, no schism). Friction pairs 3→6. Events 149→**153**. +1 test proving the fracture sheds the *named* faction even as the largest aboard and carries its subsystem penalty (111 total).
- 2026-07-22 · event families (round 4) · closed the last parity gap: brought the two thinnest families, **mystery** (9→12) and **comedy** (9→12), to the doc's 12+ bar, so every family is now ≥10 and 8 of 10 at ≥12. All 6 are century-aware (gated on generation/drift/knowledge decay) with genuine two-way choices and system couplings, not flavor one-offs: the-sealed-deck (breach a founder-welded deck for salvage vs keep it a shared mystery — engineering condition + drift), the-second-log (publish a contradicting founding record vs bury it — education knowledge + drift, seeds a `buried_second_log` consequence for a future chain), the-wandering-mind (obey the old nav archive and lose understanding vs rebuild it by hand — knowledge-crisis gate, opposite subsystem-knowledge swings), the-festival-war (rival festivals vs a fused invented holiday), the-reconstructed-feast (canonize a delicious fake vs honor the archive — education knowledge), the-office-of-lost-things (fund an absurd bureau that actually mends the shops vs disband it — engineering condition + spare parts). Events 140→**146**. +1 test locking the wandering-mind divergent-choice coupling (109 total).

### Mechanics passes (2026-07-23, outside the content-axis rotation — see "Established mechanics")

- 2026-07-23 · mechanics (real-time loop) · replaced the manual-advance frame with a **real-time auto-advancing voyage** (GDD Pillar 4 superseded). `SpeedStep` → `GameSpeed { Paused, X1, X2, X3 }`; `tick::advance` → `advance_months(sim, data, n)`; a per-frame accumulator in `game.rs` drives one month per `real_time.seconds_per_month` while under way, frozen while docked; Space-to-advance removed. Blocking events **and** dilemmas auto-resolve to a random available option after `real_time.decision_timeout_secs` (30 s). Event `population_delta.count` is now RNG-rolled within a ±`real_time.impact_variance` band shown in the modal. Completion docks to a new `Screen::Drydock`. Tabs split by state (`Screen::tabs`); SHIP tab is shipyard-docked / status-underway. Determinism relaxed: hand-stepped `advance_*` still replays; a played run does not. 220 tests.
- 2026-07-23 · mechanics (UX) · fixed the event modal header (countdown right-aligned, title clears the divider); **seeded the global RNG from the wall clock at startup** so new games actually vary (they were identical every launch), with `game_config.fixed_seed` to force a reproducible run for testing; menu flow reworked to Boot → title/main menu (Continue / New Game / Settings / Exit) → new-game picker (`MenuPhase`). 220 tests.
- 2026-07-23 · mechanics (voice/legibility) · the fuel gauge's burn/scoop sawtooth read as an unexplained twitch; added a **periodic in-world provisioning report** (`flavor.fuel_gain` + `fuel_report_gap_years`) that narrates the drive's actual haul — self-throttling via a `fuel_scooped_accum` that only grows while the tank has room, so it speaks on a crossing and stays silent on a full tank on-station. The same pooled pattern can later voice other silent stat movers. 221 tests.
- 2026-07-23 · mechanics (mortality) · **characters age and die continuously** (GDD §5.3 rewritten). New `simulation/mortality.rs`: yearly "Founding Day" aging for all; a monthly age-scaled death roll (`mortality` config block — accident floor + doubling past onset 50, certain at `member_max_age`); succession fires on a vacated seat (not a 25-yr batch); a yearly birth model renews the line toward `dynasty_target_size` (the required counterweight — continuous death alone guarantees extinction); a heavy event loss may claim a named character (`event_claim`). Aging/death removed from `succession`/`crew` (now renewal + succession helpers only). New pools `flavor.crew_death`. 224 tests (soak proving the line ages, turns over, and survives).
- 2026-07-23 · campaign skeleton (round 18) · **the succession beat** — the beat lattice's first trigger keyed to the new continuous-mortality system (real-time loop), and the first keyed to the *death of a named person*. The month a **sitting leader dies in office** (not a planned retirement handoff), `mortality::monthly_tick` sets `TickReport.leader_died` and `fire_succession_beat` (first in the beat chain, before the scheduled beats) forces a beat from `succession_beat_family` (legacy_drift) — so a captain lost mid-voyage forces the ship to *reckon with an untried command* rather than the death passing as one log line. Per-month flag, consumed on fire, voyage-only, superseded by extinction. Authored 2 legacy_drift events (`min_generation 2`, coupled to the it105 reputation traits): **the untried command** (a never-tested heir takes the chair — hold the old course (+unity/loyalty, +`resolve`), set a new course (+adaptation/drift, −loyalty, −`resolve`), or rule by council until they're ready (+stability, −influence)) and **the empty chair** (how a ship buries a captain is who it says it is — full ship's mourning (+unity/loyalty, +`mercy`) vs keep the watch (+adaptation, −morale/loyalty)). Events 244→**246** (legacy_drift 54→56). Data-load guards the family has events. +1 test proving a death-in-office is flagged and a routine survival is not (225 total). Determinism of the tick preserved.
- 2026-07-23 · subsystems (round 18) · **the medical bay slows death** — the first subsystem coupling to the real-time-loop mortality system (it131), giving the infirmary its most fundamental job at last. New `medical_mortality_relief_per_condition` (0.35) + `subsystems::medical_mortality_relief` (condition × factor, clamped <1); `mortality::monthly_tick` reads it once and scales every character's *pre-cap* death roll by `1 − relief`, so a bay in top repair (condition 1.0) cuts each monthly death chance by ~35% and a rotted one leaves the aging to their age — upkeep on the ward now literally buys the crew years. Never applies at the hard age cap (a 90-year-old still dies; no immortality). Closes a legible loop: the it62 engineering keystone speeds the bay's decay → the bay decays → the crew dies faster → the player feels it and mends the bay. One authored biology_medical event drives the choice home: **the elders' ward** (`condition_below medical_bay 0.4`, `min_generation 3`) — rebuild the ward (strip the ship's parts/hands, +0.45 bay condition → the reaper's odds fall) vs comfort-not-cure (keep the stores, ~35 souls lost early, −morale/loyalty, −`mercy`), so the choice reaches straight into the mortality mechanic the round just built. Events 246→**247** (biology_medical 18→19). +1 test proving a sound bay eases an elder's odds and can never grant immortality (226 total). Determinism of the tick preserved.
- 2026-07-23 · provisioning (round 18) · **a long hunger wears the body, not just the spirit** — the provisioning axis's coupling to the real-time-loop mortality system, completing the it89/it116 chronic-scarcity picture: where round 17's `chronic_hunger_morale_drain` wears the crew's *spirits* each year the lean holds, this wears their *bodies*. New `chronic_hunger_death_bonus` (0.003) added to every character's monthly death chance while `lean_food_years ≥ chronic_hunger_years`, on top of the age curve — so a grinding multi-year famine thins the roster, the old and weak first, and a lean *generation* is measurably deadlier than a lean winter. Composes cleanly with the it131 infirmary relief (a well-kept bay still eases the toll) and the hard age cap (nothing cheats it); a flat add, gentle by design (the slow toll of long want, not a famine's single blow). One authored survival event dramatizes it: **the winnowing** (`min_lean_food_years 8`) — spend the last reserves to carry the weakest through (−3000 food, +`mercy`, +morale/unity) vs let the hunger take whom it would (~60 lost, −morale/loyalty, −`mercy`), the choice reaching straight into the toll the round just built (and its heavy loss can trip the it130 `event_claim`, taking a named soul with the anonymous many). Events 247→**248** (survival 45→46). +1 deterministic test proving a fed ship's young are safe and a long-lean ship's are not (227 total). Determinism of the tick preserved.
- 2026-07-23 · factions (round 19) · **goodwill you can draw on, not only a fault line to avoid** — closed a long-standing asymmetry: the approval meter had a *grievance* gate (`faction_approval_below`, it soured people generate withdrawal beats) but no *positive* pole, so treating a people well only ever meant it *didn't leave*, never that it *gave back*. New `faction_approval_above` gate (schema `FactionApprovalFloor {id, at_least}`, mirrored into `passes_gate` and the data-load faction-id check): an event surfaces only while the named people is aboard *and delighted* (approval ≥ threshold). Two exemplar gift beats it unlocks: **the Hearth's feast** (comedy — a glad Hearth Union throws open the long tables: a ship-wide +morale/+unity for the cost of the grow-stores, or a modest observance that holds them) and **the volunteered works** (engineering — a proud Steel Covenant rebuilds the engineering bay on its own time for only the materials: +0.5 bay condition no paid contract could buy, or decline and spurn the gift, −their approval). Both couple factions↔events on the positive side (and the works couples factions↔subsystems), so a well-kept coalition now materially *pays back* — the well the it100 approval→unity coupling only ever drained. Events 248→**250** (comedy 15→16, engineering 28→29). Data-load guards the new gate's faction ids. +1 test proving the floor gate opens only to a delighted people (228 total). Determinism preserved.
- 2026-07-23 · charters (round 19) · **a mission can leave its people glad it was flown** — closed the loop the previous round opened: round 19's `faction_approval_above` gift beats need a people *delighted*, but the mission cycle (charters flown *because* a people is aboard, it112) had no way to earn that delight — a completed charter shaped the ship's subsystems (it104), character (it115), and pay, but never a people's goodwill. New `completion_reward.faction_approval_deltas` (schema on `CompletionReward`, applied in `apply_completion_reward` to aboard factions only, folded into `is_none`), so seeing a charter through lifts the served peoples' approval. Wired to three thematic charters: **the Seedbearers' Writ** → Verdant Kin (the greening that is their dream, and the writ already *requires* them aboard, so it always lands), **the Karst Works** → Steel Covenant (a makers' great works, feeding this-round's the-volunteered-works gift beat), **the Sanctuary Run** → Hearth Union (the communal people who bless carrying refugees home, feeding the-hearths-feast). The result is a full three-system loop — charter → a delighted people → its round-19 gift beat — so courting a coalition through the missions you fly it now pays back in the events it later offers. Data-load guards the reward's faction ids. No new events (a mechanic + 3 charter rewards); events 250. +1 test proving a completed sanctuary run leaves the aboard Hearth gladder (229 total). Determinism preserved.
- 2026-07-23 · event families (round 20) · **worn-ship complications** — the doc's "complications over one-offs" preference, aimed at the new lived-states the real-time-loop rounds created: a crisis should read and bite worse on a ship the mortality/famine systems have already ground down. Three existing crisis events gained a state-gated twist (no new events; the higher-value depth of *the same event playing several ways*): **crop_blight** + `min_lean_food_years 8` (a blight on a ship years into a hunger is not a setback but a sentence — no reserve to eat while the replant grows: −2000 food, ~30 lost), **scrubber_failure** + `max_population 400` (a skeleton crew has too few hands to work every failing scrubber — the unmanned decks go stale: −0.05 life-support, ~20 lost), **quiet_fever** + `condition_below medical_bay 0.35` (a fever the it131 failing infirmary can no longer break turns killer — it takes the frail and old: ~25 lost). Each rides only when its state holds (the first-match round-6 behaviour), its `description_add` shows the twist before the choice, and its toll lands on whichever outcome is taken — so the ship's worn condition, not just the roll, decides how hard the crisis bites. Reuses the round-15 lived-state complication gates; no schema change. Events 250 (three now play two ways each). +1 test proving the fever twist rides only on a broken ward (230 total). Determinism preserved.
- 2026-07-23 · voice (round 18) · **farewells that don't repeat** — the voice axis's answer to a debt the real-time-loop rounds ran up: continuous mortality made *death* and *leader-succession* fire dozens of times a voyage (they were once-a-generation events, keyed to the 25-year tick), so their flavor pools — 6 obituary and 6 succession lines — now cycled five-plus times a crossing and read as a form letter, exactly the "repetition tell" the quality bar forbids. Doubled the two most-hit pools and deepened the others: **obituary 6→14**, **succession 6→14**, **coming_of_age 5→10**, **crew_death 4→9**, all in the established terse generational-ship voice (a name struck from the register, a bunk reassigned by evening, an heir a generation early, a craft mastered and gone with the hand that held it), indexed by member/crew id so a long voyage's many farewells rarely echo. Pure JSON, no schema, no new events; the data-load guards (obituary/succession carry a `{name}` line) still hold. No new test (content pass); 230 total. Determinism preserved.
- 2026-07-24 · campaign skeleton (round 19) · **the long-reign beat** — round 2 of the rotation opens by giving the it142 succession beat its hopeful mirror. Continuous mortality (it127) takes most leaders within a few decades, which quietly made the *opposite* case rare and precious: a captain who endures, an era under one hand. New `Dynasty.leader_reign_years` (counted up each Founding Day in `annual_aging`, reset to 0 by `install_successor`) + `long_reign_marked` (fires once per reign) + config `long_reign_years` 35 / `long_reign_beat_family` (legacy_drift) + `fire_long_reign_beat` (second in the beat chain, after succession): once a sitting leader has held the chair 35 years, a beat is forced — leadership now has a beat at *both* poles, the captain lost too soon (it142) and the captain who would not leave. Two authored legacy_drift events (`min_generation 2`/`3`): **the long reign** (a generation that has known no other captain — honor the continuity (+stability/loyalty, +`resolve`) vs make room for the young (+adaptation, −stability)) and **the only captain they know** (the reign is old and no one under forty served another — prepare the succession now (+stability) vs let the good years run (+morale, seeds `unprepared_succession` for a future chain)). Data-load guards the family has events; the isolated-timeline soaks clear the new beat like the others. Events 250→**252** (legacy_drift 56→58). +1 test proving an enduring reign is marked once and a handoff re-arms it (231 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 19) · **the home raises the next generation, or fails to** — the subsystems axis's coupling to the real-time-loop *birth model*: the `life_support_habitat` already moved morale (it73), but nothing tied it to the dynasty renewal (it133) that keeps the line alive against continuous mortality — though the habitat is exactly where families are raised. New `habitat_renewal_penalty` (0.5) + `subsystems::habitat_renewal_factor` (`1 − penalty·(1 − condition)`, penalty-below-full, clamped >0), which scales the yearly birth chance in `annual_aging`: a sound home brings the young up on schedule, a failing one (cramped, cold, patched) raises fewer, and a fully-collapsed one halves the renewal. This closes a real neglect *spiral* — let the habitat rot and the ship loses its spirits (the morale swing) **and** its children (this), the dwindling line inching toward the extinction the mortality system made possible. One authored biology_medical event drives the choice: **the cold nurseries** (`condition_below life_support_habitat 0.4`, `min_generation 2`) — rebuild the living decks (strip stores/labor, +0.4 habitat condition → the cohorts come up full again) vs endure the cold (−morale, seeds `the_cold_years`, and the systemic penalty keeps thinning the young). Events 252→**253** (biology_medical 19→20). +1 test proving a sound home keeps the baseline renewal and a failing one raises fewer but never none (232 total). Determinism of the tick preserved.
- 2026-07-24 · provisioning (round 19) · **a long plenty fills the cradles** — the symmetric completion of the it148 chronic-hunger toll and a companion to the it152 habitat lever: last provisioning round made a sustained lean *kill* (extra monthly death); this makes a sustained plenty *breed*. New `sustained_plenty_birth_bonus` (0.5) multiplies the yearly dynasty renewal (`× 1 + bonus`) in `annual_aging` while `fat_food_years ≥ chronic_hunger_years` (the same "sustained" threshold as the hunger toll, opposite pole) — a well-fed generation raising more of its young to their majority, so the two provisioning poles now bracket the population entirely: hunger thins it, plenty fills it, with the habitat (it152) a second hand on the renewal between. The extinction the mortality system opened is now genuinely a *provisioning* stake at both ends. One authored survival event dramatizes the fat-generation problem: **the full cradles** (`min_fat_food_years 10`) — let them have their good years (soft and loved, +morale/unity, +`mercy`) vs temper them for the dark (raised lean on purpose, +stability/loyalty, −morale). Events 253→**254** (survival 46→47). +1 deterministic test proving sustained plenty fills every open cradle where a lean ship fills fewer (233 total). Determinism of the tick preserved.
- 2026-07-24 · factions (round 20) · **a people who leave take their craft with them** — steered off the mortality/renewal thread of the last three rounds and back to the faction↔subsystem coupling, from the *loss* side. A schism or withdrawal shed a people's *heads* (it via `remove_faction`) but left the ship every scrap of the *expertise* that people carried — as though a generation ship could lose the whole Steel Covenant and keep knowing everything they knew about the engineering bay. New `departed_faction_knowledge_loss` (0.25): on any departure, the module the people tended (`FactionDef.tended_subsystem` — each of the six founding peoples tends a distinct one) loses that much of its living knowledge in `remove_faction`, clamped at 0, with a log line for the lost craft. So mistreating a people into leaving now strips the knowledge it held — feeding the `knowledge_below` crisis events and leaning on the it64 education keystone's slow re-teaching to recover — the exact mirror of the recruit boon's one-time *gift* of craft: a people brings expertise aboard, and takes it away again if driven off. Mechanics-forward (a coupling, no new events; events 254). +1 test proving a departing people's tended module loses its expertise (234 total). Determinism preserved.
- 2026-07-24 · charters (round 20) · **a mission can bring home a piece of kit** — a fresh charter reward lever, the mirror of an event's salvage grant. A completed charter left the ship stats, character, goodwill, and pay, but never *equipment* — though a salvage tow or a derelict recovery is exactly the kind of voyage that ought to come home with a working component. New `completion_reward.grant_component` (schema `Option<String>` on `CompletionReward`, folded into `is_none`, applied in `apply_completion_reward` — pushes the recovered part into `sim.ship.salvage` to be fitted in drydock, mirroring `EventOutcome.grant_component`; data-load-guarded to a real component). Wired to two salvage/rescue writs whose reward was empty: **the Long Tow** recovers an experimental **warp_coil** from the dead titan-hulled colony ship it hauls home, **the Hollow Fleet** cuts a **spinal_railgun** from a derelict escort on the wreck-line — so a salvage run's prize is now tangible capability (a prestige drive, a heavy weapon) waiting on a drydock bold enough to fit it, not only cash. Mechanics + 2 charter rewards, no new events (events 254). +1 test proving a completed Long Tow leaves its warp coil in the hold (235 total). Determinism preserved.
- 2026-07-24 · event families (round 21) · **closing the loops the recent rounds left open** — an audit found two dozen consequence tags *produced by an outcome but consumed by nothing*: choices that seeded a `long_term_consequences` flag which no later event ever paid off, the exact "chain that never lands" the doc's chain-axis exists to prevent. Authored the payoff half for three of the freshest, each `requires_consequence`-gated so it stays out of the pool until the seeding choice is on record: **the unready hour** (payoff of `unprepared_succession` from the it143 long-reign event — a lifelong captain finally gone with no seasoned heir, because the ship chose years ago not to rush the ending: improvise a rocky handoff (−stability) vs rule by council (−loyalty, a break with the single-hand ideal)), **the cold generation** (payoff of `the_cold_years` from the it152 cold-nurseries event — the cohort raised in a failing habitat comes of age hardy, wary, unsentimental: make them the ship's spine (+stability/adaptation/drift) vs give them the warmth they missed (+morale/unity, +`mercy`)), **the toll at the narrows** (payoff of `war_chest` from the it116 full-coffers event — a holdfast demands a toll the fat-years war chest can meet: pay from the chest (−600cr, the prudence spent) vs refuse and run the guns (−hull, +`resolve`, a name for a hull that won't pay to pass)). Three genuine cross-round chains — a choice made one round pays off in another. Events 254→**257** (legacy_drift 58→59, biology_medical 20→21, diplomacy 29→30). +1 test proving a payoff waits for its seeded consequence (236 total). Determinism preserved.
- 2026-07-24 · voice (round 19) · **the last two form letters, pooled** — closing round 2, the voice axis went after the two highest-frequency *hardcoded* log lines left: `Milestone reached: {X}` (several a charter across many charters) and `Council decision required: {X}` — the latter the game's single loudest repetition tell, printed verbatim before *every* blocking decision, dozens a voyage, so the log's most-seen line read like a machine. Both are now data-driven pools (`flavor.milestone` / `flavor.council_summons`, 8 lines each, substituting `{milestone}`/`{title}`), indexed by `sim.log.len()` so consecutive fires never repeat, with a built-in fallback and a data-load guard that every line carries its slot. The council is *summoned* now, the bells calling it late over a hard matter or a thing that cannot be left to a duty officer; a milestone is *logged and certified*, the crews standing down a watch the way the founders did when a thing was truly done — the two lines a player reads most finally sound like the ship keeping its own log. Pure JSON + two call-sites, no new events (events 257). No new test (content pass, guarded at load); 236 total. Determinism preserved. **Completes the second full rotation through all seven axes.**
- 2026-07-24 · campaign skeleton (round 20) · **the line nearly ends** — round 3 opens with the third and final leadership beat, completing the trio the mortality system earned: the captain lost too soon (it142 succession), the captain who would not leave (it143 long-reign), and now **the founding line itself nearly ending** (dynasty-crisis). The one headcount the beat lattice never watched: the depopulation beat (it81) tracks the *population's* fractions, but the ruling *dynasty* — now a thing that can genuinely dwindle to nothing under continuous mortality — had no reckoning of its own. New `dynasty_crisis_size` 3 + `dynasty_crisis_beat_family` + `Dynasty.dynasty_crisis_marked` + `fire_dynasty_crisis_beat` (in the chain after long-reign): when the founding line falls to three, a beat forces the ship to confront the near-end of the family that has led it since dock; fires once, re-arming only when the line is restored to its it133 target. Plus a new honest gate — `EventTemplate.max_dynasty_size` (the dynasty twin of `max_population`) — so the signature event surfaces only on a dwindled line: **the last of the line** (`max_dynasty_size 3`) — bend the whole ship around its last founders (+loyalty/stability, −unity) vs open the succession to any worthy hand, blood or not (+adaptation/drift, −loyalty, +`resolve`, seeds `line_opened`). Events 257→**258** (legacy_drift 59→60). +2 tests: the beat fires once on a dwindled line, and the gate waits for it (238 total). Determinism preserved.
- 2026-07-24 · subsystems (round 20) · **a rotting drive drinks the tank** — steered off the round's dynasty/mortality thread to the one cross-system link the subsystem axis lacked: **fuel**. Every module scaled decay, output, morale, mortality, renewal — but none touched the provisioning stake the fuel narration (it151) made legible, though a well-tuned engineering bay obviously runs the drive efficient and a rotting one wastes reaction mass. New `engineering_fuel_burn_penalty` (0.5) + `subsystems::engineering_fuel_burn_factor` (`1 + penalty·(1 − condition)`, penalty-below-full, floored at 1.0 so a bay never *saves* fuel), which scales the per-travel-month burn in `month_of_contract`: a sound bay burns the baseline, a failing one drinks the tank up to half again as fast. It compounds the it62 keystone into a double bind — neglect the engineering bay and it speeds every module's decay *and* eats your fuel — tying subsystems↔provisioning at last. One authored engineering event drives it: **the rich burn** (`condition_below engineering_bay 0.4`, `phases: travel`) — overhaul the drive (strip for parts, +0.4 condition → clean burn) vs limp on (−fuel, −morale, the waste logged and paid). Events 258→**259** (engineering 29→30). +1 deterministic test proving a wrecked bay burns a travel month's fuel faster than a sound one (239 total). Determinism preserved.
- 2026-07-24 · provisioning (round 20) · **plenty lifts the spirit as hunger wears it** — completed the provisioning→morale symmetry the earlier rounds left half-drawn. Hunger had two systemic tolls (it89 morale drain, it148 death bonus) and plenty had one (it166 birth bonus): so scarcity wore both the crew's spirits *and* its bodies, but abundance only bred, never cheered — though a well-fed generation is plainly a happier one. New `sustained_plenty_morale_lift` (0.004), the exact mirror of `chronic_hunger_morale_drain` on the same sustained threshold: while `fat_food_years ≥ chronic_hunger_years`, the year tick eases morale a little each year, so the two provisioning poles now bracket the crew's *spirits* (hunger drains, plenty lifts) exactly as they already bracket its *numbers* (hunger kills, plenty breeds). A clean 2×2 closed — food → spirit and body, both directions. Mechanics-forward (a scalar coupling, no new events; events 259). +1 deterministic test proving a fat ship ends a year happier than a merely-fed one, all else equal (240 total). Determinism preserved.
- 2026-07-24 · factions (round 21) · **the quiet reads in the voice of whoever runs the ship** — the first factions↔*voice* coupling. The ambient dead-air line (voice r10–14) had grown rich in reading the ship's *condition* — a long hunger, a hollowed crew, a far-drifted people, a flush larder each colored the silence — but in *ordinary* times it fell back to one generic pool, the same plain quiet whether the augmented Ascension or the communal Hearth held the ship, though those two ships' calm are nothing alike. Gave each of the six founding peoples an `ambient` voice (2 authored lines apiece, `#[serde(default)]` on `FactionDef`) and extracted `quiet_ambient_pool` in the year-tick: the *condition* pools still speak first (a real hunger reads as hunger, whoever leads), but failing all of them the ordinary quiet now draws from the *dominant* people's own lines — a Hearth decade smells of cooking and close company, a Steel Covenant decade of hot metal and a shop always running, a Keepers' decade of the founders' log read aloud on the anniversaries. So a long calm stretch — the very silence the ambient system exists to fill — finally sounds like the people living it, closing the gap between *who runs the ship* (the lever behind drift, dilemmas, gates, reputation, demographics) and *how the ship sounds when nothing is happening*. Pure JSON voice + one extracted helper, no schema beyond the defaulted field; data-load guards every people carries ≥2 non-blank ambient lines. Events unchanged (259). +1 test proving two peoples' ordinary quiets read differently while a long hunger still speaks over either (the 5 condition-precedence tests neutralize the new voices to keep testing precedence); 241 total. Determinism of the tick preserved (indexed by year, no RNG).
- 2026-07-24 · charters (round 21) · **the guns you fit in dock decide which missions go well** — the charter↔ship-loadout coupling, closing a loop that had sat open since the drydock was built. The ship's aggregate *combat* rating did exactly one thing in the whole sim — back a Wanderer's defining gamble (`dilemma_odds`) — so the weapons a player bolts on in port never touched a *mission*, though a run through hostile space or a salvage on a scavenger-picked wreck-line plainly ought to reward firepower. New per-charter `objective_combat_scaling` (mirroring the round-14 `objective_subsystem` opt-in): each point of combat adds this fraction to the on-station accrual rate, exactly as the loadout's `speed` already does — but where speed quickens *every* mission, combat quickens *only* a contested one, so it's a niche lever, not a speed clone (a quiet asteroid survey sets 0 and guns are dead weight there). Threaded through `advance_contract` (new `combat` param beside `speed`, computed once from `loadout_stats` at the tick call-site; the ~16 test/autoplay callers pass 0, the established isolation convention) as a fourth accrual multiplier alongside speed, subsystem-condition, and base rate. Wired to the three `hostile_space` writs the field was made for: **the Hollow Fleet** (0.04, the derelict recovery that already comes home with a railgun), **the Cinder Lane warden patrol** (0.05, the most combat-native writ — literally escorting convoys through a lawless lane), and **the Vane Default enforcement contract** (0.04, stripping a settlement that will not give up its assets willingly). So the outfitting decision now feeds forward into mission choice: fit for the fight before you take a contested writ. Data-driven and deterministic (a bounded [0, 0.2] positive accelerator read from the loadout, no RNG); baseline unchanged for the default 0 (a bare-armed or unarmed ship on an unscaled charter accrues exactly as before). Data-load: the scaling is range-checked and a coverage assert requires some charter to reward firepower. Charters 21 / events 259 (fields on 3 charters, no new templates). +1 test proving a heavily armed ship works a contested writ faster while a quiet survey is indifferent to arms; 242 total. **Charters round 21 continues the third rotation.**
- 2026-07-24 · event families (round 22) · **the same crisis reads differently by the name the ship has earned** — the "complications over one-offs" directive aimed at the richest recent system the complication mechanic had never reached: *reputation*. The state-gated twist (r6) could already make a crisis play differently by who *runs* the ship (`requires_dominant_faction`), how far the people have *drifted*, a *shortage*, a *thinned crew* — but never by the ship's cumulative *character*, though reputation gates events (it105), outcomes (it115), and charters (it106). Extended the `Complication` schema with `min_reputation`/`max_reputation` gates (mirroring the outcome/charter gates, wired into `active_complication`, folded into the it105/it16 data-load reputation-provenance check so a complication can't gate on a trait no outcome nudges), then authored three reputation-colored twists on existing events — no new templates, pure internal variety, "worth three flat events": **The Petitioners** gains *drawn by a kind name* (`min mercy 0.62`, targeted at the *turn-away* choice via it116 `applies_to_outcomes`) — the desperate steered for the ship's mercy *by name*, so closing the airlock on them stains worse than refusing a stranger who expected nothing; **The Tithe Ships** gains *a name the tithe ships know* (`max mercy 0.35`) — a feared hull's shakedown crew hangs back and hedges, the ship's cold name doing half its negotiating (reputation as *leverage*, a rare *positive* twist); **The Triage Rule** gains *a rule against our own name* (`min mercy 0.62`) — a crew famous for mercy setting down in writing whom the medics may let die drafts a small betrayal of what they'd believed about themselves. So the ship's *name* now colors the crises it meets, the character-side companion to the faction-colored (who's in charge) twist — reputation reaching into the one event layer it had missed. Data-driven and deterministic (reputation gates read the paused-state character, no RNG; complications carry no reputation *effect*, only morale/unity strain, so a gated twist can't feed its own gate). Events 259 (three now play two ways each; no new templates). +1 test proving the merciful-name twist rides only on a ship past the mercy threshold and stays out at a neutral name; 243 total. Determinism preserved. (Third rotation's event-families slot — taken out of the usual lead position, since factions and charters ran first this session.)
- 2026-07-24 · voice (round 20) · **the ship remarks when it is forgetting why it flies** — gave the game's *central theme* its own voice. The ship had band-crossing voices for its spirits (it103 morale), its political climate (it104 polity), its name (it111 reputation), and its institutions (it134 governance) — four ship-level stat-voices — but not for `legacy_loyalty`, the crew's *devotion to the founders' mission*, though "a generation ship slowly forgetting why it set out" is the premise the whole game rests on. Added the **loyalty voice** (`announce_loyalty_mood`, an exact mirror of the it134 governance voice): new `loyalty_guttering`/`loyalty_bright` pools (4 lines each) + `loyalty_voice_high` 0.72 / `_low` 0.40 + a `SimState.loyalty_voice_band` tracker. Announced right after the year's `apply_voyage_drift` — which is *itself* what erodes loyalty — so the founders' fire is narrated fading *as it fades*: when loyalty crosses into a guttering band the decks remark the charter slipping from a purpose to a story the young no longer feel, and when a generation takes the dream up afresh (loyalty flares past the high line) they say so too. It's the identity-side companion to the four existing voices and distinct from all of them — a ship can be high-spirited (morale), fairly-run (polity), well-named (reputation), and soundly-governed (stability) yet quietly *losing the plot of its own voyage*, which is its own dimension and the one that matters most to the premise. The launch band (a founding crew's moderate 0.6 loyalty) is recorded not announced (mirroring the it103/it134 baseline record), so a fresh ship is silent until it genuinely crosses. Pure voice — being an announcement, no pending event, so it needs *no soak guard* (like the it104/it111/it134 voices the silent soaks' loyalty drift narrates itself harmlessly). Deterministic (reads loyalty, indexed by year, no RNG); a return to the middle re-arms; `#[serde(default)]` empty pool = inert. Data-load: both band pools non-empty and the thresholds ordered (0 < low < high < 1) when enabled. Events unchanged (259). +1 test proving a founding crew is silent, a guttering mission says so once, staying faded doesn't reprint, and a rekindled dream announces afresh; 244 total. Determinism preserved. **Completes the third full rotation through all seven axes.**
- 2026-07-24 · campaign skeleton (round 21) · *opens the fourth rotation.* **the deep middle** — closed the gap in the doc's named "era texture (early-voyage vs *mid* vs homecoming)". The skeleton had a founding-era *pool bias* (r5, first 20%) and a *homecoming beat* (it, keyed to the Return leg), so the voyage's start and end each had their reckoning — but the long **middle** had none, though it is the most generation-ship stretch of all: the centuries when the founders are generations dead and landfall is generations away, and a whole people live and die knowing only the corridor between the stars. Added the **mid-voyage beat** (`fire_midvoyage_beat`, the era twin of the homecoming beat): the first tick a voyage passes its *temporal midpoint with home still ahead* (`months_elapsed·2 ≥ total_months` and not yet in the Return leg), a single beat is forced from `midvoyage_beat_family`. New `ActiveContract.midvoyage_beat_fired` (fires once per voyage); a return-dominant charter whose midpoint already falls in its Return leg leaves the reckoning to the homecoming beat instead (the two eras never double-fire). Two authored legacy_drift events give the beat its material — the deep-middle identity crisis the family lacked: **The Middle Dark** (the navigators mark the halfway line: is a life lived wholly in transit a *home* to be embraced — the ship as world, +morale/unity/drift, −loyalty — or a *bridge* to be held for the unborn who will land, +loyalty/stability, −morale?) and **The Bridge Generations** (`min_generation 4` — a child's question no one can answer: who is the voyage *for*, if no one who launched it lives and no one who lands it is born? honour the between-generations as the voyage's keepers, or turn the ship's purpose inward to the living and let the founders' fixed destination become negotiable — seeding `purpose_turned_inward`). It's a fresh *trigger dimension* — voyage-fraction, distinct from the anniversary beat's fixed cadence and the homecoming beat's phase — and it composes with this session's loyalty voice (the deep middle is exactly where the founders' fire gutters). Data-driven and deterministic (reads the clock, no RNG); data-load guards the family has events; the three isolated-timeline soaks and the homecoming test (which jump the clock past the midpoint) silence it alongside the other forced beats. Legacy_drift 60→**62**, events 259→**261**. +1 test proving the beat fires once as the voyage crosses its halfway point with home still ahead (245 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 21) · **a well-kept watch quiets the dark it sails** — the first time any subsystem touches the *event distribution* rather than a stat, and the subsystem-side twin of the charters-round-21 combat coupling. Every subsystem coupling to date scaled a *stat* or a mechanic (decay, output, morale, mortality, births, fuel, stability, unity) — none touched *how often crises arise*, though a **security/justice corps** is exactly the system that ought to. Its two prior domains were internal (it59 unity recovery, it108 stability recovery); this gives it a third, *outward* one: defending the ship against the crises a dangerous route (it85 `hazard`) and a distressed hull breed. New `security_crisis_mitigation` (0.15) + `crisis_weight_floor` (0.1): in `category_weights`, the immediate-crisis category weight is reduced by `security_condition · mitigation`, floored so even a perfect corps only *dampens* danger, never silences it (the dark is never wholly safe). So a well-defended ship faces a calmer event mix — fewer boardings, riots, breaches reaching the council — and a rotted corps sails exposed, the precise counterweight to the charter hazard that raises the same weight. It pairs exactly with the charters-round-21 coupling (guns *work* a contested writ, the corps *quiets* the crises that writ throws) and with the it108 stability recovery (a corps that keeps both the institutions and the perimeter). No signature change — `category_weights` already had `sim` (security condition) and `config` (the penalty). One authored diplomacy event gives the decayed-corps side a face: **The Prowlers** (`condition_below security 0.4`) — scavengers shadow a fat hull whose watch has gone thin, testing edges a sharp corps would have made them respect: rebuild the watch (parts/minerals → +0.3 security condition, the shadows thin and vanish) vs bluff and endure (−morale, run dark on a strength the ship does not have, seeding `the_prowlers_probed`). Deterministic (reads security condition, no RNG; the category weights are relative, and the seeded event-selection stream held — all 246 tests green with no re-baselining). Data-load: the mitigation is range-checked gentle and the floor required when it is on. Diplomacy 30→**31**, events 261→**262**. +1 test proving a sound corps yields a lower crisis weight than a wrecked one, floored below (246 total). Determinism preserved.
- 2026-07-24 · provisioning (round 21) · **the one wasted resource finally earns its keep** — the provisioning axis's first coupling *off food*, which its recent rounds had built into an exhaustive 2×2 (hunger/plenty × spirit/body). The neglected resource was **energy**: unlike food it has no upkeep — it is produced and simply *accumulates and sits idle*, the voyage's one wholly wasted output (the summary's flagged "unused-energy" gap). Gave that idle power a purpose: **in-flight fabrication**. While the ship holds a real energy surplus (`energy ≥ surplus_energy_threshold` 4000) *and* the raw minerals to feed the run, the year tick spends `fabrication_energy_cost` watts + `fabrication_minerals_cost` ore to yield `fabrication_parts_yield` **spare parts** — the ship manufacturing its own maintenance stock off power it would otherwise waste, feeding the it `parts_upkeep_per_year` decay relief. It's a genuine *conversion*, not free creation (it needs idle watts **and** ore, and is gated on holding the feedstock), and **minerals-limited** in the long run (35/yr production vs 40/yr draw → it self-exhausts as ore runs down, so it never trivializes the parts economy), the positive twin of the shortage gates from the surplus side. A pooled `flavor.fabrication` line ({parts} slot, indexed by year) narrates it so the parts appearing read as *something the ship did*, not an unexplained jump — the same legibility principle as the fuel-scoop narration. Data-driven and deterministic (reads the resource levels, no RNG); data-load guards the {parts} slot and that the costs/yield are all positive when the mechanic is on. Mechanics-forward, no new event templates (events 262); the pure-neglect wear-curve soak disables it (`surplus_energy_threshold = 0`) to keep measuring unmaintained decay, since a power-rich ship now legitimately refills its own parts. +1 test proving a power-rich ship banks parts of its own making (spending exactly its ore feedstock) where a power-starved one fabricates nothing (247 total). Determinism preserved.
- 2026-07-24 · factions (round 22) · **content people keep their module kept** — closed the loop the neglect coupling only half-drew. it79 gave the faction↔subsystem boundary its *dark* half: a people whose tended module (`FactionDef.tended_subsystem`) is left to rot below the neglect line *sours*, shedding approval each year — the makers cannot abide a rotting engine bay, the gardeners a dying farm. But there was no *bright* half: a people **delighted** with its lot did nothing for the module it tends, though a proud people plainly keeps its craft a shade sharper than duty demands. Added `apply_proud_tender_upkeep` (the exact positive mirror of `apply_subsystem_neglect_sentiment`, called right beside it in the year tick): each year an aboard tending faction's approval sits at or above `proud_tender_approval_threshold` (0.7 — a *delighted* band), its tended subsystem gains a little condition (`proud_tender_condition_bonus` 0.01) and knowledge (`proud_tender_knowledge_bonus` 0.005), clamped to 1. This closes a genuine **feedback loop** across the boundary that the neglect coupling opened one-way: a kept module keeps its people content (it79 spares them the penalty) and content people keep the module kept — a *virtuous circle*, with a vicious twin when a module is let go and its souring people (it79) let it rot the faster. It's a fresh consequence of the approval meter (which had touched unity it100, growth it86, stability it, reputation it, and voice — but never the *physical module a people tends*), and it composes forward: the module's own condition→output/decay/mortality couplings (it62/it91/etc.) now flow in part from *how well you treat the people who run it*. Data-driven and deterministic (reads approval + the tended-module catalog, no RNG; two-pass to keep the immutable faction read off the mutable subsystem write); gentle by design (a standing dividend, not a repair crew), inert below the delighted band so a fresh ship — launch approval 0.5 — and the silent soaks (approval unmoved) never trigger it. Data-load: the threshold sits in a plausible delighted band and the bonuses are gentle. Mechanics-forward, no new event templates (events 262). +1 test proving a delighted people lifts its module by exactly the year's dividend while a merely-content one lifts nothing (248 total). Determinism preserved.
- 2026-07-24 · charters (round 22) · **a crew that believes in the work does more of it** — the objective's first coupling to the crew's *state*. Objective accrual had learned to read the ship's *fittings* (speed it, combat it166) and its *modules* (objective_subsystem it), so a better-tuned or better-armed or better-kept ship worked a mission faster — but a *demoralized* crew worked exactly as fast as a devoted one, which is false to everything the rest of the sim knows about morale. Added the **crew-morale accrual swing**: in `advance_contract` the on-station accrual is scaled by `1 + morale_objective_swing·(morale − 0.5)` (0.4, floored at runtime so a broken crew drags but never wholly stalls), read from `sim.population.morale` with no signature change. A high-hearted crew (morale → 1) drives the objective ~20% harder, a dispirited one (→ 0) ~20% slower, around the neutral 0.5 midpoint — the crew-state twin of the loadout levers, and the missing consequence that makes *keeping the crew's spirits up matter for the mission*, not only for dodging crises. It composes with the entire morale map the recent rounds built — habitat (it73), chronic hunger/plenty (it/it167), factions (it100) — so every lever that lifts or wears the crew's mood now also speeds or drags the work they're there to do, tying provisioning/factions/subsystems to mission *progress* through a single believable channel. Data-driven and deterministic (reads morale, no RNG; the swing is config); the exact-base-rate isolation test neutralizes it at the 0.5 midpoint exactly as it passes speed/combat 0, and the combat test's two runs share morale so the factor cancels. Data-load guards the swing gentle. Mechanics-forward, no new event templates (events 262). +1 test proving a high-hearted crew works a mission measurably faster than a broken one, all else equal (249 total). Determinism preserved.
- 2026-07-24 · event families (round 23) · **a crew grown soft on long plenty meets a crisis badly** — the abundance twin of the lived-state complication, and pure "complications over one-offs." it added `max_population`/`min_lean_food_years` complication gates (r15) so a crisis reads differently on a *thinned* crew or one *worn by decades of want* — the grim side of the ship's history. But the *comfortable* side had no such texture, though a generation raised in full larders and quiet watches meets its first real reckoning very differently from one hardened by scarcity. Added `Complication.min_fat_food_years` (the exact mirror of `min_lean_food_years`, wired into `active_complication`): the twist rides only once the ship has been *fat* for that many years. Authored three soft-crew complications on existing non-food crises (a fat ship is not rationing, so the softness shows in *other* emergencies), each appended to an event that already carried a complication — so these now demonstrate the doc's "an event that can arrive with 2–3 complications is worth three flat events": **Micrometeoroid Storm** (a generation that has never truly been frightened meets its first violent danger, and unpractised fear runs faster than the debris), **The Thickening Air** (a crew unused to any discomfort takes rationed air out of all proportion, grumbling and hoarding a manageable shortage into a worse one), **The Quiet Fever** (a crew that has buried few meets the fever's first losses with a shock and grief the numbers alone don't explain). All gated `min_fat_food_years 12` — a long plenty, most of a generation. Since a ship cannot be lean and fat at once, the abundance and scarcity complications on the same event pool never both ride. Data-driven and deterministic (reads the fat-years counter, no RNG; complications carry only morale/unity strain, no reputation/consequence effect). Events 262 (three now play an additional way; no new templates). +1 test proving the soft-generation twist rides only once the plenty has held long enough, and not before (250 total). Determinism preserved.
- 2026-07-24 · voice (round 21) · **the ship remarks when the crew stops being one people** — the fourth internal-state voice, filling the last gap in the set. The ship had band-crossing voices for its spirits (it103 morale), its political climate (it104 polity), its name (it111 reputation), its institutions (it134 governance), and its founders' fire (it167 loyalty) — but not for **`unity`**, the crew's cohesion, though unity is a core stat that had only a *collapse beat* (it102, the reckoning) and no gentle voice for the slower thing: a crew quietly splintering into cliques, or quietly knitting back into one people. Added the **unity voice** (`announce_unity_mood`, an exact mirror of the it134 governance voice): new `unity_fraying`/`unity_cohering` pools (4 lines each) + `unity_voice_high` 0.7 / `_low` 0.4 + a `SimState.unity_voice_band` tracker, announced right after the year's it100 faction-mood→unity coupling, security recovery, and voyage strain have all settled on the stat. When cohesion crosses into a fraying band the decks remark the ship sorting itself into wary blocs along the old seams — deck, trade, bloodline — and when it crosses into a cohering one, the mixing that makes many into one people quietly resuming. It's distinct from all four existing voices (a crew can be high-spirited, fairly-treated, well-named, soundly-governed, and sure of its purpose yet still *coming apart into factions* — its own dimension) and from the it102 unity *beat* (the collapse reckoning; this is the quieter fraying noticed before it, and the cohering after). This completes the **internal-state voice quartet** the recent voice rounds were circling — spirits, cohesion, governance, and (loosely) the polity and reputation around them — so every major way the ship's inner life can turn now has a line. The launch band (a founding crew is one people) is recorded not announced; a return to the middle re-arms; being a plain announcement it needs no soak guard (the silent soaks' unity drift narrates itself harmlessly). Deterministic (reads unity, indexed by year, no RNG); `#[serde(default)]` empty pool = inert. Data-load: both band pools stocked and the thresholds ordered when enabled. Events unchanged (262). +1 test proving a founding crew is silent, a splintering one says so once, staying frayed doesn't reprint, and a crew pulling back together announces afresh (251 total). Determinism preserved.
- 2026-07-24 · campaign skeleton (round 22) · *opens the fifth rotation.* **the founders pass** — completed the **era-beat trio** the doc named ("early-voyage vs mid vs homecoming"). Last campaign round built the mid-voyage beat (it, the deep middle); the homecoming beat (it) forces the late reckoning; but the **early** era had only a founding-era pool *bias* (r5, which merely tilts a random beat's draw), never a *guaranteed* reckoning like the other two eras. Added the **founding-era beat** (`fire_founding_beat`): the campaign-year the voyage passes `founding_beat_year` (50 — when the launch generation, the ones who *chose* to leave, has largely died of old age and the ship is crewed for the first time wholly by those born to the void), a single beat is forced from `founding_beat_family`. It's **campaign-scoped** — a new `SimState.founding_beat_fired` flag (not a per-contract one like mid-voyage/homecoming), since the founding passes once *ever*, so a back-to-back second charter does not re-mark it — a fresh trigger shape (the first campaign-once *time* beat, distinct from the per-voyage era beats and the periodic anniversary). Two authored legacy_drift events give it substance — the deep-middle events (r21) got their early-era bookend: **The Founders Pass** (the last soul who stood under an open sky dies; no one living now remembers the launch — enshrine the founders' choice as a covenant a void-born crew still lives by (+loyalty/unity) vs honour them and let the founding become history, the living owing the dead remembrance not obedience (+adaptation/drift, seeds `founding_let_go`)) and **The Void-Born Come of Age** (`min_generation 2` — the first cohort born aboard to parents born aboard, to whom the ship is not a journey but the only home: teach them a sky they'll never see (+loyalty) vs let them belong to the ship, at home where their elders were only ever in transit (+adaptation/unity)). So the three great turnings of a generational voyage — the founding's close, the deep middle, the turn for home — each now force their reckoning. Data-driven and deterministic (reads the campaign clock, no RNG); data-load guards the family has events and an early year; the full-voyage soaks silence it alongside the other forced beats (the many voyage tests that resolve pending events absorb it untouched). Legacy_drift 62→**64**, events 262→**264**. +1 test proving the beat fires once as the launch generation passes and does not re-fire (252 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 22) · **a warm, fed ship can still be a grim one** — completed morale's environmental map with its missing *cultural* pillar. Morale drew from the **physical** (the habitat it73, the larder's lean/fat years it/it167) and the **social** (the peoples' mood it100), but nothing **cultural** — though a crew can be housed and fed and cohesive and still grim if its schools teach rote, its arts are forgotten, and the year's festivals have quietly lapsed. Added `education_morale_effect` (the exact cultural twin of it73's `habitat_morale_effect`): the education/culture module's *condition* — its schools, theatres, and festival spaces functioning — moves morale by `education_morale_swing · (condition − 0.5)` each year, a living culture lifting spirits above the midpoint and a hollow one dragging them below. It reads education's **condition** (the cultural facilities), deliberately distinct from its **knowledge** (the founding remembered), which the it knowledge-keystone and it drift-resistance already use — so the module's two faces do two different things: its *memory* holds the people to the founders, its *vitality* keeps their spirits up. This gives the education module a third coupling and, more, completes a clean three-pillar morale model — body (habitat/food), belonging (factions), and meaning (culture) — so every major dimension of a crew's wellbeing now feeds the stat, and it composes with this session's charters-round-22 morale→mission coupling (a culturally-starved crew is now not only unhappy but measurably slower at the work). Gentler than the habitat swing by design (0.008 vs 0.01 — culture a softer pillar than shelter). Data-driven and deterministic (reads condition, no RNG); `#[serde(default)]` 0 = inert. Data-load guards the swing gentle. Mechanics-forward, no new event templates (events 264). +1 test proving a living culture lifts morale, a hollow one drags it, and a middling one does neither (253 total). Determinism preserved.
- 2026-07-24 · provisioning (round 22) · **a lone ship is a whale in a thin market** — the provisioning axis's second diversification off food (round 21 gave *energy* the fabricators; this gives the **market** its first responsiveness). The market was a pure yearly random walk (`drift_prices`, GDD §5.1 "keep as-is") that never once reacted to the ship's *own* behaviour — you could buy up a whole waypoint's minerals or dump ten thousand tonnes of surplus and the price would not so much as twitch, though a lone generation ship trading at a small outpost is exactly the kind of whale that moves a thin pool. Added **market price impact**: a bulk buy pushes the good's local price *up* against the next lot, a bulk sell floods the market and pushes it *down*, scaled by `market_impact_per_unit` (0.0003) × the resource's base price × the amount, and clamped to the *same* 0.5×–3× band the drift already honours. It is deliberately **additive, not a rework** — the "keep as-is" yearly random walk is untouched, and in fact does the *recovery*: a bulk trade's mark on the price is real but temporary, the drift walking it back toward base over the following years. So trading is now a *timed* provisioning decision — dump your mineral haul all at one stop and you tank the price you get for the tail of it; stock up on food in one desperate spree and you pay a rising premium — rewarding spreading trades across waypoints and years over one big transaction. The factor is copied onto `MarketState` at campaign start (so `buy`/`sell` need no config plumbing at their call sites); `#[serde(default)]` 0 = a bottomless market a single ship never moves (which is what the old behaviour was). Data-driven and deterministic (the shift reads the trade amount, no RNG; it composes with the seeded drift without touching its stream). Data-load guards the impact gentle. Mechanics-forward, no new event templates (events 264). +1 test proving a bulk buy drives a price up, a bulk sell drives it down, and even a whale trade stays inside the drift's band (254 total). Determinism preserved.
- 2026-07-24 · factions (round 23) · **two quarreling peoples under one hull is a standing cost, not just an event** — made the rival/ally graph a *continuous* pressure on cohesion. The friction pairs (it14 rivals) and kinship pairs (it17 allies) had until now touched the ship only at *event time* — a favored people's approval spilled onto its rivals (down) and allies (up). But the mere fact of carrying two large rivals, or a large allied bloc, exerted no *standing* pressure, though a ship split between two peoples who cannot abide each other is plainly harder to hold together year on year than one whose great peoples are kin. Added `apply_faction_relationship_cohesion` (the relationship-side twin of the it100 approval→unity coupling, called right beside it in the year tick): each year, every pair of aboard **rivals** grinds `rival_unity_friction` off unity and every pair of aboard **allies** adds `ally_unity_solidarity`, each **scaled by the product of the two peoples' shares** — so a rivalry bites only when *both* parties are large (a tiny remnant faction troubles no one) and the pressure is strongest on a ship split evenly between two great rivals. So the *composition* of the roster, not only its mood (it100) or its ideological spread (it18), is now a standing cohesion cost or dividend — and the three faction→cohesion couplings read three genuinely different things: how content the peoples are, how ideologically split, and how they stand to *each other*. It deepens the "who you carry" decision (a coalition of kin is a materially steadier ship than a hull of rivals, beyond any event) and composes with everything downstream of unity (the it102 crisis/recovery beats, this session's it unity voice). Deterministic (reads the roster shares, no RNG; pairs read symmetrically, the constants absorbing the double-count); gentle (the share-product scaling keeps even an evenly-split great-rival ship to a small yearly drift). `#[serde(default)]` 0 = inert. Data-load guards the pressures gentle. Mechanics-forward, no new event templates (events 264). +1 test proving two large aboard rivals wear at unity while a large allied bloc lifts it (255 total). Determinism preserved.
- 2026-07-24 · charters (round 23) · **a mission you can only lose ground on** — the doc's "different quantified objectives, not a reskin," finally taken at its word with a genuinely new objective *shape*. Every charter ever authored *accrues* its objective — starts at zero and builds toward the target during Operation (quickened lately by speed it, subsystem condition it, combat it166, and crew morale it). A **preserve** objective inverts that completely: the ship sets out *carrying* the full target and the mission is to arrive with as much of it intact as possible — a number that can only ever go *down*. New `ContractTemplate.preserve_objective` + `preserve_attrition_per_year`: `start_contract` seeds the progress at the full target instead of zero, and `advance_contract` replaces the accrual with a steady per-month erosion across the whole voyage (Travel/Operation/Return), on top of whatever hazard events take; pay is the surviving fraction. The exemplar is **The Ark Charter: Ten Thousand Sleepers** — a whole world's frozen founding population entrusted to the ship for a three-century crossing, the cold banks failing here and there no matter how well tended, so that everything the crew does for three hundred years is in service of a number that only shrinks (delivered whole, its completion reward is the rare pride of having kept faith with the frozen). It's a fresh strategic feel — no charter before rewarded *minimising a loss* rather than maximising a gain, and it turns the whole voyage-survival apparatus (hazard, the security→crisis coupling it, morale, the medical bay) toward protecting a cargo instead of enduring a hardship. Data-driven and deterministic (the erosion reads the clock, no RNG); the ark run carries no milestones (a preserve objective starts at full fraction, which would trip progress-threshold marks), grading purely on its metrics. Data-load guards a preserve charter erodes at a gentle positive rate (else "keep the cargo" is a free win). Charters 21→**22**, events 264 (a new charter, no new event templates). +1 test proving a preserve charter sets out full and only erodes while an ordinary one builds from zero (256 total). Determinism preserved.
- 2026-07-24 · event families (round 24) · **the loops this session opened, closed — and a new chain shape** — a chains round that paid off the four consequence tags the fifth rotation's *mechanics* rounds had left dangling (a chain seeded but never landed is the exact fault the chain-axis exists to prevent). The mid-voyage beat (it) seeded `the_voyage_became_home` and `purpose_turned_inward`, the founding beat (it) seeded `founding_let_go`, and the security event (it) seeded `the_prowlers_probed` — all produced, none consumed. Authored three payoffs, and in doing so introduced a **convergent chain** — the first payoff gated on *two* seeds at once (`requires_consequence` is AND), where every prior chain was a single seed→payoff line: **The Untethered** (`founding_let_go` **and** `purpose_turned_inward`, `min_generation 5`) reckons only for a ship that has *both* buried its founders and turned its loyalty to the living — a people that answers to no one but itself, choosing between authoring a destination of its own or wandering free of all of them, the capstone of the whole post-founding identity arc the era beats built. The other two are linear: **The Shore Declined** (`the_voyage_became_home`) — the ship that decided it was a home, not a journey, reaches its founders' landfall at last and a void-born people must choose whether to make planetfall or salute the shore and sail on; **The Watchers Return** (`the_prowlers_probed`) — the scavengers that once tested a thin watch and were turned by a bluff come back to call it, and the ship's answer turns on whether it grew real teeth or is still bluffing (moving `resolve`, it). Each payoff is *terminal* (seeds no new tag), so the round nets the ledger down (31→27 orphans). Data-driven and deterministic (the gates read the consequence ledger, no RNG). Legacy_drift 64→**66**, diplomacy 31→**32**, events 264→**267**. +1 test proving the convergent capstone opens only with *both* seeds on record and stays barred with one (AND, not OR) (257 total). Determinism preserved.
- 2026-07-24 · voice (round 22) · **the ship's own body finds its voice** — the first voice for the *vessel* rather than the crew within it. Every band-crossing voice to date read the *people* — their spirits (it103 morale), cohesion (it unity), political climate (it104), name (it111), government (it134), and founders' fire (it loyalty). But the ship itself — the aging machine that carries them all — went through its whole long decline and its refits in silence, though a worn hull is as legible a thing as a low morale: it groans on the burn, it weeps at the seams, it rides heavy and complaining. Added the **hull voice** (`announce_hull_condition`, the same shape as the it134 governance voice but reading `sim.ship.hull_integrity` off the *ship* rather than a population stat): new `hull_groaning`/`hull_sound` pools (4 lines each) + `hull_voice_high` 0.72 / `_low` 0.45 + a `SimState.hull_voice_band` tracker, announced right after the year's hull wear (and any refit) settles. When the hull crosses into disrepair the decks remark the old frame complaining, the plates a quilt of patches, a generation ship that has begun unmistakably to *age*; when a hard refit brings it back the ship rides tight and true again, sound enough that the crew stop bracing for the next failure. It's a genuinely new voice *dimension* — not a seventh reading of the crew's inner life but the first reading of the ship as a physical body, the machine-side companion to all the people-side voices — and it oscillates richly (hull decays *and* is repaired, so it re-arms far more than the near-monotonic identity stats would). The launch band (a new-built hull is sound) is recorded not announced; a return to the middle re-arms; being a plain announcement it needs no soak guard. Deterministic (reads hull integrity, indexed by year, no RNG); `#[serde(default)]` empty pool = inert. Data-load: both band pools stocked and the thresholds ordered when enabled. Events unchanged (267). +1 test proving a new-built hull is silent, an aging one groans once, staying worn doesn't reprint, and a refit hull rides sound afresh (258 total). Determinism preserved. **Completes the fifth full rotation through all seven axes.**
- 2026-07-24 · campaign skeleton (round 23) · *opens the sixth rotation.* **the frame itself fails** — completed the voice→beat pair the last voice round opened. The it17 subsystem-collapse beat guarantees a reckoning when a *module's* condition fails (engineering, the decay keystone), but the ship's own **hull** — its structural frame, a distinct survival stat that decays yearly and is mended by parts — had no beat: it could rot all the way to breakup on a lucky run of not-rolled events. And having just given the hull a *voice* (voice r22), the established pattern (loyalty/reputation/stability each have a gentle voice *and* a beat at the strong band) demanded its beat. Added the **hull-collapse beat** (`fire_hull_beat`, the structural twin of the subsystem-collapse beat): the month `hull_integrity` falls to or below `hull_beat_threshold` (0.25 — well past the it warning line, the ship not merely worn but *failing*), a beat is forced from `hull_beat_family` (engineering); a `SimState.hull_beat_band` tracker fires it once per crossing and re-arms on a refit back above the line (a ship rebuilt and let fail again reckons anew — richer than the campaign-once collapse beats, because a hull oscillates). Added a new honest gate — `EventTemplate.hull_below` (the structural parallel to the subsystem `condition_below`) — so the reckoning surfaces *hull*-crisis content, and two authored engineering events give the beat its substance: **The Failing Frame** (`hull_below 0.25` — the load-bearing spine has crossed from worn to failing: strip whole decks for their structural steel and fly a smaller sound ship, or stay whole and fly a dying frame gently) and **The Condemned Decks** (`hull_below 0.25` — the failing hull cannot be held all at once: seal and abandon the weakest fifth of the ship for a sound remainder, at the cost of the decks and a few who won't leave, or brace it all and hold the whole strained vessel). Together with the it voice, the ship's *body* now both murmurs (the voice, as it wears) and reckons (the beat, when it fails) — exactly the pattern the crew-stats have. Data-driven and deterministic (reads hull integrity, no RNG); data-load guards the family has events, a red line in (0,1), and a hull_below event to surface; the full-voyage soaks (which rot the hull past the line) silence it alongside the other forced beats. Engineering 32→**34**, events 267→**269**. +1 test proving the beat fires once as the frame fails and re-arms on a refit (259 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 23) · **the home raises the young; the infirmary keeps them alive to grow up** — completed the "raising the next generation" picture the habitat round only half-drew. it19 gave the *habitat* a coupling to the real-time-loop birth model (a sound home raises the cohort on schedule, a failing one sees fewer come of age) — but the **medical bay**, whose whole purpose is keeping people alive, touched only *death* (the it131 mortality relief, the it famine relief), never *birth*, though an infirmary is precisely what carries a cohort through the fevers and frailties of childhood to their majority. Added `medical_renewal_factor` (the healthcare twin of it19's `habitat_renewal_factor`): the medical bay's condition scales the yearly birth chance by `1 - medical_renewal_penalty·(1 - condition)`, penalty-below-full, **stacked multiplicatively** with the habitat factor in `annual_aging` — so renewal is now *housing × healthcare*, a home to raise the young in and a bay to keep them living. It's distinct from the it131 medical death-relief (which keeps the *grown* alive against the monthly age roll): this keeps the *unborn-to-adult* pipeline whole, the infant/child-survival end the death relief never covered. And it deepens the medical-neglect stakes into a two-sided spiral — let the bay rot and the ship loses both the sick it would have saved *and* the children it would have raised, the dwindling line inching toward the extinction the mortality system made possible. Gentler than the habitat penalty (0.3 vs 0.5 — housing is the larger lever), penalty-capped so even a collapsed bay only *thins* the cohort, never zeroes births. Data-driven and deterministic (reads condition, no RNG); `#[serde(default)]` 0 = inert. Data-load guards the penalty a fraction. Mechanics-forward, no new event templates (events 269). +1 test proving a sound infirmary keeps the baseline renewal, a failing one raises fewer of the young, and a wrecked one loses the most but never all (260 total). Determinism preserved.
- 2026-07-24 · provisioning (round 23) · **the sleeper-ark gets its signature crises** — a *content* round (the axis's first in seven, after r16–22's run of couplings), giving this session's new **ark run** (it, the preserve charter carrying ten thousand frozen colonists) the tag-keyed events every other charter shape has and it launched without. Added a distinctive `cryo_ark` tag to the ark run and authored two crises gated to it (`requires_charter_tag`), each wired straight into the it preserve mechanic — the objective *is* the sleepers, so a loss is a `objective_progress_delta` off the surviving count: **The Failing Bank** (`phases: travel` — a cryo-bank section faults, the cold creeping toward the no-wake line: strip the living ship thin of power and parts to hold it, or seal it and let the several hundred in it go, −0.04 of the objective) and **The Waking Temptation** (an ethics dilemma unique to a ship carrying its own future in the hold: wake the specialists the strained crew needs and found a smaller colony, −0.02 objective and −loyalty, or keep faith with the frozen and carry the ship's burdens with its own hands, +unity/loyalty and +`resolve`). Both give the preserve charter what an accruing one never needed — the sense that the number can be *spent*, choice by choice, and that every soul lost or woken is one the waiting shore will not receive. It exercises the `objective_progress_delta` erosion path on the exact charter built to be eroded, closing the loop between the it charter mechanic and the events that press on it. Data-driven and deterministic (the gate reads the charter's tags, the loss reads the objective, no RNG); data-load's tag-provenance check binds `cryo_ark` to the ark run that carries it. Survival 47→**48**, ethics 15→**16**, events 269→**271**. +1 test proving the failing bank surfaces on the sleeper-ark in transit and not on an ordinary mining run (261 total). Determinism preserved.
- 2026-07-24 · factions (round 24) · **losing a whole people leaves a hole the crew feel** — completed the *cost of a departure*. When a people secedes into the dark or settles a world and stays, the ship had accounted for two of the three losses: the bodies (the W7 headcount drop) and the craft (the it20 knowledge the tending people took with them). But the third — the wound to the *community* — went unmodelled, though losing a whole people is plainly a blow to a ship's cohesion beyond any ledger: a familiar quarter gone quiet, the balance upset, the remaining crew shaken. Added a **departure cohesion scar** in `remove_faction`: on any loss (Departed or Settled), morale *and* unity each take `departure_cohesion_scar` (0.15) **scaled by the departing people's share of the ship** before they left — so a great secession (half the crew walking) is a real blow (~0.075 off both) while a tiny remnant slipping away barely registers (the share-scaling the it23 rival friction also uses, so the mechanic never over-punishes losing a group that was small anyway). It composes with everything: the scar stacks on the schism *event's* own choice-cost (the emotional valence of how they left) and on the it20 knowledge loss and the raw headcount, so a schism is now felt across bodies, craft, *and* spirit — three distinct debts — and it feeds forward into the it23 rival/ally cohesion pressures and the it unity voice (a big departure can tip the ship into a fraying band the decks then remark). Distinct from the it100 approval→unity coupling (that reads how *content* the aboard peoples are; this is the one-time shock of one *leaving*). Deterministic (reads the departing share, no RNG); the silent soaks (no events → no schisms) and autoplay (absorbs the scar) untouched, all green. `#[serde(default)]` 0 = a departure costs only bodies and craft. Data-load guards the scar bounded so even a full-ship secession can't empty cohesion in one stroke. Mechanics-forward, no new event templates (events 271). +1 test proving losing a people holding half the ship scars morale and unity by the scar times its share (262 total). Determinism preserved.
- 2026-07-24 · charters (round 24) · **a bigger hold hauls more** — completed the loadout→accrual *trio*. Objective accrual answered to two of the ship's three loadout stats — `speed` (it, every mission) and `combat` (it166, contested ones) — but the third, **cargo capacity**, did exactly one passive thing (the it `minerals_per_cargo` yearly trickle from bigger holds) and never once touched a *mission*, though on a **haul** writ the hold *is* the bottleneck: a mining run measured in tonnes or a salvage stripping a wreck is limited by how much the ship can carry out of every operation cycle. Added `objective_cargo_scaling` (the exact mirror of it166's `objective_combat_scaling`, threaded through `advance_contract` as a `cargo` param beside `speed`/`combat`): on a haul writ each point of cargo adds this fraction to the accrual, so a big-bellied ship hauls tonnage faster and a small-hold one fills and must stop — but *only* where the objective is a quantity of *material* (a survey hauls no readings and a greening lands no settlers faster, so those set 0 and hold size is dead weight). Wired to the three material-extraction writs — **the Deep Camp** (tonnes extracted), **the Karst Works** (tonnes brought online), and **the Vane Default** enforcement salvage (tonnes recovered) — each at a far gentler per-unit rate than combat's (0.0005 vs 0.04, since cargo counts in the hundreds where combat counts in single digits). So the drydock's three-way fitting trade — a fast engine, heavy guns, or a deep hold — now each flies a *different kind* of mission well, the loadout↔charter coupling completed across all three stats. Data-driven and deterministic (a bounded positive accelerator read from the loadout, no RNG); the default 0 leaves every non-haul charter's accrual byte-for-byte unchanged, and the ~16 test/autoplay `advance_contract` callers pass cargo 0 (the established isolation convention). Data-load range-checks the field. Charters 22 / events 271 (fields on 3 charters, no new templates). +1 test proving a bigger hold hauls a material writ faster while a survey is indifferent to it (263 total). Determinism preserved.
- 2026-07-24 · event families (round 25) · **a debt the dead signed, come due on the living** — a *timed* chain, the under-used chain shape. Nearly every arc built to date is *state*-based (`requires_consequence` — a payoff waits until some flag is on record); the it9 `schedule_followup` mechanism, which fires a payoff on a *fixed clock* regardless of state, had only four uses in the whole set, though "an early choice re-fires a consequence decades later" is exactly the chain the doc names. Authored a two-event timed chain that leans into what only a clock can do — carry a debt *across generations* to a crew that never agreed to it: **The Mortgaged Passage** (a prosperous waystation offers a generous resupply now against a bond repaid twice over when its collectors come calling "to the year"; take the bond for the stores and `schedule_followup` the reckoning 55 years on, or decline and go leaner but unmortgaged, +`resolve`) and **The Collectors Return** (`scheduled_only` — 55 years later the collectors match course to claim a debt signed by a ship that no longer exists in any living memory: pay it and keep a name for a hull whose word outlives its crews, +`resolve`/stability/loyalty, or repudiate a debt the living never chose and be marked a hull that does not pay what it owes, −`resolve`). It's the timed-chain twin of the state chains: where those pay off when the ship *becomes* something, this pays off when the ship simply *arrives at a year* — a promise made by one generation and kept or broken by another, which is the most generation-ship kind of consequence there is. Reuses the it9 scheduling + `scheduled_only` payoff, no schema change. Data-driven and deterministic (the payoff is queued for a computed year, forced past its gates when the clock strikes, no RNG); data-load's scheduled-event provenance binds the seed's `schedule_followup` to the real payoff and confirms the payoff is `scheduled_only`. Diplomacy 32→**34**, events 271→**273**. +1 test proving taking the bond queues the collectors for the exact year it named while declining leaves nothing on the clock (264 total). Determinism preserved.
- 2026-07-24 · voice (round 23) · **the air goes close and stale, and the decks feel it** — the second ship-body voice, completing the pair the hull voice opened. it22 gave the ship's *structure* a voice (the hull groaning as it wears, riding sound after a refit); its twin survival stat, **life-support** — the ship's *air* — went through its own staling and clearing in silence, though a crew notices a close, thick, over-recycled atmosphere as plainly as a groaning frame, and differently: not a sound but a smell, a heaviness, a headache waiting on the lower decks. Added the **air voice** (`announce_air_condition`, the exact shape of the it22 hull voice but reading `sim.ship.life_support`): new `air_stale`/`air_fresh` pools (4 lines each) + `air_voice_high` 0.75 / `_low` 0.5 + a `SimState.air_voice_band` tracker, announced right after the year's life-support wear (and any repair) settles. When the air crosses into stale the decks remark the scrubbers labouring, the sour flat used-up quality of an atmosphere through the reclaimers one too many times; when an overhaul clears it, the ship breathes easy again, the green edge back, the crew waking rested and not remarking on it — which is the point. Structure and atmosphere, the ship's two survival systems, now *both* speak, so the whole physical vessel — not just the crew inside it — has its voice. It oscillates as richly as the hull (life-support decays *and* is restored). The launch band (a new ship breathes clean) is recorded not announced; a return to the middle re-arms; being a plain announcement it needs no soak guard. Deterministic (reads life-support, indexed by year, no RNG); `#[serde(default)]` empty pool = inert. Data-load: both band pools stocked and the thresholds ordered when enabled. Events unchanged (273). +1 test proving a new ship's air is silent, staling air says so once, staying stale doesn't reprint, and cleared air says so afresh (265 total). Determinism preserved.
- 2026-07-24 · campaign skeleton (round 24) · **when the ship itself starts to suffocate** — completed the air voice→beat pair, exactly as it23 completed the hull's. The it23 hull-collapse beat forces a reckoning when the ship's *frame* fails; this session's it23 air *voice* murmurs as the air stales — but life-support, the ship's second survival stat, had no *beat*, so the atmosphere could fail all the way to killing on a quiet run where the dice never rolled a crisis. Added the **air-collapse beat** (`fire_air_beat`, the atmosphere twin of `fire_hull_beat`): the month `life_support` falls to or below `air_beat_threshold` (0.25 — the air not merely stale but *failing*), a beat is forced from `air_beat_family` (biology_medical); a `SimState.air_beat_band` tracker fires it once per crossing and re-arms on an overhaul back above the line. Added the `life_support_below` event gate (the atmosphere parallel to the it23 `hull_below`) so the reckoning surfaces *air*-crisis content, and two authored biology_medical events give it substance, distinct from the hull's frame crises: **The Dying Air** (`life_support_below 0.25` — the scrubbers can no longer clean what the whole ship breathes: seal everyone into the decks the failing plant can still hold and weld off the rest, or hold the whole ship open and fight the thinning air across its length at a heavier cost in lives) and **The Green Lungs** (`life_support_below 0.25` — flood every free volume with crash-grown algae and let the oldest air-maker there is breathe for the ship while the machines are rebuilt, or trust metal over moss and crash-rebuild the mechanical scrubbers from cannibalized parts). The ship's *body* now reckons at both its mortal failures — frame (it23) and air — each with a voice that murmurs first and a beat that forces the crisis, the full physical-vessel pattern the crew-stats already had. Data-driven and deterministic (reads life-support, no RNG); data-load guards the family, the red line, and a `life_support_below` event; the full-voyage soaks (which rot life-support past the line) silence it alongside the other forced beats. Biology_medical 21→**23**, events 273→**275**. +1 test proving the beat fires once as the air fails and re-arms on an overhaul (266 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 24) · **the bay that mends the ship mends the hull too** — closed a gap the decay keystone left open in plain sight. The it62 engineering bay is the ship's *maintenance capacity* — its condition scales how fast every *other module* decays, and it166/it131 made it eat fuel and (via the keystone) speed rot. But the ship's own **hull** — the one structure most obviously *maintained in the engineering bay* — decayed at a rate touched only by spare parts and fuel, never by the bay itself, so a wrecked bay sped every module's rot while leaving the frame it welds and fabricates untouched, which is backwards. Added `engineering_hull_decay_factor` (the hull twin of the it166 fuel-burn factor): hull wear is scaled by `1 + engineering_hull_decay_penalty·(1 - condition)`, penalty-below-full and floored at 1.0 — a sound bay holds the hull at its baseline wear, a failing one lets the frame rot up to half again as fast, and a good bay never makes the hull immortal. It extends the keystone from the modules to the ship's structure and, more, **compounds this session's hull-collapse spiral**: a neglected engineering bay now hastens the hull toward the it hull-collapse beat's red line even as it (it62) hastens its own — so letting the bay go is the single most dangerous neglect on the ship, speeding module decay, wasting fuel, *and* aging the frame toward structural failure all at once. Data-driven and deterministic (reads the bay's condition, no RNG); at full condition the factor is exactly 1.0, so every existing hull-wear result (the neglect soak's careful curve included) is unchanged. `#[serde(default)]` 0 = inert. Data-load guards the penalty bounded. Mechanics-forward, no new event templates (events 275). +1 test proving a sound bay holds the hull's baseline wear, a failing one rots it faster, and a wrecked one fastest of all (267 total). Determinism preserved.
- 2026-07-24 · provisioning (round 24) · **a hold is not a granary; the dark does not preserve** — closed the last unbounded resource. Food had a rich system around its *scarcity* (lean years → morale/death) and its *plenty* (fat years → morale/births), but no *ceiling*: unlike fuel (burned) or parts (spent) or energy (now sunk into it166 fabrication), stored food had no upkeep and no cap, so it could pile up without limit — a ship could hoard a hundred thousand units against every future lean stretch and pay nothing for the hoarding, which is false to a sealed hull whose cold-holds and hydroponics can only keep so much fresh. Added a **food carrying capacity** (`food_carrying_capacity` 60000) + **spoilage** (`food_spoilage_fraction` 0.1): each year, a tenth of whatever food is held *above* the capacity rots. It is a deliberately *soft* cap — the excess *asymptotes* toward the line rather than being clipped to it, so a temporary surplus (a good harvest, a big resupply) is tolerated and only a *sustained* over-deep hoard erodes — and set well above the it fat line, so a prudent reserve still reads as plenty and never spoils the ship out of its own abundance. So abundance is now about *flow* — keeping the larder comfortably full — rather than a stockpile you can bank without limit, and the fat-years plenty content is the reward for sustained good provisioning, not a hoard. Narrated (a pooled `flavor.food_spoilage` line, {spoiled} slot, indexed by year) so the loss reads as something that happened, not an unexplained dip — the same legibility principle as the fuel-scoop (it) and fabrication (it) lines. It fires almost never in ordinary play (a normal ship's larder hovers far below the cap) — its job is to bound the extreme, not tax the typical. Data-driven and deterministic (reads the food store, no RNG); the soaks stock food far past the cap and simply spoil toward it, staying famine-free (still ~1200 years of stores at the cap), all green. `#[serde(default)]` 0 = stores never spoil, as before. Data-load guards the cap above the fat line, the fraction gentle, the narration slotted. Mechanics-forward, no new event templates (events 275). +1 test proving an over-deep hoard erodes toward the cap while a ship at sensible stores loses nothing (268 total). Determinism preserved.
- 2026-07-24 · factions (round 25) · **who runs the ship unlocks a choice others can't take** — the last layer of the "who's in charge" coupling. The dominant faction already decides which whole *events* fire (it6 `requires_dominant_faction`), which *complications* ride (it22), and shifts a *dilemma*'s odds (it10) — but it never once put a distinct *option on the table within an ordinary event*, though reputation does exactly that (it17 outcome gates). Added `requires_dominant_faction` to `OutcomeRequirement` (the choice-level parallel, wired into `outcome_available` and folded into `is_unconditional`, the it6 faction-id data-load check extended to cover it): an outcome appears only while the named people runs the ship, so who is in charge shapes not just the crises the ship meets but the *answers* it has to them. Authored two exemplars, each a signature solution only its people could offer, appended after the always-shown base choices (the it12 outcome-0-unconditional contract intact): **The Wasting** (a slow incurable degenerative illness — quarantine it or endure it openly, always available; but under the **Ascension Circle**, *rewrite the affliction* out of the germ line, a true cure that steps the crew a measured pace further from the founders' baseline, +adaptation/drift −loyalty — the augmenters editing their afflictions where others must bear them) and **The Seized Works** (a great old founder-fitted assembly jammed — jury-rig a bypass or replace it from dwindling stores, always available; but under the **Steel Covenant**, *rebuild it better than the founders'*, the makers handing back not a restoration but an improvement that leaves the engineering bay itself sharper, +condition/knowledge — a makers' solution no other polity offers). So the six peoples are now distinguished not only by their approval, drift, and demographics but by the *choices they bring* to the ship's crises — a tech-cult ship, a makers' ship, and a traditionalist ship genuinely meet the same crisis with different options. Data-driven and deterministic (reads the dominant people, no RNG); a base choice always shows, so a no-clear-majority ship simply sees the unconditional options. Biology_medical 23→**24**, engineering 34→**35**, events 275→**277**. +1 test proving the Ascension cure is on the table only under Ascension rule while the base choices always are (269 total). Determinism preserved.
- 2026-07-24 · charters (round 25) · **a mission done well teaches more than one barely scraped** — the capstone on the loadout→accrual trio. This session completed the three accrual accelerators (speed it, combat it166, cargo it) — a ship fit for a mission finishes it *better* — but the it15 completion reward, the lasting capability a charter leaves the ship, was **all-or-nothing**: the it18 gate granted the *full* boon at any non-Failure conclusion, so a mission barely scraped to a Pyrrhic pass and one flown to a clean Complete left the crew exactly the same craft. Made the reward's **lessons scale with performance**: `apply_completion_reward` now takes the mission's success *score* and scales the it15 subsystem capability boon (condition + knowledge — the "the lessons of the mission stay with the ship") by it, so a Complete (score → 1.0) teaches the full craft and a Pyrrhic (→ 0.4) barely a third of it. It closes a real loop — the accelerators that help a ship finish better now *pay off* in a bigger lasting boon, so fitting the right engine/guns/hold and keeping the crew's spirits up compounds into permanent capability, not just this mission's pay. Deliberately scoped: only the *capability lessons* scale (the craft you half-learn from a scrappy job); the **deed** and what it earns — the it17 reputation the mission built, the it19 faction goodwill, the it20 salvaged component — stay *binary*, because those *happened* (you carried the refugees, you recovered the hull) regardless of how efficiently, and a discrete component cannot be half-granted anyway. Data-driven and deterministic (scales by the computed score, no RNG); the reward still no-ops on Failure (it18) and on an ordinary charter, and the pure-fn tests pass performance 1.0 (full, isolating the reward as before). Charters/events unchanged (22/277 — a scaling of an existing reward). +1 test proving a clean completion teaches exactly twice what a half-score one does (270 total). Determinism preserved.
- 2026-07-24 · event families (round 26) · **the thinnest family, and its first genuinely alien encounters** — grew `exploration_first_contact`, the floor family (long stuck at 12) and, on a closer look, the *shallowest*: seven of its twelve were mere *navigation* flavor (arrival burns, beacons, course forks), leaving only five that were actually *first contact*. Authored two rich alien-encounter scenarios that lean into what the family is supposed to be about — and, per "complications and twists over one-offs," gave one of them the faction-outcome coupling this session's it25 built, so a first contact reads differently by *who runs the ship*: **The Long Watched** (the ship discovers it has been silently observed for years by an unhailing intelligence — hail them first and be a greeter rather than a specimen, seeding `hailed_the_watchers`, or slip away and carry the unease of being seen and choosing not to look back) and **The Offered Road** (an old, casually superior civilization offers a folded-space corridor that would cut generations off the crossing — the price is the ship's whole archive, its founding and story, copied into alien keeping: keep the long road and your own story at the cost of the generations, +`resolve`; trade the archive for the shortcut, seeding `archive_given_to_strangers`; **or, under the Ascension Circle, bargain as their kindred** — the augmented treating with the strangers as a lesser kin rather than a supplicant, winning the road *and* keeping the archive, paid only in how much further the deal carries the crew from the founders' baseline). So the weakest family now has genuine first-contact texture *and* the who's-in-charge dimension the rest of the game has, and the two `long_term_consequences` it plants (`hailed_the_watchers`, `archive_given_to_strangers`) are deliberate seeds for a future payoff round — the watchers answering, the archive resurfacing in a stranger's hands. Data-driven and deterministic (the faction gate reads the dominant people, no RNG); the base choices always show, so a no-majority ship sees the ordinary two. Exploration_first_contact 12→**14**, events 277→**279**. +1 test proving The Offered Road's kindred bargain opens only under Ascension rule while its two base choices always show (271 total). Determinism preserved.
- 2026-07-24 · voice (round 24) · **the ship's deaths stop reading as a form letter** — a variety pass on the last high-frequency *hardcoded* log lines, the doc's "replace remaining flat lines / no repetition tells." The stat-voices are saturated (crew: it103 morale, it unity, it104 polity, it111 reputation, it134 stability, it loyalty; ship-body: it22 hull, it23 air), and the generational deaths were pooled long ago (it18 obituary/succession/crew_death) — but three flat one-liners survived in the two *worst* places for repetition: the **event death-claim** (a heavy event or complication taking a named soul — one string for an officer, `{name}, the ship's {post}, was among the lost`, one for a crew member) and the **failing life-support toll** (`{losses} were lost to the thinning decks`), the latter reprinting the *same line every single year* the air fails, a stretch that can run a generation. Pooled all three (`event_loss_officer` 5 lines with {name}/{post}, `event_loss_member` 5 with {name}, `life_support_loss` 4 with {losses}), indexed by log length / year so consecutive fires vary — a disaster-heavy voyage's many losses and a long suffocating stretch now read as the ship keeping its grim ledger rather than a machine reprinting one sentence. Deliberately the *death* lines, since those are the ones a player reads most in a crisis and most needs to not go numb to. Pure JSON + three call-sites, no schema beyond the defaulted pools, no new events (events 279); data-load guards each pool carries its slots ({name}+{post}, {name}, {losses}); a built-in fallback so an empty pool never blanks the log. Deterministic (indexed by log length / year, no RNG). +1 test proving a crashed life-support plant thins the crew and its toll is narrated from the pool, substituted, not a literal (272 total). Determinism preserved.
- 2026-07-24 · campaign skeleton (round 25) · **a ship that cannot make its heading** — the *mobility* reckoning, completing the ship-crisis beats. The it23/it24 hull and air collapse beats force a reckoning when the ship's *integrity* fails — it breaks up, it suffocates — but a ship can be sound and full of air and still be *stranded*: tanks dry, drive cold, falling through the dark on momentum alone, unable to burn or correct. That third mortal failure, *mobility*, had no beat, and fuel — the resource it turns on — was the least-coupled of all (acute crises only, no sustained state). Added a **becalmed beat** (`fire_becalmed_beat`): a new `SimState.fuel_stall_years` counter (incremented each year a Travel leg stalls dry, reset the moment the ship burns again — the fuel twin of it89's `lean_food_years`) drives it, and once the ship has been becalmed `becalmed_beat_years` (5) running, a beat is forced from `becalmed_beat_family` (survival); a `becalmed_beat_band` tracker fires it once per stranding and re-arms when the drive lights again. It's a genuinely *different* ship-crisis axis from the integrity collapses — a ship breaking up and a ship going nowhere are not the same emergency — and it gives fuel its first *sustained-state* mechanic, the counterpart to the acute dry-tank events. One authored survival event carries the reckoning: **The Long Drift** (`fuel_below 0.12` — years becalmed, the drive cold, the crew realizing they may be a becalmed *people*: mount a desperate all-hands scavenge for enough mass for one real burn to take back the heading, or settle into the drift as a way of life, +adaptation and a home that need not be moving to be a home, seeding `settled_into_the_drift`). Data-driven and deterministic (reads the stall counter, no RNG); data-load guards the family has events and the year count is set; the full-voyage soaks (which can starve fuel) silence it alongside the other forced beats. Survival 48→**49**, events 279→**280**. +1 test proving the beat fires once the ship has been stranded the threshold years and re-arms when it burns again (273 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 25) · **the infirmary keeps the crew human, as the archive keeps them themselves** — the physiological twin of a coupling that had stood half-built since round 10. it10 made the education archive's *knowledge* resist the crew's **cultural** drift (a vivid founding memory keeps the people culturally human), and it166's comment explicitly noted the *body's* adaptation happens "whether or not the archive holds" — a gap left open on purpose. But a real generation ship's clinic would *manage* that bodily drift: monitor the bone loss and the gene changes, run the gene-work that keeps a crew fit to one day live on a world. So the medical bay had no business being silent on adaptation. Added `medical_adaptation_resistance` (0.3): the crew's yearly `adaptation` rise now scales by `1 - resistance · medical_bay_knowledge`, the exact mirror of it10's `1 - archive_drift_resistance · education_knowledge` — a ship that keeps its *medical craft* alive holds its crew closer to baseline-human, adapting slower, while a neglected infirmary lets the bodies go shipborn. It reads **knowledge**, not condition, deliberately: like the archive is the *memory* of the founders, the infirmary is the *craft* of the body — the living know-how of keeping people human, which the it13 knowledge keystone can lose and the it64 education slowly re-teach. This completes a clean identity-resistance pair — the archive holds the *culture*, the infirmary holds the *body* — so a ship bound for landfall can now actively keep its colonists fit for a world instead of watching them drift into people who can only live in a hull, and it gives the medical bay's *knowledge* (not just its condition) a standing stake for the first time. Capped a fraction below 1, so even a perfect clinic only *slows* the drift, never stops the bodies adapting. Data-driven and deterministic (reads medical knowledge, no RNG); at zero medical knowledge adaptation runs at the old full rate, so the existing adaptation-crossing content is unchanged where the craft is gone. Mechanics-forward, no new event templates (events 280). +1 test proving a full-knowledge infirmary slows the shipborn drift over twenty years while a craftless one lets it run full, and the bodies still adapt either way (274 total). Determinism preserved.
- 2026-07-24 · provisioning (round 25) · **a voyage that will not move wears the crew like a hunger that will not end** — gave a chronic becalming its *systemic toll*, the standing cost beside this session's it25 becalmed *beat*. The becalmed beat (campaign r25) forces a *reckoning* the moment the ship has been stranded long enough, and it built the `fuel_stall_years` counter to drive it — but a stranding that has already been reckoned with keeps grinding on, year after year, and nothing modelled the slow despair of it. Added `becalmed_morale_drain` (0.005): once the ship has been stalled dry past `chronic_hunger_years` (5) running — the same "chronic" threshold the food system uses — the year tick wears a little morale each year the becalming holds, the exact fuel/mobility mirror of it89/round-17's `chronic_hunger_morale_drain`. So fuel now has the same two-layer shape food does: an acute crisis (the dry-tank events), a guaranteed reckoning (the becalmed beat), *and* a grinding systemic toll (this) — a ship going nowhere loses heart the way a ship going hungry does. It pairs the beat and the drain the way the food axis pairs its famine events and its chronic drain: the beat is the moment the crew *confronts* the stranding, this is the weight they carry every year it does not lift, and via the it103 ship-mood voice a long enough becalming now audibly pulls the decks into a grim band. Threshold-gated so a bad month coasting is inert — only a *sustained* stranding bites — and reset the instant the drive lights again. Deterministic (reads the stall counter, no RNG); the soaks keep fuel up so `fuel_stall_years` stays 0 and the drain never fires, all green. `#[serde(default)]` 0 = a becalming costs no morale. Data-load guards the drain a gentle yearly attrition. Mechanics-forward, no new event templates (events 280). +1 test proving a ship still becalmed ends a year grimmer than one that burns again, the gap exactly the year's drain (275 total). Determinism preserved.
- 2026-07-24 · factions (round 26) · **a people that merges knits the crew together as one that leaves tears it** — completed the cohesion picture of *losing* a people, from the gentler side. This session's it24 departure scar made a people *leaving* — a secession into the dark, a settlement that stays — a wound to unity: a hole in the community. But the ship loses a people two ways, and the other, soft **assimilation** (the W7 generation-boundary fold of a tiny, far-drifted remnant into the largest people), left cohesion untouched though it is the *opposite* motion — no one departs, the members stay aboard, a faint distinct culture simply blends into the majority. Added `assimilation_unity_lift` (0.08): each such fold now *lifts* unity, scaled by the assimilated share, the exact positive mirror of the departure scar — where a people leaving tears a faultline open, a people merging closes one, the crew a step more one people. So the two kinds of loss now read as the opposite cohesion events they are: a departure is a rupture (unity **down**, it24), an assimilation a consolidation (unity **up**), and the same drift that drives a tiny remnant to blend (the it W7 gate) is now the drift that quietly makes the ship more unified as its diversity narrows — a bittersweet completeness (the polity easier to hold, the founders' many peoples fewer). Gentle and share-scaled, so a vanishing sliver barely registers while a larger merge knits more together; it composes with the it23 rival/ally cohesion and the it unity voice (a big fold can lift the ship into a cohering band the decks then remark). Deterministic (reads the merged share, no RNG); assimilation only fires at a generation boundary past the high drift gate, so the silent soaks (which don't drift that far) never trigger it, all green. `#[serde(default)]` 0 = an assimilation is the bare headcount move it always was. Data-load guards the lift gentle. Mechanics-forward, no new event templates (events 280). +1 test proving a tiny remnant folding into the majority lifts unity (276 total). Determinism preserved.
- 2026-07-24 · charters (round 26) · **fit for the mission before you may even take it** — the drydock's coupling to charter *availability*, the last unbuilt half of the loadout↔charter link. The loadout already decided how *well* a fitted ship works a mission — a fast engine, heavy guns, a deep hold each quicken the objective (it21/it166/it) — but never whether the ship could *take* one, though a megahaul plainly needs a hold to carry it and an enforcement writ the guns to serve it. Added `min_combat`/`min_cargo`/`min_speed` to `ContractTemplate` and a `meets_loadout_gate` (kept a *separate* gate from the it12 in-world one, so the writ board can name which of the two bars a locked writ): a writ that names a minimum is offered only to a hull whose aggregate loadout clears each, and the board reads `LOCKED · NEEDS CBT 3` when it doesn't. Wired to two mid-tier writs the fitting should matter for: **the Vane Default** enforcement salvage now demands `min_combat 3` (an unarmed hull cannot be trusted to strip a settlement that will not give up its assets), and **the Karst Works** megahaul demands `min_cargo 250` (a great works needs a great hold to feed it). So the drydock's three-way fitting trade — engine, guns, or hold — now gates *which missions the board even shows*, not only how they go: a captain outfits *toward* the work they mean to fly, and a charter can be a reason to refit. It composes with everything downstream — a ship fit to *take* the enforcement writ is, by the it166 combat coupling, also fit to work it *well* — so availability and accrual pull the same direction. Data-driven and deterministic (reads the loadout, no RNG); the founding-tier writs name no minimum (`#[serde(default)]` 0) so early play is ungated, and the gate is separate so all the existing `meets_in_world_gate` tests are untouched. Data-load: loadout minimums are non-negative. Charters/events unchanged (22/280 — fields on 2 charters). +1 test proving the enforcement writ is barred to an unarmed hull, open to an ungated founding writ, and unlocked the moment a real weapon is fitted (277 total). Determinism preserved.
- 2026-07-24 · event families (round 27) · **the seeds this session planted, come due** — landed the three chains the fifth/sixth rotation's content rounds had deliberately seeded, each a *long*-horizon payoff (generations, not decades) that only a clock and a consequence-ledger together can carry. it26 left `hailed_the_watchers` (a ship that greeted a silent watching intelligence) and `archive_given_to_strangers` (a ship that traded its whole history for a shorter road); it25's becalmed content left `settled_into_the_drift` (a stranded crew that made peace with going nowhere). Authored the three terminal payoffs (`requires_consequence`-gated, so each waits for its seed and never appears otherwise): **The Watchers Answer** (`hailed_the_watchers`, science_anomaly — two generations on, the watchers reply, but in a signal of impossible density the crew can tell is *meaning* without being able to *read*: bend the whole ship to decoding an alien language and be changed by the learning, or log the answer whole and unread for grandchildren who may hold the key), **The Archive Returns** (`archive_given_to_strangers`, mystery — an automated craft returns the traded archive *annotated in an alien hand*, the ship's own founding catalogued by outsiders who saw patterns it never could: take the mirror and see itself from without, no longer quite believing its own story, or refuse it and keep a memory warmer and less true), and **The Drift People** (`settled_into_the_drift`, legacy_drift, `min_generation 3` — the stillness could end, a chance to burn again appears, but the crew are a drift-people now, born to it: light the drive and drag a reluctant people back onto a heading, or let the chance pass and stay in the home they made of going nowhere). Each is *terminal* (seeds no new tag), netting the ledger down. Deliberately spread one into science_anomaly (the thinnest family, 13→14) to grow it. Data-driven and deterministic (the gates read the consequence ledger, no RNG). Science_anomaly 13→**14**, mystery 15→**16**, legacy_drift 66→**67**, events 280→**283**. +1 test proving The Drift People reckoning waits for the drift to be chosen and opens the moment it is on record (278 total). Determinism preserved.
- 2026-07-24 · voice (round 25) · **the crew notices its own bodies becoming a new kind of people** — gave the last core stat without a voice one. The crew-state voices had grown to cover the *spirits* (it103 morale), *cohesion* (it unity), *political climate* (it104 polity), *name* (it111 reputation), *government* (it134 stability), and *belief in the founders' cause* (it167 loyalty) — but **adaptation**, the crew's slow *physiological* drift from the baseline-human stock the founders launched, went its whole long way in silence, though "a generation ship remaking its people in the flesh" is as central a Pillar as any. Added the **adaptation voice** (`announce_adaptation_mood`, mirroring the it167 loyalty voice on `adaptation`): new `crew_shipborn`/`crew_baseline` pools (4 lines each) + `adaptation_voice_high` 0.55 / `_low` 0.2 + a `SimState.adaptation_voice_band` tracker, announced right after the year's voyage drift (and this session's it25 medical resistance to it) settles. When the descendants cross into a *shipborn* body the decks remark the medics calling it a lineage — taller, lighter-boned, senses re-tuned to spin and recycled air, a people fitted to the ship and fit for nowhere else, the old dream of walking out onto open ground acquiring a note of doubt; and when a well-kept infirmary *holds* them to the founders' baseline (the rarer crossing, and the reward for the it25 medical craft) the decks remark a crew kept planet-fit for grandchildren who will finally stand on a shore. It's the **body** to the loyalty voice's **belief** — a crew can still *believe* in the founders' cause while ceasing to be, in the flesh, the founders' kind — and it composes with the it25 coupling (a ship investing in its infirmary now *hears* its crew held human). It completes the crew-state voice set: spirits, cohesion, polity, name, government, belief, and now the body. The launch band (a founding crew is baseline-human) is recorded not announced; a return to the middle re-arms; being a plain announcement it needs no soak guard. Deterministic (reads adaptation, indexed by year, no RNG); `#[serde(default)]` empty pool = inert. Data-load: both band pools stocked and the thresholds ordered when enabled. Events unchanged (283). +1 test proving a founding crew is silent, a shipborn one says so once, staying shipborn doesn't reprint, and a crew held to baseline says so afresh (279 total). Determinism preserved.
- 2026-07-24 · campaign skeleton (round 26) · **the crew becomes so much the ship's own kind that a planet is no longer possible** — gave the just-voiced adaptation stat its *beat*, exactly as the hull got a voice (it r22) then a beat (it r23). The ship-crisis beats reckon with the vessel's *body* failing — hull breaks up (it r23), air suffocates (it r24), drive goes becalmed (it r25) — but the *crew's* body had only the gentle ascending `adaptation_beats` milestones (the descendants growing suited to the ship), and no **terminal** reckoning for the point where they have grown *too* suited: so shipborn that an open sky would kill them, the founders' whole purpose — make landfall, settle a world — quietly become physically impossible. Added the **adaptation-divergence beat** (`fire_divergence_beat`, the high-side crew-body twin of the it23/it24/it25 ship-body crisis beats): the month `population.adaptation` first rises to or above `divergence_beat_threshold` (0.82 — past the it25 shipborn *voice* line, the crew now not merely changed but *diverged*), a beat is forced from `divergence_beat_family` (legacy_drift); a `SimState.adaptation_divergence_band` tracker fires it once per crossing and re-arms if a strong infirmary holds the line back below (the reward for the it25 medical resistance — a bay can *un-fire* this reckoning). Added a new high-side event gate — `EventTemplate.adaptation_above` (the mirror of the it23 `hull_below`/it24 `life_support_below` collapse gates) — so the reckoning surfaces *divergence* content, and two authored legacy_drift events give it substance: **The People of the Ship** (`adaptation_above 0.82` — the medics confirm the crew can no longer survive an open sky: strike the planetfall clause and declare the ship itself the destination, +unity/+adaptation but the founding purpose struck from the charter, or commit the infirmary to a baseline seed-population kept planet-fit at cost so the promise is not physically dead) and **The Open-Sky Fear** (`adaptation_above 0.82` — the shipborn young recoil bodily from an unroofed horizon: let the open-sky training bay go, or drill against the reflex to keep landfall an option for the stubborn). It composes straight down the it-session chain: the it25 voice *murmurs* the drift, this beat forces its point of no return, and both couple to the it25 medical craft (a strong bay slows the drift, quiets the voice, and can re-arm the beat). Completes the ship-and-crew *body* crisis-beat set — frame, air, mobility, and now the flesh. Data-driven and deterministic (reads adaptation, no RNG); data-load guards the family has events, a red line in (0,1), and an `adaptation_above` event to surface; the full-voyage soaks (whose rising adaptation trips it) silence it alongside the other forced beats. Legacy_drift 67→**69**, events 283→**285**. +1 test proving the beat fires once as the crew crosses fully shipborn and re-arms when the infirmary holds it back (280 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 26) · **the fabrication hall finally does the fabricating** — closed the gap where the ship's own manufacturing ignored the module it physically lives in. The it21 provisioning fabrication run (idle reactor surplus + raw ore → spare parts, the voyage's one wasted resource put to work) turned out a *flat* yield no matter the state of the **engineering bay** — yet the engineering bay *is* the fabrication hall, its top tier literally "able to remanufacture nearly anything the ship is made of." A wrecked foundry and a pristine one fabricated exactly alike. Added `engineering_fabrication_factor` (the manufacturing twin of `agriculture_condition_food_factor`): the bay's condition scales the run's yield by `1 - engineering_fabrication_penalty·(1 - condition)` (penalty 0.6, floored at 0), and the caller floors the run at one part — so a sharp hall turns out the full run, a neglected one a diminished one, a wrecked one only what improvised hands can, but a run that happens at all always yields *something*. It's a fourth reason the it62 engineering keystone earns its upkeep: neglect the bay and it already speeds every module's decay (it62), burns more fuel (it), and hastens the hull toward collapse (it24) — now it also *starves the ship's spare-parts supply*, the very stock those repairs consume, tightening the neglect spiral into a genuine trap (a failing bay makes fewer parts *and* needs more of them). Full-condition (the launch state) leaves the factor at 1.0, so the it21 fabrication tests and every fresh-campaign path are unchanged; `#[serde(default)]` 0-penalty disables it. Data-driven and deterministic (reads condition, no RNG); data-load guards the penalty a fraction in [0,1]. Mechanics-forward, no new event templates (events 285). +1 test proving a sharp hall fabricates the full run, a failing one less, and a wrecked one the least but never a negative yield (281 total). Determinism preserved.
- 2026-07-24 · provisioning (round 26) · **political capital is only as real as the council that mints it** — gave the last flat-generated resource an income coupling, and the one that closes a governance spiral. The provisioning axis had built rich systems around *food* (scarcity, plenty, spoilage), *energy* (the it21 fabricators), *fuel* (the it25 becalming), and the *market* (it22) — but **influence**, the ship's political capital, was still produced dead-flat: `production.influence × crew_skill`, blind to whether the ship was actually *governable*. Yet influence is the one resource whose whole meaning is institutional — a council that cannot reach quorum cannot issue the authority its officers spend. Added `influence_governance_factor`: influence income now scales with `stability` — at or above the governance line (`influence_governance_threshold` 0.5) the ship mints the full amount (inert), and below it the factor falls linearly from 1.0 at the line toward `influence_governance_floor` (0.25) at zero stability, so even an ungoverned ship earns a little raw standing but never zero and never more than full. It's the governance twin of this session's it26 fabrication trap, and a genuine spiral: a ship whose institutions are failing earns *less* of the very influence its council-mediation and reform choices cost to spend — so governance decline makes its own recovery dearer, exactly as a rotting foundry starves the parts its repairs need. It composes with the it15/it16 stability system (the it108 security corps that *recovers* stability now also protects the ship's influence income) and gives the it102 stability stat a provisioning consequence it never had. Launch stability (0.7) sits above the line, so the factor is 1.0 on every fresh campaign and no economy path changes until a ship actually slips; `#[serde(default)]` 0-threshold disables it. Data-driven and deterministic (reads stability, no RNG); data-load guards the line a fraction in (0,1) and the floor in [0,1) strictly below it (so the factor is continuous and never inverts). Mechanics-forward, no new event templates (events 285). +1 test proving a governed ship mints full influence, a slipping one earns proportionally less, and a collapsed one earns exactly the floor — never zero (282 total). Determinism preserved.
- 2026-07-24 · factions (round 27) · **a people is not only a source of the ship's character but a judge of it** — closed the `reputation_leanings` loop the it16 round left half-drawn. Round 16 gave the *dominant* people the power to bend the ship's reputation toward its `reputation_leanings` (`apply_dominant_reputation_lean` — a kind majority grows a merciful name, a cold one hardens it) — but that flow was strictly *ship←faction*: the reputation the ship actually earned was never *felt* back by the peoples who hold those very leanings, though the same `reputation_leanings` data plainly encodes what each people *values*. Added `apply_reputation_alignment_sentiment` (the exact reverse, wired in right beside its round-16 twin so the pair reads as one bidirectional coupling): each year every aboard people's *approval* drifts by `reputation_alignment_approval_scale · Σ leaning · (reputation − 0.5)·2` over its leanings — a merciful ship (the it16 traits recentred to [−1,1]) *contents* the mercy-prizing Hearth (mercy +0.5) and *sours* the mercy-scorning Ascension (mercy −0.4); a ruthless ship does the reverse. So the ship's name is now a standing approval force, and the two halves interlock into a slow feedback the sim never had: a dominant people drifts the ship toward its own values (it16), which in turn *contents that people and its like-minded* while *souring its opposites* — a majority's character becoming self-reinforcing among its kind and self-alienating among its rivals, precisely the political drift a generation ship would live. It also gives the it105/it16 reputation system its first coupling *back into* the faction politics it emerges from, and hands the player a real lever: the reputation you build by your event choices is now also a tool for keeping the peoples you need onside (or a liability with the ones you don't). A neutral character (every trait 0.5 — the launch state) shifts no one, so no fresh-campaign path changes until the ship has earned a name; `#[serde(default)]` 0-scale disables it. Data-driven and deterministic (reads reputation + catalog leanings, no RNG; matches the unguarded factions-config scalars, so no bespoke data-load guard). Mechanics-forward, no new event templates (events 285). +1 test proving a merciful ship warms the mercy-prizing and cools the mercy-scorning, a ruthless one flips both signs, and a neutral character moves no one (283 total). Determinism preserved.
- 2026-07-24 · charters (round 27) · **a well-armed ship makes a lawless route think twice** — gave the combat stat the one role its whole nature demanded and it lacked: *defending against the route's danger*. The recent charter rounds had built the ship's **combat** rating into a full loadout↔charter citizen — it quickens a *contested* objective (it21 accrual), backs a Wanderer's gamble (dilemma odds), and gates the enforcement writs (it26 min_combat) — but the route **hazard** system (it11: a dangerous writ adds a standing crisis-weight for its whole voyage) was answered only by the *security subsystem's* condition (it21 `security_crisis_mitigation`, the ship's internal *order*). The ship's actual *guns* — the literal deterrent to the scavengers and raiders a lawless route is dangerous *with* — did nothing about that danger, though deterring it is precisely what weapons are *for*. Added `hazard_combat_mitigation`: in the crisis-weight roll the route's hazard is now cut by `combat · this` (0.03/point) before it lands, floored at 0 — so a survey (hazard 0) is unaffected, a moderately-armed ship meaningfully quiets a derelict-field run (hazard 0.28), and a heavily-armed one (a 7-combat spinal railgun) can deter the whole of a route's added risk, never dropping crises below the ship's own base rate. It's the *direct-firepower twin* of the security corps' mitigation — guns deter the **route's** danger, the corps quiets **every** crisis — and it completes combat's arc: the guns you fit in dock now decide which missions go *well* (it21), which you may *take* (it26), *and* how *safely* you run the dangerous ones. A real drydock trade sharpens: firepower is no longer only offense and gate-key but *insurance* on a hazardous charter. (Threaded the ship's live combat into `category_weights` by passing it `&GameData`, updating its three call sites.) Data-driven and deterministic (reads the loadout's combat + the contract hazard, no RNG); a fresh unarmed hull has combat 0 so the it11 hazard math is unchanged until a gun is fitted, and `#[serde(default)]` 0 disables it; data-load guards the per-point deterrence a gentle fraction in [0, 0.2]. Mechanics-forward, no new event templates (events 285). +1 test proving an unarmed hull takes a route's full hazard, a gun aboard breeds fewer crises, and the drop is exactly combat×mitigation up to cancelling the hazard (284 total). Determinism preserved.
- 2026-07-24 · event families (round 28) · **the thinnest family, and its first crises that escalate** — grew `science_anomaly`, now the floor family (14, after it26 lifted exploration off it) and, like that one, the *shallowest*: all 14 events were flat 2-outcome binaries with **zero complications** — the only family without a single one, so a science anomaly never *deepened* with the ship's state the way a plague or a schism does. Added four events (14→**18**) built to fix exactly that, giving the family its first six complications and a self-contained chain. **The Slow Pulse** (a too-regular signal from the deep; 3 outcomes — turn the archive to it, log it, or answer in kind) carries two complications that make the *same* anomaly read differently by ship: a worn `education_culture` archive (`condition_below 0.4`) can barely decode it, and a drift-far crew (`min_cultural_drift 0.6`) hears not a signal but *a voice, a summons* — the family's science bent against its myth. It seeds `chased_the_slow_pulse`, paid off a generation on by **What the Pulse Was** (`requires_consequence` + `min_generation 3`, a self-contained chain — seed and payoff both added, no new orphan): the pulse was a *beacon over a grave*, a dead ship's forever-headstone — with a complication where a dominant Ascension Circle insists it is not a grave but a *mind still running*, kin, the immortality they reach for. **The Ringing Hull** (a resonance field that will not let the ship be quiet; damp it / endure it / *tune the ship to it* as an instrument) complicates on a wrecked `engineering_bay` (can only half-damp the note) and on **recurrence** (`min_prior_occurrences` — a ship that has rung this stretch before dreads it worse). **The Cold Lantern** (a cold body that shines from no fire) rewards a ship that has *made a name for marvels*. That last needed a real trait to gate on, so the cluster also **introduces and builds a new reputation trait, `wonder`**: the three "study/chase" outcomes now earn it, so a science-hungry ship accumulates a name for wonder that the it105/it16 reputation system, the it27 faction-alignment sentiment, and this very family's own complication all then read — a family that at last *shapes* the ship's character, not just spends a season on a marvel. Data-driven (pure content + one trait); the chain is deterministic (a state gate, no RNG). +1 test proving the pulse's answer waits until the ship chose to chase it and the seed event is offered to any ship (285 total). Events 285→**289**. Determinism preserved.
- 2026-07-24 · voice (round 26) · **the crew notices it has become, in custom and memory, a new people** — gave `cultural_drift` the gentle *voice* it lacked, the exact cultural twin of the it25 adaptation voice (their *bodies*). The identity-drift stats had grown lopsided: **adaptation** got both a voice (it25) and a beat (this session's it26 divergence beat), but **cultural_drift** — the founders' customs, calendars, and tongue fading into something new — had only its it2 milestone *beats*, no quiet crossing-voice, though it is the more legible half of "a generation ship becoming its own civilization." Added the **cultural-drift voice** (`announce_drift_mood`, mirroring `announce_adaptation_mood` on `cultural_drift`): new `culture_newfound` / `culture_founders_kept` pools (4 + 3 lines) + `drift_voice_high` 0.6 / `_low` 0.15 + a `SimState.drift_voice_band` tracker, announced in the year tick right after the it25 body voice — the two identity-drift voices now side by side, the flesh and the culture. When the crew's customs cross into a *people the founders would not recognise* (drift ≥ 0.6 — the calendar keyed to the ship's cycles, the old logs needing translation for the young, the founders honored ancestors and genuine strangers) the decks remark it once; and when a well-kept archive *holds the founders' ways close* (drift ≤ 0.15, the rarer crossing and the reward for the it10 archive drift-resistance, exactly as the adaptation voice's baseline pool is the reward for the it25 medical resistance) the decks remark the old customs kept on purpose against a distance meant to erase them. The two poles complete the identity-drift picture: a crew can become new in the *body* (it25) while holding the old *culture*, or the reverse — the archive and the infirmary keep different halves of the founders' people. The launch band (a founding crew keeps the founders' ways; drift launches at 0.1, below the low line, so band −1 is recorded not announced) re-arms on a return to the middle; being a plain announcement it needs no soak guard. Deterministic (reads cultural_drift, indexed by year, no RNG); `#[serde(default)]` empty pool = inert. Data-load: both band pools stocked and the thresholds ordered when enabled (mirrors the it25 guard). Events unchanged (289). +1 test proving a founding crew's culture is silent, drifting into a new people says so once, staying does not reprint, and keeping the old ways afresh says so (286 total). Determinism preserved.
- 2026-07-24 · campaign skeleton (round 27) · **the crew can no longer read the reason it is out here** — gave `cultural_drift` its *terminal* beat, the exact cultural twin of the it26 adaptation-divergence beat (their *bodies*), completing the voice→beat pattern this session ran for both identity-drift stats (adaptation: it25 voice → it26 beat; cultural_drift: it26 voice → it27 beat, now). The it26 divergence beat reckons when the crew's bodies have grown so shipborn a planet is *physically* impossible; this reckons when their *culture* has drifted so far the founders' charter is a **dead language** — the mission carried by rote by people who no longer source its *why*. Added the **cultural-divergence beat** (`fire_cultural_divergence_beat`): the month `cultural_drift` first rises to or above `cultural_divergence_beat_threshold` (0.88 — set *above* the top it2 `drift_beats` milestone 0.85, so it is the terminal mark and not another rung, a data-load invariant now enforced) a beat is forced from `cultural_divergence_beat_family` (legacy_drift); a `SimState.cultural_divergence_band` tracker fires it once per crossing and re-arms if a strong archive (the it10 drift-resistance) revives the old ways back below the line — a bay can un-fire the body reckoning, an archive the cultural one. It **reuses the existing `min_cultural_drift` event gate** (no new gate field, cleaner than the it26 beat's bespoke `adaptation_above`), and two authored legacy_drift events gated at `min_cultural_drift 0.85` give it its substance, distinct from the it schism/exodus content (faction splits) by being about *meaning-loss*: **The Untranslatable Charter** (the founding charter can no longer be read in a tongue that has drifted past it — translate it into the living language and lose the founders' exact words, or enshrine the original as a sacred unread relic and let the mission run on faith) and **The Reason Forgotten** (a young officer asks *why are we going where we are going* and no one can answer — dig the reason back out of the archive, or accept that the doing is enough without the meaning). The two divergence beats now bracket the whole identity: a crew can pass the point of no return in *body* (it26) or in *memory* (it27), the infirmary and the archive each guarding a different half of the founders' people, each able to un-fire its own reckoning. Data-driven and deterministic (reads cultural_drift, no RNG); data-load guards the family has events, a red line in (0,1) at/above every drift milestone, and a high-`min_cultural_drift` event to surface; the full-voyage soaks (whose rising drift trips it) silence it alongside the other forced beats. Legacy_drift 69→**71**, events 289→**291**. +1 test proving the beat fires once as the charter goes unreadable and re-arms when the archive revives the old ways (287 total). Determinism of the tick preserved.
- 2026-07-24 · subsystems (round 27) · **the schools finally teach the training** — gave the **education/culture** module a third active role, on the one verb it thematically owned but did not touch. Education was already two keystones — the *generational* knowledge keystone (`transmit_knowledge`: its condition scales how much craft *every* module passes to the next generation, it13) and the it10 archive drift-resistance (this session's cultural-drift voice/beat lean on it) — but the **deliberate** `train_subsystem_knowledge` verb, the drydock action that trains a fresh cohort up on a module's craft for credits, imparted a *flat* `train_knowledge_gain` no matter the state of the schools, though an academy is precisely what training *is*. Added `education_training_factor`: a training cohort's gain now scales by the education module's condition (`1 - education_training_penalty·(1 - condition)`, penalty 0.5), so a true academy trains a crew to the full craft while a crumbling one — rote lessons, lost method, no masters to apprentice under — imparts only a fraction. It closes a satisfying loop distinct from the two keystones: transmission is what passes on *by default* each generation, this is what an *active* investment buys, and both now answer to the same schools — so letting the education module rot doesn't only bleed craft down the generations (it13) and speed cultural drift (it10), it also blunts your ability to *deliberately rebuild* any module's lost knowledge, the very tool you'd reach for to recover. The penalty is kept strictly below 1 (a data-load invariant) so even a wrecked academy still teaches *something* and a crew can always bootstrap its schools back — no repair deadlock (train needs knowledge, the academy needs condition, and condition is restored by *repair*, not training). Full-condition (the launch state) leaves the factor at 1.0, so the existing training/repair test and every fresh-campaign path are unchanged; `#[serde(default)]` 0-penalty disables it. Data-driven and deterministic (reads condition, no RNG). Mechanics-forward, no new event templates (events 291). +1 test proving a true academy trains the full cohort, a wrecked one still teaches but less, and an actual training run imparts less knowledge on a wrecked academy than a sound one (288 total). Determinism preserved.
- 2026-07-24 · provisioning (round 27) · **a home that will not stay whole wears the crew like a hunger that will not end** — completed the *sustained-privation morale* trio, the pattern the axis had drawn for two of the ship's three great stores and left undrawn for the third. Hunger has a chronic cost (it89/round 17: `lean_food_years` past the line grinds morale) and so does becalming (round 25: `fuel_stall_years` past the line grinds morale) — the *larder* and the *drive* — but the **toolroom** had none: the ship's spare-parts upkeep was a *binary* yearly check (`maintained`? then wear is relieved, else full), with no memory of how *long* the ship had gone unmended and no standing cost for a chronic disrepair, though a ship held together with tape for a generation is exactly the kind of slow privation the other two model. Added a new `SimState.lean_parts_years` counter (the parts twin of `lean_food_years`/`fuel_stall_years` — incremented each year the stores fall short of upkeep, reset the moment they can cover it) and a `disrepair_morale_drain`: once the ship has run unmended past the same `chronic_hunger_years` line the other two use, morale bleeds a gentle amount each year — the crew watching their home slowly come apart, deck plates buckled and seals weeping, with nothing to fix them. It completes the symmetry exactly: the three things a generation ship can run out of and cannot quickly replace — food, fuel, and the parts to mend itself — now each carry both an *acute* cost (famine deaths, a dead-drive stall, accelerated wear) and a *chronic* morale one, so a captain who lets any of the three grind on for a generation pays for it in the crew's heart. Threshold-gated on the shared "years-to-chronic" line, so one lean year between resupplies is inert; only a sustained disrepair bites. Gentle by design (0.004/yr, matching the hunger drain); `#[serde(default)]` 0 disables it. Data-driven and deterministic (reads the parts counter, no RNG); data-load guards the drain a gentle attrition in [0, 0.05] (mirrors the it25 becalmed guard). Mechanics-forward, no new event templates (events 291). +1 test proving a ship still short of its upkeep loses heart where one that can cover it does not, the gap exactly the year's disrepair drain (289 total). Determinism preserved.
